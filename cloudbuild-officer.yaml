steps:
  # Build the Officer Portal Docker image with API URL baked in
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/puda-officer:$COMMIT_SHA"
      - "--build-arg"
      - "VITE_API_BASE_URL=${_API_URL}"
      - "-f"
      - "Dockerfile.officer"
      - "."

  # Push image to Container Registry before deploying
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/puda-officer:$COMMIT_SHA"

  # Deploy to Cloud Run
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - "run"
      - "deploy"
      - "puda-officer"
      - "--image"
      - "gcr.io/$PROJECT_ID/puda-officer:$COMMIT_SHA"
      - "--region"
      - "asia-southeast1"
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"

images:
  - "gcr.io/$PROJECT_ID/puda-officer:$COMMIT_SHA"

substitutions:
  _API_URL: ""

options:
  logging: CLOUD_LOGGING_ONLY
