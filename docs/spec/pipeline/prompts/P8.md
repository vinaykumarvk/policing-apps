/goal
  objective: Adapt DOPAMS and IQW to validate platform claims server-side for platform-launched routes.
  context:
    - docs/spec/phased-plan.yaml phase P8
    - docs/spec/auth-entitlements-contract.md
    - docs/spec/auth-claim-fixtures.json
    - packages/authz from P5
    - apps/platform-api app registry behavior from P6
  constraints:
    - Local/domain auth cannot bypass platform decision evidence for platform-launched routes.
    - Break-glass or bootstrap paths must be audited and time-boxed.
    - Wrong module, wrong jurisdiction, wrong clearance, stale claim, and revocation cases must deny.
    - Do not make Social Media, Forensic, or Knowledge active.
  freedom:
    - Implement adapters as idiomatic TypeScript middleware for DOPAMS and Python/FastAPI middleware for IQW.
    - Choose gateway versus in-service placement if the server-side enforcement remains explicit and tested.
  work_loops:
    - name: Adapter implementation
      max_iterations: 3
      repeat_until: DOPAMS and IQW platform auth adapters and cross-runtime tests exist and pass.
      steps:
        - implement apps/dopams-api/src/middleware/platform-auth.ts
        - document docs/spec/iqw-platform-auth-adapter.md
        - implement domains/iqw-api/src/middleware/platform_auth.py
        - add DOPAMS and IQW platform auth tests
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p8.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p8.sh
        - repair bypass or negative-test gaps
        - confirm app registry can mark DOPAMS and IQW pilot only after adapter checks pass
  evidence_required:
    - apps/dopams-api/src/middleware/platform-auth.ts
    - docs/spec/iqw-platform-auth-adapter.md
    - domains/iqw-api/src/middleware/platform_auth.py
    - apps/dopams-api/src/__tests__/platform-auth.test.ts
    - tests/python/test_iqw_platform_auth.py
  escalate_when:
    - either runtime cannot validate platform claims server-side
    - existing local auth would be weakened
    - break-glass behavior cannot be audited

