/goal
  objective: Decide the long-term knowledge runtime and prove retrieval security before enabling any platform knowledge UI.
  context:
    - docs/spec/phased-plan.yaml phase P10
    - docs/spec/access-control-threat-model.md
    - docs/spec/data-classification-policy.md
    - docs/spec/pilot-fixtures.md
    - Existing RAG-app and KIS knowledge runtime materials
  constraints:
    - Knowledge query UI remains disabled unless pre-retrieval scope filtering and citation filtering are proven.
    - Scoped retrieval must happen before vector or graph search.
    - Citations must be filtered before response and audited.
    - Do not ingest real sensitive case data in this phase.
  freedom:
    - Choose comparison criteria and scoring format if governance, retrieval security, snapshots, citations, PII, deployment, and tests are covered.
  work_loops:
    - name: Runtime decision
      max_iterations: 3
      repeat_until: knowledge decision, ingestion event schema, retrieval security doc, and scope contract tests exist.
      steps:
        - compare RAG-app and KIS against the required criteria
        - write docs/spec/knowledge-runtime-decision.md
        - write docs/spec/knowledge-ingestion-event.schema.json
        - write docs/spec/knowledge-retrieval-security.md
        - add scope contract and selected-runtime retrieval tests
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p10.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p10.sh
        - fix missing retrieval or citation criteria
        - keep platform knowledge route disabled if proof is incomplete
  evidence_required:
    - docs/spec/knowledge-runtime-decision.md
    - docs/spec/knowledge-ingestion-event.schema.json
    - docs/spec/knowledge-retrieval-security.md
    - apps/platform-api/src/__tests__/knowledge-scope-contract.test.ts
    - selected runtime retrieval scope tests
  escalate_when:
    - scoped retrieval cannot be enforced before search
    - citation filtering cannot be audited
    - runtime selection needs stakeholder approval not represented in existing docs

