/goal
  objective: Define data classification, ABAC, threat model, and immutable authorization decision evidence before active platform routes.
  context:
    - docs/spec/phased-plan.yaml phase P3
    - docs/spec/auth-entitlements-contract.md
    - docs/spec/auth-claim-fixtures.json
    - docs/spec/manifest.json traceability requirements R-SEC-001, R-SEC-002, and R-DATA-001
  constraints:
    - Every pilot read path must deny by default when policy inputs are missing or stale.
    - Every allow and deny must record claims snapshot, policy version, source/projection version, redaction decision, path, outcome, and correlation ID.
    - Do not expose evidence storage_uri from central APIs by default.
    - Do not activate app registry routes in this phase.
  freedom:
    - Choose ABAC implementation structure and audit ledger types while keeping them independent from domain internals.
  work_loops:
    - name: Policy and evidence design
      max_iterations: 3
      repeat_until: threat model, data classification policy, decision evidence contract, ABAC implementation, and audit evidence implementation exist.
      steps:
        - write docs/spec/access-control-threat-model.md
        - write docs/spec/data-classification-policy.md
        - write docs/spec/authorization-decision-evidence.md
        - implement packages/authz/src/abac.ts
        - implement packages/audit-ledger/src/decision-evidence.ts
        - add negative ABAC and decision evidence tests
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p3.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p3.sh
        - repair missing negative cases
        - verify every decision evidence field is tested
  evidence_required:
    - docs/spec/access-control-threat-model.md
    - docs/spec/data-classification-policy.md
    - docs/spec/authorization-decision-evidence.md
    - packages/authz/src/abac.ts
    - packages/audit-ledger/src/decision-evidence.ts
    - packages/authz/src/__tests__/abac.negative.test.ts
    - packages/audit-ledger/src/__tests__/decision-evidence.test.ts
  escalate_when:
    - policy cannot resolve legal hold, redaction, stale projection, or jurisdiction conflicts
    - a route can be allowed without complete decision evidence
    - data classification cannot be defined without real case data

