/goal
  objective: Define the target monorepo layout and workspace structure without breaking existing app commands.
  context:
    - docs/spec/phased-plan.yaml phase P1
    - docs/spec/source-inventory.md
    - docs/spec/import-map.yaml
    - docs/spec/policing-platform-integration-execution-plan.md
  constraints:
    - Do not import source subtrees unless P0 is approved.
    - Preserve existing app run commands or document replacements explicitly.
    - Do not introduce package ownership crossings without a shared package boundary.
    - Do not add production dependencies unless justified in docs/spec/repo-layout.md.
  freedom:
    - Choose npm workspace layout, domain folder names, and package boundaries consistent with the plan.
    - Add package scripts needed for later phases when they are safe no-op or validation scripts.
  work_loops:
    - name: Layout definition
      max_iterations: 3
      repeat_until: docs/spec/repo-layout.md and workspace package.json define the target structure and validation commands.
      steps:
        - map current apps to apps, domains, packages, and deploy paths
        - define TypeScript and Python ownership rules
        - update or create package.json workspace metadata
        - keep source app command compatibility documented
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p1.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p1.sh
        - fix invalid workspace metadata
        - update docs/spec/repo-layout.md with any caveats
  evidence_required:
    - docs/spec/repo-layout.md
    - package.json
    - docs/spec/import-map.yaml
    - npm run build:packages
    - npm run typecheck
  escalate_when:
    - existing app commands cannot be preserved or replaced safely
    - a layout choice changes auth, API, or DB contracts outside this phase
    - source import requires secret or data handling not approved in P0

