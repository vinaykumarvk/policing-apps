/goal
  objective: Create shared platform service packages for authz, audit ledger, case core, and evidence core.
  context:
    - docs/spec/phased-plan.yaml phase P5
    - docs/spec/auth-entitlements-contract.md
    - docs/spec/authorization-decision-evidence.md
    - docs/spec/data-classification-policy.md
    - docs/spec/repo-layout.md
  constraints:
    - Packages must stay independent from DOPAMS, IQW, Social Media, Forensic, and Knowledge internals.
    - No TypeScript any/as any in new package source or tests.
    - No swallowed errors; define explicit error behavior.
    - Do not add active routes or migrations in this phase.
  freedom:
    - Choose package internal file layout and exported type names consistent with repo conventions.
  work_loops:
    - name: Package implementation
      max_iterations: 3
      repeat_until: all four package manifests and their tests exist and pass.
      steps:
        - create packages/authz/package.json
        - create packages/audit-ledger/package.json
        - create packages/case-core/package.json
        - create packages/evidence-core/package.json
        - expose typed contracts needed by platform API and adapters
        - add package tests listed in the phased plan
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p5.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p5.sh
        - remove any TypeScript any/as any
        - fix failing package tests
  evidence_required:
    - packages/authz/package.json
    - packages/audit-ledger/package.json
    - packages/case-core/package.json
    - packages/evidence-core/package.json
    - npm workspace tests for authz, audit-ledger, case-core, and evidence-core
  escalate_when:
    - shared packages need domain-specific data access
    - package APIs require changing approved auth, audit, case, or evidence contracts
    - a new production dependency is required without approval

