/goal
  objective: Freeze the source scope, complete secret hygiene, and produce a deterministic import allowlist for the policing platform integration.
  context:
    - docs/spec/phased-plan.yaml phase P0
    - docs/spec/policing-platform-integration-execution-plan.md
    - docs/spec/manifest.json
    - Source repos named in the plan: policing-apps, compliant-parser, RAG-app
  constraints:
    - Do not import code into the target monorepo in this phase.
    - Do not rewrite source histories.
    - Do not commit or expose real secrets, .env values, tokens, PII, or active case data.
    - Treat any destructive or irreversible cleanup as a hard stop.
  freedom:
    - Choose the inventory format and checksum algorithm, as long as each selected source path has owner, target path, checksum, import status, and risk notes.
    - Choose the secret scanning command, but record exact command and output summary in the report.
  work_loops:
    - name: Inventory and hygiene
      max_iterations: 3
      repeat_until: docs/spec/source-inventory.md, docs/spec/secret-hygiene-report.md, docs/spec/import-map.yaml, and both check scripts exist and are internally consistent.
      steps:
        - inventory selected source repos and excluded/reference repos
        - run secret scan and document findings without printing secrets
        - write import allowlist with checksums and owners
        - add scripts/check-import-allowlist.mjs and scripts/check-no-known-secret-paths.mjs
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p0.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p0.sh
        - fix missing artifacts or invalid YAML
        - record unresolved risks in docs/spec/secret-hygiene-report.md
  evidence_required:
    - docs/spec/source-inventory.md
    - docs/spec/secret-hygiene-report.md
    - docs/spec/import-map.yaml
    - scripts/check-import-allowlist.mjs
    - scripts/check-no-known-secret-paths.mjs
  escalate_when:
    - a real secret or sensitive local file is found and cannot be quarantined safely
    - repo inclusion remains ambiguous after checking existing artifacts
    - a destructive source-history or filesystem operation is needed

