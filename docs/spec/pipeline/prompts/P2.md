/goal
  objective: Define the canonical platform claims and entitlement contract with versioned TypeScript and Python fixtures.
  context:
    - docs/spec/phased-plan.yaml phase P2
    - docs/spec/import-map.yaml
    - docs/spec/policing-platform-integration-execution-plan.md
    - docs/spec/manifest.json traceability requirements R-SEC-001 and R-SEC-002
  constraints:
    - Deny by default for missing, stale, incompatible, or ambiguous claims.
    - Do not rely on UI-only entitlement decisions.
    - Do not introduce TypeScript any/as any.
    - Do not weaken existing domain-local auth; platform auth is additive until adapters are ready.
  freedom:
    - Choose claim schema representation and fixture structure as long as it covers module, domain, org, jurisdiction, clearance, assignment, purpose, MFA, version, and expiry.
  work_loops:
    - name: Claim contract
      max_iterations: 3
      repeat_until: auth contract, fixtures, TypeScript helpers, Python fixture tests, and entitlement tests exist.
      steps:
        - write docs/spec/auth-entitlements-contract.md
        - write docs/spec/auth-claim-fixtures.json with required personas
        - implement packages/authz/src/claims.ts and packages/authz/src/entitlements.ts
        - add TypeScript and Python tests from the phased plan
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p2.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p2.sh
        - fix contract/test gaps
        - verify deny-by-default negative cases
  evidence_required:
    - docs/spec/auth-entitlements-contract.md
    - docs/spec/auth-claim-fixtures.json
    - packages/authz/src/claims.ts
    - packages/authz/src/entitlements.ts
    - packages/authz/src/__tests__/claims.test.ts
    - packages/authz/src/__tests__/entitlements.test.ts
    - tests/python/test_platform_claims.py
  escalate_when:
    - role, jurisdiction, assignment, purpose, or clearance semantics remain ambiguous
    - entitlement compatibility with Python and TypeScript cannot be proven from fixtures
    - a claim would allow access without server-side verification

