/goal
  objective: Create the platform API and app registry with entitlement, health, readiness, and decision evidence behavior.
  context:
    - docs/spec/phased-plan.yaml phase P6
    - docs/spec/auth-entitlements-contract.md
    - docs/spec/authorization-decision-evidence.md
    - docs/spec/repo-layout.md
    - packages/authz and packages/audit-ledger from P5
  constraints:
    - No active route may appear unless the backing domain has passed the server-side platform-claim gate.
    - Planned and blocked modules must not include active launch URLs.
    - Denied entitlement checks must emit decision evidence.
    - List endpoints must use bounded pagination if added.
  freedom:
    - Choose API framework wiring and route organization consistent with the monorepo layout.
    - Choose app registry config format if it supports planned, pilot, available, and blocked states.
  work_loops:
    - name: API implementation
      max_iterations: 3
      repeat_until: platform API files, migration, registry tests, entitlement tests, and decision evidence tests exist and pass.
      steps:
        - create apps/platform-api/package.json
        - implement apps/platform-api/src/app.ts
        - implement routes for /api/v1/platform/me, /apps, /entitlements/check, /health, and /ready
        - implement app registry states and launch URL blocking
        - add 001_platform_foundation migration
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p6.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p6.sh
        - repair route/auth/test gaps
        - verify denied checks produce decision evidence
  evidence_required:
    - apps/platform-api/package.json
    - apps/platform-api/src/app.ts
    - apps/platform-api/src/routes/platform.routes.ts
    - apps/platform-api/src/app-registry.ts
    - apps/platform-api/src/migrations/001_platform_foundation.sql
    - apps/platform-api/src/__tests__/app-registry.test.ts
    - apps/platform-api/src/__tests__/entitlements.test.ts
    - apps/platform-api/src/__tests__/decision-evidence.test.ts
  escalate_when:
    - an app can be launched without server-side platform claims
    - decision evidence cannot be emitted for deny paths
    - API behavior requires changing approved contracts
