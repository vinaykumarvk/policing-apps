/goal
  objective: Add an integrated local deployment profile for the platform pilot with only gated DOPAMS and IQW routes exposed.
  context:
    - docs/spec/phased-plan.yaml phase P11
    - apps/platform-api from P6
    - apps/platform-web from P7
    - DOPAMS and IQW adapters from P8
    - case and evidence projections from P9
  constraints:
    - Local compose is a pilot proof, not production security evidence.
    - Reverse proxy paths may expose only pilot-gated services.
    - Non-pilot services must be represented as planned or blocked, not routable.
    - Do not embed localhost URLs in production paths.
  freedom:
    - Choose docker compose service names, proxy paths, and smoke script details if the profile starts with one command and validates route blocking.
  work_loops:
    - name: Local profile
      max_iterations: 3
      repeat_until: compose profile, proxy config, smoke script, and pilot E2E test exist and pass.
      steps:
        - add deploy/docker-compose/policing-platform.yml
        - add deploy/nginx/policing-platform.local.conf
        - add scripts/smoke-platform-local.sh
        - add e2e/tests/platform-pilot-flow.spec.ts
        - document local limitations versus production
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p11.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p11.sh
        - repair service health or proxy issues
        - verify blocked modules are not routable
  evidence_required:
    - deploy/docker-compose/policing-platform.yml
    - deploy/nginx/policing-platform.local.conf
    - scripts/smoke-platform-local.sh
    - e2e/tests/platform-pilot-flow.spec.ts
    - successful local smoke output
  escalate_when:
    - smoke tests pass while blocked modules remain routable
    - local profile needs production credentials
    - rollback would break existing standalone app access

