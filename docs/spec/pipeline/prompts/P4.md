/goal
  objective: Create synthetic pilot user, case, evidence, and denial fixtures that exercise platform control-plane rules.
  context:
    - docs/spec/phased-plan.yaml phase P4
    - docs/spec/auth-entitlements-contract.md
    - docs/spec/data-classification-policy.md
    - docs/spec/authorization-decision-evidence.md
  constraints:
    - Fixtures must be synthetic only; no real PII, real officers, real complainants, real suspects, or active case data.
    - Fixtures must cover allow, deny, redaction, legal hold, stale state, revocation, wrong module, and wrong jurisdiction.
    - Do not make fixtures depend on production credentials or external services.
  freedom:
    - Choose fixture IDs and synthetic names if they are clearly artificial and stable.
    - Choose JSON schema details that support both TypeScript and Python tests.
  work_loops:
    - name: Fixture generation
      max_iterations: 3
      repeat_until: pilot fixture docs and all JSON fixture files exist and parse.
      steps:
        - write docs/spec/pilot-fixtures.md
        - create fixtures/platform/users.json
        - create fixtures/platform/cases.json
        - create fixtures/platform/evidence.json
        - create fixtures/platform/denials.json
        - add pilot persona and platform API fixture tests
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p4.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p4.sh
        - fix missing denial or redaction cases
        - document synthetic-data safeguards
  evidence_required:
    - docs/spec/pilot-fixtures.md
    - fixtures/platform/users.json
    - fixtures/platform/cases.json
    - fixtures/platform/evidence.json
    - fixtures/platform/denials.json
    - packages/authz/src/__tests__/pilot-personas.test.ts
    - apps/platform-api/src/__tests__/fixtures.test.ts
  escalate_when:
    - any fixture appears to contain real PII or real operational data
    - denial scenarios do not map to ABAC policy inputs
    - synthetic fixture scope is insufficient for DOPAMS plus IQW pilot flows

