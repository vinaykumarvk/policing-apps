/goal
  objective: Complete governance, cutover, traceability, and release-gate evidence for the pilot platform.
  context:
    - docs/spec/phased-plan.yaml phase P12
    - docs/spec/manifest.json
    - docs/spec/pipeline/phases.yaml
    - docs/spec/source-inventory.md
    - docs/spec/access-control-threat-model.md
    - docs/spec/authorization-decision-evidence.md
    - integrated local deployment evidence from P11
  constraints:
    - Release cannot pass if unauthorized app, case, evidence, or knowledge access is possible.
    - Release cannot pass if allow/deny decisions cannot be reconstructed from decision evidence.
    - Rollback must preserve original domain app access and audit trails.
    - Production pilot requires cutover and run-state governance approval.
  freedom:
    - Choose release gate document structure and traceability matrix format if every global gate and phase is covered.
  work_loops:
    - name: Governance package
      max_iterations: 3
      repeat_until: release gate, cutover runbook, run-state governance, and traceability matrix exist.
      steps:
        - write docs/spec/release-gate.md
        - write docs/spec/cutover-governance-runbook.md
        - write docs/spec/run-state-governance.md
        - write docs/spec/traceability-matrix.md
        - run release checks listed in the phased plan
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p12.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p12.sh
        - fix traceability and release evidence gaps
        - document accepted residual risks with owner/date
  evidence_required:
    - docs/spec/release-gate.md
    - docs/spec/cutover-governance-runbook.md
    - docs/spec/run-state-governance.md
    - docs/spec/traceability-matrix.md
    - npm run typecheck
    - npm --workspace apps/platform-api run test
    - npm --workspace apps/platform-web run test
    - scripts/smoke-platform-local.sh
  escalate_when:
    - emergency revocation, incident response, access review, or stale projection remediation is missing
    - legal hold or audit continuity cannot be guaranteed
    - any global release gate remains unresolved
