/goal
  objective: Create the platform web shell that renders entitled modules and route availability from the platform API.
  context:
    - docs/spec/phased-plan.yaml phase P7
    - docs/spec/repo-layout.md
    - apps/platform-api route and app registry contracts from P6
    - docs/spec/policing-platform-integration-execution-plan.md Release 1 scope
  constraints:
    - The web shell must not calculate or hardcode entitlements locally.
    - Planned and blocked modules must be visually distinct and must not expose launch URLs.
    - Only pilot-gated DOPAMS and IQW links may be active.
    - Do not add skeleton UI; all visible surfaces must have real data states.
  freedom:
    - Choose React route/component organization and test library consistent with the existing frontend stack.
  work_loops:
    - name: Shell implementation
      max_iterations: 3
      repeat_until: platform web app, launcher, route table, decision audit panel, and app registry tests exist.
      steps:
        - create apps/platform-web/package.json
        - implement apps/platform-web/src/App.tsx
        - implement apps/platform-web/src/routes.tsx
        - implement AppLauncher and DecisionAuditPanel
        - add UI and E2E tests listed in the phased plan
    - name: Review-repair
      max_iterations: 3
      repeat_until: docs/spec/pipeline/checks/p7.sh passes.
      steps:
        - run bash docs/spec/pipeline/checks/p7.sh
        - verify no local entitlement logic
        - verify unavailable modules have no launch links
  evidence_required:
    - apps/platform-web/package.json
    - apps/platform-web/src/App.tsx
    - apps/platform-web/src/routes.tsx
    - apps/platform-web/src/components/AppLauncher.tsx
    - apps/platform-web/src/components/DecisionAuditPanel.tsx
    - apps/platform-web/src/__tests__/app-launcher.test.tsx
    - e2e/tests/platform-app-registry.spec.ts
  escalate_when:
    - UI needs to infer authorization instead of asking the platform API
    - blocked or planned modules need active links for demo purposes
    - frontend implementation requires weakening the app registry contract

