# TEF AI-Driven Social Media Monitoring Tool - Functional Test Cases

| Field | Value |
|---|---|
| Document ID | TC-TEF-SMMT-1.0 |
| Version | 1.0 |
| Date | 2026-03-03 |
| Status | Draft |
| Project | TEF AI-Driven Social Media Monitoring Tool |
| BRD Reference | BRD-TEF-SMMT-2.0 |
| Prepared For | Telangana Eagle Force / Telangana Technology Services Ltd. |
| Tech Stack | React (Frontend), Node.js / Fastify (API), PostgreSQL (DB) |
| Test ID Convention | TC-SMM-FRXX-NNN |
| Priority Levels | P1 (Critical), P2 (High), P3 (Medium), P4 (Low) |

---

## Table of Contents

1. [Test Case Summary](#1-test-case-summary)
2. [FR-01: Platform Deployment and Environment Management](#2-fr-01-platform-deployment-and-environment-management)
3. [FR-02: Identity, RBAC, and Organization Hierarchy](#3-fr-02-identity-rbac-and-organization-hierarchy)
4. [FR-03: Source Onboarding and Lawful Ingestion](#4-fr-03-source-onboarding-and-lawful-ingestion)
5. [FR-04: Unified Monitoring Dashboard and Search Workspace](#5-fr-04-unified-monitoring-dashboard-and-search-workspace)
6. [FR-05: Keyword, Slang, Entity, OCR, and Transcript Analysis](#6-fr-05-keyword-slang-entity-ocr-and-transcript-analysis)
7. [FR-06: AI Categorization and Model Operations](#7-fr-06-ai-categorization-and-model-operations)
8. [FR-07: Risk Scoring and Prioritization](#8-fr-07-risk-scoring-and-prioritization)
9. [FR-08: Legal and Policy Mapping](#9-fr-08-legal-and-policy-mapping)
10. [FR-09: Translation and Language Intelligence](#10-fr-09-translation-and-language-intelligence)
11. [FR-10: Alerts, Escalation, Collaboration, and SLA Management](#11-fr-10-alerts-escalation-collaboration-and-sla-management)
12. [FR-11: Evidence Preservation and Chain of Custody](#12-fr-11-evidence-preservation-and-chain-of-custody)
13. [FR-12: Case, Task, and Workflow Management](#13-fr-12-case-task-and-workflow-management)
14. [FR-13: Reporting, Template Management, and MIS Analytics](#14-fr-13-reporting-template-management-and-mis-analytics)
15. [FR-14: Administration, Taxonomy, Legal Rules, and Configuration Governance](#15-fr-14-administration-taxonomy-legal-rules-and-configuration-governance)
16. [FR-15: Notifications, Sharing, and External Integrations](#16-fr-15-notifications-sharing-and-external-integrations)
17. [FR-16: Audit Logging, Observability, and Data Retention](#17-fr-16-audit-logging-observability-and-data-retention)
18. [FR-17: Security, Privacy, and Responsible AI Controls](#18-fr-17-security-privacy-and-responsible-ai-controls)
19. [FR-18: Implementation Delivery, Training, Support, Warranty, and Ownership](#19-fr-18-implementation-delivery-training-support-warranty-and-ownership)
20. [Traceability Matrix](#20-traceability-matrix)

---

## 1. Test Case Summary

| FR | Functional Area | Test Case Count | P1 | P2 | P3 | P4 |
|---|---|---|---|---|---|---|
| FR-01 | Platform Deployment & Environment Management | 5 | 2 | 2 | 1 | 0 |
| FR-02 | Identity, RBAC, and Organization Hierarchy | 6 | 2 | 2 | 1 | 1 |
| FR-03 | Source Onboarding and Lawful Ingestion | 14 | 6 | 5 | 2 | 1 |
| FR-04 | Unified Monitoring Dashboard and Search Workspace | 6 | 2 | 2 | 1 | 1 |
| FR-05 | Keyword, Slang, Entity, OCR, and Transcript Analysis | 5 | 1 | 2 | 1 | 1 |
| FR-06 | AI Categorization and Model Operations | 6 | 2 | 2 | 1 | 1 |
| FR-07 | Risk Scoring and Prioritization | 7 | 3 | 3 | 1 | 0 |
| FR-08 | Legal and Policy Mapping | 5 | 1 | 2 | 1 | 1 |
| FR-09 | Translation and Language Intelligence | 5 | 1 | 2 | 1 | 1 |
| FR-10 | Alerts, Escalation, Collaboration, and SLA Management | 7 | 4 | 2 | 1 | 0 |
| FR-11 | Evidence Preservation and Chain of Custody | 6 | 3 | 2 | 1 | 0 |
| FR-12 | Case, Task, and Workflow Management | 5 | 2 | 2 | 1 | 0 |
| FR-13 | Reporting, Template Management, and MIS Analytics | 5 | 1 | 2 | 1 | 1 |
| FR-14 | Administration, Taxonomy, Legal Rules, and Config Governance | 7 | 3 | 3 | 1 | 0 |
| FR-15 | Notifications, Sharing, and External Integrations | 5 | 2 | 2 | 1 | 0 |
| FR-16 | Audit Logging, Observability, and Data Retention | 5 | 2 | 2 | 1 | 0 |
| FR-17 | Security, Privacy, and Responsible AI Controls | 5 | 2 | 2 | 1 | 0 |
| FR-18 | Implementation Delivery, Training, Support, Warranty | 4 | 1 | 1 | 1 | 1 |
| **Total** | | **108** | **42** | **40** | **18** | **8** |

---

## 2. FR-01 - Platform Deployment and Environment Management

**BRD Reference:** FR-01, AC-FR-01-01 through AC-FR-01-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR01-001 | FR-01 / AC-FR-01-01 | Verify UAT and Production environment isolation | Both UAT and Production environments are deployed and accessible. | 1. Navigate to UAT URL (e.g., `https://uat-smmt.tsef.gov.in`). 2. Attempt to authenticate using Production credentials. 3. Verify UAT database connection string differs from Production. 4. Verify UAT object storage bucket/path is separate from Production. 5. Inspect environment configuration for shared secrets by querying `GET /api/v1/health` on both environments and comparing `environment` field. | UAT and Production are on separate URLs. Production credentials are rejected on UAT (and vice versa). Database connection strings, object storage paths, and runtime secrets are completely separate. The `/api/v1/health` endpoint returns `"environment": "UAT"` or `"environment": "PRODUCTION"` respectively. | P1 |
| TC-SMM-FR01-002 | FR-01 / AC-FR-01-02 | Verify HTTPS enforcement on all endpoints | Application is deployed and DNS is configured. | 1. Attempt to access the React frontend via `http://` (plain HTTP) URL. 2. Attempt to call `http://api-host/api/v1/health` via plain HTTP. 3. Verify TLS certificate validity using `openssl s_client` or browser inspector. 4. Confirm certificate expiry date is > 30 days from current date. | All HTTP requests are redirected to HTTPS (301/302). No API endpoint responds on plain HTTP. TLS certificate is valid and not expired. Certificate monitoring alert is configured for expiry within 30 days. | P1 |
| TC-SMM-FR01-003 | FR-01 / AC-FR-01-03 | Verify configuration promotion from UAT to Production requires approval | A taxonomy change (e.g., new category "Synthetic Opioids") exists in UAT with status `APPROVED_UAT`. Platform Administrator and Supervisor accounts are available. | 1. Log in as Platform Administrator on UAT. 2. Navigate to Admin > Configuration Promotion. 3. Select the "Synthetic Opioids" taxonomy change for promotion. 4. Attempt to promote without approver authorization. 5. Submit promotion request for Supervisor approval. 6. Log in as Supervisor and approve the promotion. 7. Verify audit log entry contains before/after version numbers. | Step 4 fails with error "Approver authorization required for Production promotion." Step 6 succeeds and the configuration appears in Production. Audit log entry records actor, timestamp, config object ID, previous version (e.g., v3), new version (v4), and approver identity. | P2 |
| TC-SMM-FR01-004 | FR-01 / AC-FR-01-04 | Verify daily backup execution and restore capability in UAT | Backup schedule is configured. UAT environment has at least 24 hours of operational data including alerts, evidence, and cases. | 1. Verify backup schedule configuration shows daily frequency. 2. Check backup log for the most recent successful run (must be within last 24 hours). 3. Initiate a restore drill in a UAT sandbox. 4. Verify restored database contains the expected alert count, evidence items, and configuration objects. 5. Verify object-store metadata is restored and evidence media files are accessible. | Backup ran successfully within the last 24 hours. Restore completes without errors. Alert, evidence, and case counts match pre-backup values. Evidence media files referenced by `master_object_key` are accessible post-restore. | P2 |
| TC-SMM-FR01-005 | FR-01 / AC-FR-01-05 / Edge Case | Verify rollback after failed deployment | A new application version (e.g., v2.1.1) is prepared for deployment. The current stable version is v2.1.0. | 1. Deploy v2.1.1 to UAT with a known defect that causes the `/api/v1/health` endpoint to return HTTP 500. 2. Observe the deployment health check failure. 3. Verify the system initiates automatic rollback to v2.1.0. 4. Confirm `/api/v1/health` returns HTTP 200 with `"version": "2.1.0"` after rollback. 5. Verify Platform Administrator received a deployment failure notification. | Deployment of v2.1.1 fails health check. System automatically reverts to v2.1.0. Application is accessible and healthy after rollback. Notification is sent to Platform Administrator and Vendor support with failure details. | P3 |

---

## 3. FR-02 - Identity, RBAC, and Organization Hierarchy

**BRD Reference:** FR-02, AC-FR-02-01 through AC-FR-02-05, ROL-01 through ROL-10

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR02-001 | FR-02 / AC-FR-02-01 | Verify role and unit-scoped permission enforcement | Users exist: Analyst-A (ROL-03, Hyderabad District), Analyst-B (ROL-03, Warangal District). Alerts exist in both districts. | 1. Log in as Analyst-A. 2. Navigate to Dashboard > Alerts. 3. Verify only Hyderabad District alerts are visible. 4. Attempt to access a Warangal alert by direct URL (`/alerts/{warangal_alert_id}`). 5. Log in as Analyst-B and verify only Warangal alerts are visible. | Analyst-A sees only Hyderabad alerts. Direct URL access to Warangal alert returns HTTP 403 with message "Access denied: insufficient unit scope." Analyst-B sees only Warangal alerts. Effective permissions are computed from role (ROL-03) plus primary unit scope. | P1 |
| TC-SMM-FR02-002 | FR-02 / AC-FR-02-02 | Verify Leadership Read Only role restrictions | User "LeadershipUser" has role ROL-02 (Leadership Read Only). Dashboard has alerts, cases, and evidence. | 1. Log in as LeadershipUser. 2. Navigate to Dashboard and verify data is visible. 3. Attempt to create a new alert by calling `POST /api/v1/alerts`. 4. Attempt to edit an existing case. 5. Attempt to export an evidence package. 6. Attempt to approve a legal mapping. | Dashboard data renders correctly in read-only mode. All create, edit, export, and approval actions return HTTP 403. The React UI does not render action buttons (Create, Edit, Export, Approve) for this role. | P1 |
| TC-SMM-FR02-003 | FR-02 / AC-FR-02-04 | Verify session timeout for privileged and non-privileged roles | Two users: Admin (ROL-10, PL4 privileged) and Analyst (ROL-03, PL2 non-privileged). Both are logged in. | 1. Log in as Admin. Leave session idle for 15 minutes. 2. Attempt any API call after 15 minutes of inactivity. 3. Log in as Analyst. Leave session idle for 25 minutes. 4. Verify Analyst session is still active at 25 minutes. 5. Leave Analyst session idle until 30 minutes total. 6. Attempt any API call after 30 minutes of inactivity. | Admin session expires at 15 minutes; API returns HTTP 401 and React app redirects to login. Analyst session is active at 25 minutes. Analyst session expires at 30 minutes; API returns HTTP 401 and React app redirects to login. | P2 |
| TC-SMM-FR02-004 | FR-02 / AC-FR-02-03 | Verify maker-checker approval requirement for case closure | Investigator (ROL-05) has an open case. Supervisor (ROL-06) is available. Case closure requires supervisory approval in workflow config. | 1. Log in as Investigator. 2. Navigate to Case > Submit for Review. 3. Attempt to close the case directly without supervisor approval. 4. Submit case for supervisor review. 5. Log in as Supervisor. 6. Approve the closure with closure reason "Investigation complete - no further action." | Step 3 fails: "Case closure requires supervisory approval." Step 4 succeeds: case moves to AWAITING_REVIEW. Step 6 succeeds: case moves to CLOSED. Audit log records both the submission and approval with actor identities, timestamps, and closure reason. | P2 |
| TC-SMM-FR02-005 | FR-02 / Edge Case | Verify break-glass administrator access during SSO outage | SSO/OIDC provider is simulated as unavailable (connection timeout). A break-glass administrator account exists with `auth_provider = LOCAL_BREAK_GLASS`. A normal OIDC user account exists. | 1. Attempt to log in as the normal OIDC user. 2. Verify the login page displays "Identity provider unavailable. Please contact administrator." 3. Log in using the break-glass administrator credentials via the local login form. 4. Verify break-glass admin can access the Admin panel. 5. Verify all actions by the break-glass admin are audit-logged with `auth_provider = LOCAL_BREAK_GLASS`. | Normal users receive a controlled denial message and cannot authenticate. Break-glass admin authenticates successfully via local credentials. All break-glass actions are logged with the local auth provider flag for audit trail. | P3 |
| TC-SMM-FR02-006 | FR-02 / BR-FR-02-02 | Verify disabled user cannot authenticate and tasks are reassigned | User "AnalystX" (ROL-03) has 3 open alert tasks. Platform Admin disables the account. | 1. Log in as Platform Admin. 2. Navigate to Admin > Users > AnalystX. 3. Set account_status to DISABLED. 4. Verify system prompts for reassignment of 3 open tasks. 5. Reassign tasks to "AnalystY" in the same unit. 6. Attempt to log in as AnalystX. | Account is disabled. System requires reassignment of all open tasks before disabling is finalized. Tasks are reassigned to AnalystY with audit trail. AnalystX login attempt returns HTTP 401 with "Account disabled. Contact administrator." | P4 |

---

## 4. FR-03 - Source Onboarding and Lawful Ingestion

**BRD Reference:** FR-03, AC-FR-03-01 through AC-FR-03-05, ENT-04 (SourceConnector), ENT-05 (ContentItem)

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR03-001 | FR-03 / AC-FR-03-01 | Verify source connector configuration for all supported platforms | Platform Admin is logged in. No connectors configured yet. | 1. Navigate to Admin > Source Connectors. 2. Create a connector for X/Twitter with: acquisition_mode = API, entitlement_reference = "TEF-ENT-2026-001", poll_interval_sec = 300, rate_limit_per_min = 60, retry_policy_json = `{"maxRetries": 3, "backoffMultiplier": 2}`. 3. Repeat for Instagram, Facebook, YouTube, and Reddit with platform-specific settings. 4. Verify each connector shows status = ACTIVE. 5. Call `GET /api/v1/connectors` and verify all 5 connectors are returned. | Five connectors are created, each with platform_code (X, INSTAGRAM, FACEBOOK, YOUTUBE, REDDIT), acquisition mode, entitlement reference, polling interval, retry policy, and ACTIVE status. The API returns all connectors with correct attributes. | P1 |
| TC-SMM-FR03-002 | FR-03 / AC-FR-03-02 / AC-FR-03-04 | Verify content normalization and duplicate prevention | X/Twitter connector is active. A sample tweet JSON payload exists with source_content_id = "tweet_12345". | 1. Trigger ingestion of the tweet payload. 2. Verify a ContentItem record is created with platform_code = X, source_content_id = "tweet_12345", normalized_hash (SHA-256), and processing_status = INGESTED. 3. Verify the raw source payload is retained and linked to the content_id. 4. Trigger ingestion of the same tweet payload again (identical source_content_id and content hash). 5. Call `GET /api/v1/content?source_content_id=tweet_12345` and verify only one record exists. | ContentItem is created with all normalized fields. Raw payload is preserved with linkable identifier. Duplicate ingestion is blocked: the system detects the matching source_content_id + normalized_hash and does not create a second record. Only one ContentItem exists for the tweet. | P1 |
| TC-SMM-FR03-003 | FR-03 / AC-FR-03-03 | Verify exponential backoff retry and dead-letter queue on connector failure | X/Twitter connector is active with retry_policy_json = `{"maxRetries": 3, "backoffMultiplier": 2, "initialDelayMs": 1000}`. X/Twitter API is simulated as returning HTTP 503. | 1. Trigger a polling cycle for the X/Twitter connector. 2. Observe retry attempts: first at ~1s, second at ~2s, third at ~4s. 3. Verify that after 3 failed retries, the failed payload is moved to the dead-letter queue (DLQ). 4. Navigate to Admin > Dead Letter Queue and verify the payload is visible with error details. 5. Verify connector_status remains ACTIVE (not permanently failed). | Connector retries using exponential backoff (1s, 2s, 4s). After retry exhaustion, the payload enters DLQ with HTTP 503 error, timestamp, and retry count. Connector remains ACTIVE for future polling cycles. Admin can inspect DLQ entries. | P2 |
| TC-SMM-FR03-004 | FR-03 / AC-FR-03-05 / BR-FR-03-02 | Verify rate limit and legal basis metadata are viewable and credentials are secure | Instagram connector is configured with rate_limit_per_min = 30 and entitlement_reference = "TEF-ENT-2026-002". | 1. Log in as Platform Admin. 2. Navigate to Admin > Source Connectors > Instagram. 3. Verify rate_limit_per_min = 30 and entitlement_reference = "TEF-ENT-2026-002" are displayed. 4. Verify connector credentials are NOT displayed in the UI (shown as masked e.g., `****`). 5. Call `GET /api/v1/connectors/{connector_id}` and verify credentials are not returned in the API response body. 6. Verify credentials are stored in the secrets manager (not in source code or plain-text config). | Rate limit and legal basis metadata are visible to administrators. Connector credentials are masked in the UI and excluded from API responses. Secrets manager integration is confirmed (no plain-text credentials in config or database). | P2 |
| TC-SMM-FR03-005 | FR-03 / Failure Handling | Verify normalization failure preserves raw payload | A malformed Reddit payload is prepared with invalid UTF-8 encoding in the content_text field. Reddit connector is active. | 1. Submit the malformed payload through the Reddit connector ingestion pipeline. 2. Verify the system attempts normalization and it fails. 3. Check that the raw payload is preserved with processing_status = FAILED_NORMALIZATION. 4. Verify the item appears in the Admin > Reprocessing Queue. 5. Fix the encoding issue and trigger reprocessing. 6. Verify the item moves to processing_status = INGESTED after successful normalization. | Raw payload is preserved even when normalization fails. The item is flagged as FAILED_NORMALIZATION and routed for reprocessing. After reprocessing, the item normalizes successfully and proceeds through the pipeline. No data loss occurs. | P3 |
| TC-SMM-FR03-006 | FR-03 / AC-FR-03-01 / AC-FR-03-02 | Verify Reddit public API connector fetches and normalizes content without authentication | Reddit connector is ACTIVE in source_connector table. Default Monitoring watchlist is active with keyword "cyber crime india" and platform "reddit". | 1. Start the connector scheduler. 2. Wait for the first poll cycle to complete (check logs for POLL_CYCLE_COMPLETE). 3. Query `SELECT COUNT(*) FROM content_item WHERE platform = 'reddit' AND ingested_at > NOW() - INTERVAL '5 minutes'`. 4. Verify count > 0. 5. Inspect a sample content_item: verify platform_post_id starts with "reddit_", author_handle is populated, content_text contains title+body, content_url points to reddit.com, metadata_jsonb contains subreddit, score, num_comments. 6. Verify connector_id FK links to the reddit source_connector record. | Reddit connector fetches posts via public JSON API (no auth required). Content items are created with normalized fields. Metadata includes subreddit, score, and comment count. The content_url is a valid Reddit permalink. No authentication error occurs. | P1 |
| TC-SMM-FR03-007 | FR-03 / AC-FR-03-01 / AC-FR-03-02 | Verify YouTube Data API v3 connector fetches videos with API key authentication | YouTube connector is ACTIVE. YOUTUBE_API_KEY environment variable is set with a valid Google API key. YouTube Data API v3 is enabled in the Google Cloud project. Watchlist includes keyword "online fraud" with platform "youtube". | 1. Start the connector scheduler. 2. Wait for poll cycle to complete. 3. Query `SELECT COUNT(*) FROM content_item WHERE platform = 'youtube' AND ingested_at > NOW() - INTERVAL '5 minutes'`. 4. Verify count > 0. 5. Inspect a sample: verify platform_post_id starts with "youtube_", content_url matches `https://www.youtube.com/watch?v=*`, author_name contains channel title, metadata_jsonb contains channelId, thumbnails. 6. Verify content_text contains video title and description concatenated. | YouTube connector authenticates with API key and fetches recent videos. Content items contain video title + description, channel info, and YouTube watch URL. Metadata includes channel ID and thumbnail URLs. | P1 |
| TC-SMM-FR03-008 | FR-03 / AC-FR-03-01 / AC-FR-03-02 | Verify Twitter/X API v2 connector fetches tweets with Bearer Token | Twitter connector is ACTIVE. TWITTER_BEARER_TOKEN is set with a valid token (Basic tier or above). Watchlist includes keyword "social media threat" with platform "twitter". | 1. Start the connector scheduler. 2. Wait for poll cycle to complete. 3. Query `SELECT COUNT(*) FROM content_item WHERE platform = 'twitter' AND ingested_at > NOW() - INTERVAL '5 minutes'`. 4. Verify count > 0. 5. Inspect a sample: verify platform_post_id starts with "twitter_", author_handle starts with "@", content_url matches `https://twitter.com/*/status/*`, metadata_jsonb contains publicMetrics (retweet_count, like_count). 6. Verify retweets are excluded (query uses `-is:retweet`). | Twitter connector authenticates with Bearer Token and fetches recent original tweets. Author expansion provides username and display name. Public metrics are stored in metadata. Retweets are filtered out. | P1 |
| TC-SMM-FR03-009 | FR-03 / AC-FR-03-01 / AC-FR-03-02 | Verify Instagram Graph API connector fetches hashtag media | Instagram connector is ACTIVE. META_ACCESS_TOKEN and INSTAGRAM_BUSINESS_ACCOUNT_ID are set. Watchlist includes keyword "cyber crime" with platform "instagram". | 1. Start the connector scheduler. 2. Wait for poll cycle to complete. 3. Query `SELECT COUNT(*) FROM content_item WHERE platform = 'instagram' AND ingested_at > NOW() - INTERVAL '5 minutes'`. 4. Verify count > 0 (or 0 if hashtag has no recent media — check logs for non-error completion). 5. If items exist, verify platform_post_id starts with "instagram_", content_text contains caption, content_url is a valid Instagram permalink, metadata_jsonb contains mediaType. 6. Verify the connector performed two API calls: hashtag search then recent_media. | Instagram connector resolves hashtag ID via ig_hashtag_search, then fetches recent media. Content items contain caption text, permalink, and media type metadata. Two-step API flow executes correctly. | P2 |
| TC-SMM-FR03-010 | FR-03 / AC-FR-03-01 / AC-FR-03-02 | Verify Facebook Graph API connector fetches page posts | Facebook connector is ACTIVE. META_ACCESS_TOKEN is set with pages_read_engagement permission. Watchlist includes keyword "online fraud" with platform "facebook". | 1. Start the connector scheduler. 2. Wait for poll cycle to complete. 3. Query `SELECT COUNT(*) FROM content_item WHERE platform = 'facebook' AND ingested_at > NOW() - INTERVAL '5 minutes'`. 4. Verify count >= 0 (check logs for non-error completion). 5. If items exist, verify platform_post_id starts with "facebook_", content_text contains post message, metadata_jsonb contains pageId, pageName, shares count. 6. Verify the connector searched pages then fetched posts from top 5 matches. | Facebook connector searches public pages by keyword, then fetches recent posts from top matching pages. Content items contain message text, permalink, and share count metadata. | P2 |
| TC-SMM-FR03-011 | FR-03 / AC-FR-03-04 | Verify dedup index prevents duplicate platform_post_id insertion across poll cycles | Reddit connector is ACTIVE. A previous poll cycle has already ingested 25 Reddit posts. | 1. Trigger a second poll cycle with the same keywords. 2. Verify the ingestion pipeline performs dedupe check: `SELECT content_id FROM content_item WHERE platform = $1 AND platform_post_id = $2`. 3. Verify logs show duplicate count > 0. 4. Query `SELECT COUNT(*) FROM content_item WHERE platform = 'reddit'` and verify count has NOT doubled. 5. Verify the unique partial index `idx_content_platform_post_unique` on `(platform, platform_post_id) WHERE platform_post_id IS NOT NULL` exists. 6. Attempt a direct `INSERT` with a duplicate platform + platform_post_id and verify it fails with unique violation. | Duplicate posts from the same platform are detected and skipped. The dedup index enforces uniqueness at the database level. The POLL_CYCLE_COMPLETE log shows duplicates > 0 on subsequent cycles. No duplicate content_item rows exist. | P1 |
| TC-SMM-FR03-012 | FR-03 / Edge Case | Verify connector scheduler advisory lock prevents concurrent poll cycles | Two API server instances are running (or simulated). Both attempt to start a poll cycle simultaneously. | 1. Instance A acquires advisory lock 900_004 via `pg_try_advisory_lock(900004)`. 2. Instance B attempts to acquire the same lock. 3. Verify Instance B's `pg_try_advisory_lock` returns false. 4. Verify Instance B skips the poll cycle (no API calls made). 5. Instance A completes and releases the lock via `pg_advisory_unlock(900004)`. 6. Verify the next cycle allows Instance B to acquire the lock. | Advisory lock prevents duplicate poll cycles across instances. Only one instance runs the poll at a time. The losing instance silently skips without error. Lock is released after cycle completion. | P2 |
| TC-SMM-FR03-013 | FR-03 / Failure Handling | Verify graceful degradation when connector API key/token is missing | YOUTUBE_API_KEY, TWITTER_BEARER_TOKEN, META_ACCESS_TOKEN are all unset (empty). Only Reddit connector has no auth requirement. | 1. Start the API server with no external API tokens configured. 2. Verify startup logs show: "YouTube connector disabled — YOUTUBE_API_KEY not set", "Twitter connector disabled — TWITTER_BEARER_TOKEN not set", "Instagram connector disabled — ...", "Facebook connector disabled — ...". 3. Verify "Connector scheduler started" log shows platforms = ["reddit"] only. 4. Wait for poll cycle. 5. Verify Reddit content is ingested successfully. 6. Verify no errors or crashes from missing tokens. | Missing API tokens result in informational warnings, not crashes. Only Reddit (no auth) is active. The scheduler runs normally with the available connectors. Other connectors are gracefully disabled. | P2 |
| TC-SMM-FR03-014 | FR-03 / AC-FR-03-05 | Verify inter-request rate limiting between platform API calls | All connectors are ACTIVE. Watchlist has 3 keywords across 3 platforms (9 API calls expected). | 1. Start a poll cycle and capture timestamps of all outbound API calls from connector logs. 2. Verify there is at least 1500ms (1.5s) delay between consecutive API calls. 3. Verify total poll cycle duration is >= 9 * 1.5s = 13.5s. 4. Verify no API rate-limit errors (HTTP 429) are returned from any platform. | Inter-request delay of 1.5 seconds is enforced between all platform API calls. This prevents rate-limit violations on external APIs. Total cycle time reflects the cumulative delay. No HTTP 429 errors occur. | P3 |

---

## 5. FR-04 - Unified Monitoring Dashboard and Search Workspace

**BRD Reference:** FR-04, AC-FR-04-01 through AC-FR-04-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR04-001 | FR-04 / AC-FR-04-01 | Verify dashboard cross-platform filters | Intelligence Analyst is logged in. Alerts exist across all 5 platforms (X, Instagram, Facebook, YouTube, Reddit) with various categories, risk bands, and districts. | 1. Navigate to Dashboard. 2. Apply filter: platform = X. Verify only X/Twitter alerts appear. 3. Clear filter. Apply filter: risk_band = CRITICAL. Verify only Critical alerts appear. 4. Apply combined filters: platform = Instagram, category = "Drug Sale", district = "Hyderabad", time_range = "Last 7 days". 5. Verify result count matches database query. 6. Apply filter: language = Telugu. Verify only Telugu-language content appears. | Each filter narrows results correctly. Combined filters work with AND logic. Result counts match backend data. Filters include platform, district, language, category, risk band, alert status, and time range as per AC-FR-04-01. All filter controls render on the React dashboard with responsive layout. | P1 |
| TC-SMM-FR04-002 | FR-04 / AC-FR-04-02 | Verify Control Room dedicated high-priority view | Control Room Operator (ROL-04) is logged in. Alerts exist with priorities: 5 Critical, 8 High, 20 Medium, 15 Low. | 1. Log in as Control Room Operator. 2. Verify the default dashboard view shows only Critical and High queue items (13 total). 3. Verify each alert card displays SLA countdown timer (e.g., "Ack due in 8 min"). 4. Verify escalation action buttons are visible on Critical alerts. 5. Verify Medium and Low alerts are NOT shown by default. 6. Optionally expand filters to include Medium alerts and verify 20 additional items appear. | Default Control Room view shows 13 items (5 Critical + 8 High). SLA countdown is visible on each item. Escalation buttons (Escalate, Acknowledge, Share) are rendered. Medium and Low alerts are hidden by default but accessible via filter expansion. | P1 |
| TC-SMM-FR04-003 | FR-04 / AC-FR-04-04 | Verify saved search creation, sharing, and re-execution | Intelligence Analyst-A and Analyst-B are in the same unit. Analyst-A is logged in. | 1. Analyst-A creates a search: platform = YouTube, category = "Drug Promotion", time_range = "Last 30 days", keywords = "ganja delivery". 2. Save search with name "YouTube Drug Promo Q1". 3. Share the saved search with "Intelligence Wing Hyderabad" unit. 4. Log in as Analyst-B (same unit). 5. Navigate to Saved Searches and verify "YouTube Drug Promo Q1" appears. 6. Re-run the saved search. 7. Verify results match the original query without manual reconstruction. | Saved search is stored with name and filters. It appears in the shared unit's saved search list. Re-execution returns current results matching the filter criteria. Query does not require manual reconstruction. | P2 |
| TC-SMM-FR04-004 | FR-04 / AC-FR-04-03 | Verify aggregated actor timeline with cross-platform handles | An actor "DrugLordX" has content from X (handle: @druglordx), Instagram (handle: druglord_x), and Facebook (name: "Drug Lord X"). Cross-platform matching confidence is above configured threshold (e.g., 85%). | 1. Navigate to Dashboard > Actor Timeline view. 2. Search for "druglordx". 3. Verify the aggregated actor card groups all three platform handles. 4. Verify the timeline shows source records from X, Instagram, and Facebook in chronological order. 5. Click on individual source records and verify they link to the original content items. | Aggregated actor card shows all three handles with platform icons. Timeline displays content chronologically across platforms. Each entry links to the original ContentItem detail. Confidence score for the match is displayed (e.g., 87%). | P2 |
| TC-SMM-FR04-005 | FR-04 / BR-FR-04-01 / Edge Case | Verify low-confidence actor matching shows merge suggestion instead of auto-merge | Two handles exist: X handle "@sharma_raj" and Instagram handle "rajsharma_22". Cross-platform matching confidence is 55% (below the configured threshold of 75%). | 1. Navigate to Actor Timeline view. 2. Search for "sharma_raj". 3. Verify the two handles appear as SEPARATE actor cards (not merged). 4. Verify a "Possible match" merge suggestion banner appears between the cards. 5. Click "Merge" suggestion. 6. Verify a confirmation dialog appears with confidence score (55%) and requires analyst approval. | Handles below the confidence threshold are NOT auto-merged. Separate actor cards are displayed. A merge suggestion is shown with the confidence percentage. Merging requires explicit analyst confirmation. | P3 |
| TC-SMM-FR04-006 | FR-04 / Failure Handling | Verify partial-data banner when a source is unavailable | X/Twitter connector is in ERROR status. Instagram, Facebook, YouTube, Reddit connectors are ACTIVE. Dashboard has mixed-platform data. | 1. Navigate to Dashboard. 2. Verify a partial-data banner is displayed at the top: "Data from X/Twitter may be incomplete. Last successful sync: 2026-03-03 08:15 IST." 3. Verify dashboard totals show data from the 4 available platforms. 4. Verify the banner includes the affected platform name and last_success_at timestamp. | Partial-data banner is visible and clearly identifies X/Twitter as the affected platform. Last successful sync timestamp is displayed in Asia/Kolkata timezone. Dashboard continues to show data from other 4 platforms. No error page or blank dashboard. | P4 |

---

## 6. FR-05 - Keyword, Slang, Entity, OCR, and Transcript Analysis

**BRD Reference:** FR-05, AC-FR-05-01 through AC-FR-05-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR05-001 | FR-05 / AC-FR-05-01 / AC-FR-05-04 | Verify slang dictionary match with context and confidence | Slang dictionary contains term: "maal" (language: Hindi, transliteration: "maal", severity_hint: 70, effective_from: 2026-01-01). A Hindi tweet contains "aaj maal aa raha hai bhai". | 1. Ingest the Hindi tweet through the X/Twitter connector. 2. Verify the keyword/slang matching pipeline processes the content. 3. Navigate to the alert detail for this content. 4. Verify "maal" is highlighted in the content text. 5. Verify match result shows: matched_term = "maal", dictionary_version = "v12", context_snippet = "...aaj **maal** aa raha hai...", confidence = 0.92. | Slang term "maal" is matched from the dictionary. Match result displays the term, dictionary version, context snippet with highlighting, and AI-derived confidence score. The severity hint (70) feeds into risk scoring. | P1 |
| TC-SMM-FR05-002 | FR-05 / AC-FR-05-02 | Verify entity extraction from content text | An Instagram post contains: "Contact @dealer_hyd on WhatsApp +91-9876543210 for stuff. Payment via UPI: drugs@ybl. Meet near Charminar, Hyderabad. #ganjadeals". | 1. Ingest the Instagram post. 2. Verify entity extraction runs on the normalized content. 3. Navigate to the content detail view. 4. Verify extracted entities: handle = "@dealer_hyd", phone = "+91-9876543210", payment_reference = "drugs@ybl", location = "Charminar, Hyderabad", hashtag = "#ganjadeals", substance_reference = "ganja". | Entity extraction identifies all entity types: handles, phone numbers, payment references, location mentions, hashtags, and substance references. Each entity is displayed in the content detail with its type label. Entities are searchable in the dashboard search workspace. | P2 |
| TC-SMM-FR05-003 | FR-05 / AC-FR-05-03 | Verify OCR text extraction from image post | OCR feature is enabled. An Instagram post consists of an image containing text "Buy premium quality charas - DM for rates" with no caption text. | 1. Ingest the image-only Instagram post. 2. Verify the OCR pipeline processes the image. 3. Verify extracted OCR text = "Buy premium quality charas - DM for rates" is stored in `ContentMedia.ocr_text`. 4. Search for "charas" in the dashboard search workspace. 5. Verify the image-only post appears in search results. 6. Verify the OCR text is linked to the source media with page offset. | OCR extracts text from the image. Extracted text is searchable and linked to the source media record. The slang term "charas" triggers keyword matching. Image-only posts are not missed by the analysis pipeline. | P2 |
| TC-SMM-FR05-004 | FR-05 / AC-FR-05-05 | Verify analyst can submit candidate slang term for approval | Intelligence Analyst is logged in. Analyst discovers a new coded term "ice cream" being used for methamphetamine. | 1. Navigate to Admin > Slang Dictionary > Suggest New Term. 2. Enter: term = "ice cream", language = English, transliteration = N/A, severity_hint = 80, notes = "Coded term for methamphetamine observed in Hyderabad network". 3. Submit for approval. 4. Verify the term is saved with config_status = DRAFT (not published to Production). 5. Log in as Platform Administrator. 6. Verify the candidate term appears in the approval queue. | Analyst can submit candidate terms. The term is saved as DRAFT and does NOT appear in the active Production slang dictionary. It enters the approval queue for administrator/supervisor review. No direct publishing to Production is possible from the analyst role. | P3 |
| TC-SMM-FR05-005 | FR-05 / Failure Handling / Edge Case | Verify processing continues when OCR provider is unavailable | OCR provider is simulated as unavailable (HTTP 503). A Facebook post contains an image with embedded text and a caption "Check this out". | 1. Ingest the Facebook post with image. 2. Verify OCR processing fails for the image. 3. Verify the content item continues through text-only processing on the caption "Check this out". 4. Verify the ContentMedia record has processing_status = FAILED_MEDIA_PROCESSING. 5. Verify the content item is flagged MEDIA_TEXT_NOT_AVAILABLE. 6. Verify keyword matching and AI categorization still execute on the caption text. | OCR failure does not block the pipeline. Content proceeds with text-only analysis. The media record is flagged for later OCR retry. Caption text is still matched against keywords and categorized by AI. No data loss or pipeline halt. | P4 |

---

## 7. FR-06 - AI Categorization and Model Operations

**BRD Reference:** FR-06, AC-FR-06-01 through AC-FR-06-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR06-001 | FR-06 / AC-FR-06-01 | Verify classifier output conforms to fixed JSON schema | Content item exists with text "Selling brown sugar 500g minimum order DM me" from X/Twitter. AI model is available. | 1. Trigger AI classification on the content item. 2. Retrieve the classification result via `GET /api/v1/content/{content_id}/classification`. 3. Validate the JSON response against the fixed schema. 4. Verify fields: `categories` (array with at least one `category_code`), `confidence_score` (decimal 0-1), `rationale_snippet` (string), `model_version` (string), `prompt_version` (string), `rule_version` (string). | Classification response conforms exactly to the schema. At least one category is returned (e.g., `DRUG_SALE`). Confidence score is between 0 and 1 (e.g., 0.94). Rationale snippet explains the classification (e.g., "Text contains explicit drug sale solicitation for 'brown sugar'"). Model, prompt, and rule version identifiers are populated. | P1 |
| TC-SMM-FR06-002 | FR-06 / AC-FR-06-03 | Verify analyst feedback (Accept, Correct, Reject) is captured with version tracing | Alert exists with AI classification: category = "DRUG_SALE", confidence = 0.89, model_version = "llm-v2.3", prompt_version = "p-v1.7". Intelligence Analyst is logged in. | 1. Navigate to Alert detail. 2. Review AI classification. Click "Accept" to confirm the category. 3. Verify feedback record stores: decision = ACCEPTED, model_version = "llm-v2.3", prompt_version = "p-v1.7". 4. Navigate to a second alert with incorrect classification. Click "Correct" and change category from "DRUG_USE" to "DRUG_MANUFACTURING". 5. Verify feedback record stores: decision = CORRECTED, original_category = "DRUG_USE", corrected_category = "DRUG_MANUFACTURING", model_version, prompt_version. 6. Navigate to a third alert. Click "Reject" with reason "Not drug related - farming terminology." | All three feedback types (Accept, Correct, Reject) are captured. Each feedback record links to the specific model_version and prompt_version that produced the original classification. Corrected records show both original and analyst-corrected categories. Rejected records include mandatory reason text. | P1 |
| TC-SMM-FR06-003 | FR-06 / AC-FR-06-05 | Verify low-confidence classification is marked NEEDS_REVIEW and not auto-escalated | Confidence threshold is configured at 0.60. Content item is processed and AI returns confidence = 0.45 with category = "DRUG_PROMOTION". | 1. Verify the alert created from this content has status field showing NEEDS_REVIEW flag. 2. Verify the alert is NOT automatically escalated to Control Room or Supervisor. 3. Verify the alert appears in the analyst review queue with a visual indicator (e.g., amber "Low Confidence" badge). 4. Navigate to alert detail and verify the confidence score (0.45) is prominently displayed. | Alert is marked NEEDS_REVIEW due to confidence (0.45) being below threshold (0.60). No auto-escalation occurs. The alert appears in the analyst manual review queue. Low-confidence badge is visible in the React UI. | P2 |
| TC-SMM-FR06-004 | FR-06 / AC-FR-06-04 | Verify rule-layer update without changing LLM weights or prompts | A rule-layer threshold change is needed: category "DRUG_TRAFFICKING" severity hint changes from 85 to 95. Platform Admin is logged in. | 1. Navigate to Admin > Classification Rules. 2. Update the severity_default for "DRUG_TRAFFICKING" from 85 to 95. 3. Submit for approval. 4. Approve and promote to Production. 5. Ingest a new content item classified as "DRUG_TRAFFICKING". 6. Verify the risk score calculation uses severity = 95 (not 85). 7. Verify model_version and prompt_version remain unchanged from the previous classification. | Rule-layer update deploys without changing LLM weights or prompts. New classifications use the updated severity (95). Model and prompt versions remain the same. The change is traceable to the rule_version update in audit logs. | P2 |
| TC-SMM-FR06-005 | FR-06 / Edge Case | Verify content fitting multiple categories | Content text: "Manufacturing synthetic drugs in Warangal lab and selling via Instagram. Also promoting use among college students." | 1. Trigger AI classification on this content. 2. Verify the classifier returns MULTIPLE category codes (e.g., DRUG_MANUFACTURING, DRUG_SALE, DRUG_PROMOTION). 3. Verify each category has its own confidence score. 4. Verify the alert uses the highest-severity category as `primary_category_id`. 5. Verify all categories are visible in the alert detail view. | Multiple categories are returned in the classification result. Each has an independent confidence score. The primary category is set to the highest-severity match. All categories are displayed in the React alert detail component for analyst review. | P3 |
| TC-SMM-FR06-006 | FR-06 / Failure Handling | Verify fallback to rules-based triage when AI service is unavailable | AI/LLM service is simulated as unavailable (connection timeout). Rules engine has baseline rules for keyword-based triage. Content item with text "selling ganja 100g" is ingested. | 1. Verify the classification pipeline detects AI service unavailability. 2. Verify the content is processed using rules-only triage. 3. Verify the alert is created with `processing_mode = RULES_ONLY`. 4. Verify the keyword "ganja" triggers a rule-based category match (e.g., DRUG_SALE). 5. Verify the alert detail shows "AI unavailable - rules-only classification" indicator. | System falls back to rules-based triage seamlessly. Alert is created with processing_mode = RULES_ONLY. Keyword-based rules still categorize the content. The UI clearly indicates the classification method used. No pipeline halt or data loss. | P4 |

---

## 8. FR-07 - Risk Scoring and Prioritization

**BRD Reference:** FR-07, AC-FR-07-01 through AC-FR-07-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR07-001 | FR-07 / AC-FR-07-01 / AC-FR-07-02 | Verify risk score computation and priority band mapping | Content item classified as DRUG_TRAFFICKING (severity = 90). Actor has 3 prior alerts. Post has 5,000 shares (high virality). Policy weight for trafficking = 1.2x. | 1. Trigger risk scoring on the content item. 2. Retrieve the alert via `GET /api/v1/alerts/{alert_id}`. 3. Verify `risk_score` is on a 0-100 scale. 4. Verify `priority_band` = CRITICAL (score expected to be 85-100 given high severity, repeat actor, and virality). 5. Navigate to alert detail and inspect the scorecard. 6. Verify factor contributions are displayed: content_severity = 90, actor_frequency = +5 (3 prior alerts), virality = +8 (5000 shares), historical_behavior = +3, policy_weight = 1.2x. | Risk score is computed and falls within 85-100 (CRITICAL band). Scorecard shows transparent factor contributions for each component. Priority band mapping follows configured thresholds: Critical 85-100, High 70-84, Medium 50-69, Low 0-49. All factors are analyst-visible in the React alert detail. | P1 |
| TC-SMM-FR07-002 | FR-07 / AC-FR-07-03 | Verify threshold-based routing to Control Room queue | Risk score threshold configuration: score >= 85 routes to Control Room queue. A new alert is created with risk_score = 92, priority_band = CRITICAL. | 1. Verify the alert automatically appears in the Control Room Operator's queue. 2. Log in as Control Room Operator (ROL-04). 3. Verify the alert is visible in the Control Room dashboard. 4. Verify the alert is also visible to the Supervisor in the escalation queue. 5. Verify an Intelligence Analyst sees the alert only if it is in their district scope. | Critical alert (score 92) is auto-routed to the Control Room queue per threshold configuration. Control Room Operator sees the alert immediately. Routing is configuration-driven and does not require code changes. | P1 |
| TC-SMM-FR07-003 | FR-07 / BR-FR-07-02 | Verify analyst priority override with mandatory reason and audit trail | Alert exists with computed risk_score = 55, priority_band = MEDIUM. Intelligence Analyst believes the threat is more urgent. | 1. Log in as Intelligence Analyst. 2. Navigate to Alert detail. 3. Click "Override Priority". 4. Attempt to change priority to HIGH without providing a reason. 5. Verify validation error: "Reason is mandatory for priority override." 6. Enter reason: "Actor linked to known trafficking network in Warangal - immediate attention needed." 7. Change priority to HIGH. 8. Verify the original computed score (55) remains visible alongside the overridden priority (HIGH). | Override without reason is rejected. With mandatory reason, override succeeds. The original computed score (55) and original priority band (MEDIUM) remain visible in the audit trail. The overridden priority (HIGH) is the active routing priority. Audit log captures the override with analyst identity, reason, and timestamp. | P2 |
| TC-SMM-FR07-004 | FR-07 / AC-FR-07-05 | Verify score recalculation after virality spike | Alert exists with initial risk_score = 52 (MEDIUM). The source post initially had 100 shares. After 2 hours, the post has gone viral with 50,000 shares. | 1. Simulate updated engagement data: shares = 50,000 via the ingestion pipeline. 2. Verify the risk scoring engine triggers recalculation. 3. Verify the updated risk_score reflects the virality increase (expected to push into HIGH or CRITICAL range). 4. Verify the priority_band is updated accordingly. 5. Verify the alert is re-routed if it now crosses a threshold (e.g., from analyst queue to supervisor/Control Room queue). | Score recalculation is triggered by the virality metric change. Risk score increases significantly (e.g., from 52 to 78). Priority band updates from MEDIUM to HIGH. Alert is re-routed to the appropriate queue based on the new threshold. Timeline records the recalculation event. | P2 |
| TC-SMM-FR07-005 | FR-07 / BR-FR-07-01 / Edge Case | Verify brand-new actor with no history defaults to zero contribution | Content item from a new actor with no prior alerts, no historical cases, and no linked records. Post has 10 likes (low virality). Category severity = 60. | 1. Trigger risk scoring on the content item. 2. Verify the scorecard shows: actor_frequency = 0, historical_behavior = 0, virality = minimal contribution. 3. Verify risk_score is computed primarily from content_severity (60) and policy weight. 4. Verify priority_band = MEDIUM (expected score 50-69 range). 5. Verify no errors or null failures from missing history data. | Missing virality and history data default to zero contribution (not null failure). Risk score is computed from available factors only. The system handles first-time actors gracefully without errors. Scorecard displays 0 for unknown factors. | P3 |
| TC-SMM-FR07-006 | FR-07 / AC-FR-07-01 / AC-FR-07-03 | Verify auto-alert generation when ingested content exceeds configurable threat threshold | ALERT_THREAT_THRESHOLD is set to 40 (default). Reddit connector is ACTIVE. Watchlist contains keywords likely to match threat-related content (e.g., "cyber crime india"). | 1. Trigger a connector poll cycle. 2. Wait for ingestion to complete. 3. Query `SELECT COUNT(*) FROM sm_alert WHERE alert_type = 'AUTO_DETECTED'`. 4. Verify count > 0 (at least one auto-alert created). 5. Inspect an auto-alert: verify priority is HIGH (risk_score >= 70), MEDIUM (>= 50), or LOW (< 50). 6. Verify alert title contains classification category and platform name. 7. Verify alert description includes risk score, author handle, and content URL. 8. Verify content_id FK links to the ingested content_item. | Auto-alerts are created for content with riskScore >= ALERT_THREAT_THRESHOLD (40). Priority bands map correctly: HIGH >= 70, MEDIUM >= 50, LOW < 50. Alert includes full source attribution (platform, author, URL). content_id foreign key links the alert to the ingested content. | P1 |
| TC-SMM-FR07-007 | FR-07 / AC-FR-07-01 / Edge Case | Verify no auto-alert for content below threat threshold | ALERT_THREAT_THRESHOLD is set to 40. Content item is ingested with benign text (e.g., "Beautiful sunset photos from Hyderabad park") that produces riskScore = 0. | 1. Ingest the benign content item. 2. Verify content_item is created with threat_score = 0. 3. Query `SELECT COUNT(*) FROM sm_alert WHERE content_id = '{content_id}'`. 4. Verify count = 0 (no alert created). 5. Verify the content appears in the content list but not in the alerts list. | Content below the threat threshold does not generate an auto-alert. The content is still stored and searchable, but no alert workflow is triggered. This prevents alert fatigue from low-risk content. | P2 |

---

## 9. FR-08 - Legal and Policy Mapping

**BRD Reference:** FR-08, AC-FR-08-01 through AC-FR-08-05, ENT-13 (LegalMappingRule), ENT-14 (LegalMappingResult)

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR08-001 | FR-08 / AC-FR-08-01 / AC-FR-08-02 | Verify legal mapping results on alert card | Alert exists with category = DRUG_SALE. Legal mapping rules are configured for Bharatiya Nyaya Sanhita (BNS) and NDPS Act. Rule version v3 is active. | 1. Navigate to the alert detail view. 2. Verify the "Legal Mapping" section shows suggested provisions. 3. Verify the top suggestion displays: law_name = "NDPS Act, 1985", provision_code = "Section 20(b)(ii)", rule_version = "v3", rationale = "Sale of cannabis derivative exceeding small quantity", confidence = 0.88, reviewer_status = PENDING. 4. Verify linked evidence references are displayed. 5. Verify a second suggestion for BNS is also shown if applicable. | Alert card displays legal mapping suggestions with all required fields (law_name, provision_code, rule_version, rationale, confidence, reviewer_status). Evidence references are linked. Multiple provisions can appear for one alert. Reviewer status defaults to PENDING until a Legal Reviewer acts. | P1 |
| TC-SMM-FR08-002 | FR-08 / AC-FR-08-05 | Verify legal draft cannot be exported without Legal Reviewer approval | An investigation report draft with legal sections has been generated for a case. Legal Reviewer has NOT yet approved it. Investigator attempts to export. | 1. Log in as Investigator (ROL-05). 2. Navigate to Case > Reports > Generated Draft. 3. Click "Export as PDF". 4. Verify the export is blocked with message: "Legal draft requires approval by Legal Reviewer before export." 5. Log in as Legal Reviewer (ROL-07). 6. Navigate to the report, review legal sections, and click "Approve". 7. Log back in as Investigator and attempt export again. | Export is blocked when legal draft is unapproved (reviewer_status = PENDING). After Legal Reviewer approval (reviewer_status = APPROVED), export succeeds. The PDF includes the approved legal text with reviewer identity and approval timestamp in metadata. | P2 |
| TC-SMM-FR08-003 | FR-08 / AC-FR-08-04 | Verify legal rule versioning, effective dating, and rollback | Legal rule "NDPS-SEC-20B" version v3 is active. A new version v4 is prepared with updated rationale text and effective_from = 2026-04-01. | 1. Log in as Platform Admin. 2. Create rule version v4 for "NDPS-SEC-20B" in UAT with effective_from = 2026-04-01. 3. Submit for Legal Reviewer approval. 4. Approve in UAT. Promote to Production. 5. Verify v3 remains active for alerts processed before 2026-04-01. 6. After effective date, verify v4 is used for new classifications. 7. Trigger rollback of v4 back to v3. 8. Verify v3 is restored without any code changes. | Rule versioning supports effective dating. v3 remains active until the effective date of v4. After effective date, v4 is used. Rollback restores v3 as the active rule without code changes. All version transitions are audit-logged with before/after version numbers. | P2 |
| TC-SMM-FR08-004 | FR-08 / BR-FR-08-02 / Edge Case | Verify alert remains triageable when no legal mapping rule matches | Alert exists with a new/unusual category "PRECURSOR_CHEMICAL_TRADE" for which no legal mapping rule has been configured yet. | 1. Trigger legal mapping evaluation for the alert. 2. Verify the system does not error out. 3. Verify alert.legal_status = NO_MATCH_FOUND. 4. Verify the alert remains visible in the analyst queue and is fully triageable. 5. Verify the alert detail shows "No matching legal provision found" with a visual indicator. | Alert is not blocked by the absence of legal rules. legal_status = NO_MATCH_FOUND is set. The alert remains in the analyst queue for manual triage. Analysts can proceed with investigation and manually specify legal provisions if needed. | P3 |
| TC-SMM-FR08-005 | FR-08 / Failure Handling | Verify graceful handling when legal rule evaluation fails | Legal mapping engine encounters a runtime error (e.g., malformed rule_expression in rule "NDPS-SEC-22A"). Alert is being processed. | 1. Trigger legal mapping evaluation on the alert. 2. Verify the rule evaluation failure is caught and logged. 3. Verify alert.legal_status = PENDING_REVIEW. 4. Verify the error event is recorded with rule_id, error details, and timestamp. 5. Verify the alert remains triageable in the analyst queue. 6. Verify the analyst can manually assign legal provisions. | Rule evaluation failure does not crash the pipeline. Alert shows legal_status = PENDING_REVIEW. Error is logged for administrator investigation. Alert remains fully functional for analyst triage and manual legal assignment. | P4 |

---

## 10. FR-09 - Translation and Language Intelligence

**BRD Reference:** FR-09, AC-FR-09-01 through AC-FR-09-05, ENT-15 (TranslationRecord)

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR09-001 | FR-09 / AC-FR-09-01 / AC-FR-09-02 | Verify language detection and one-click translation from alert screen | Alert exists with Telugu content: "గంజాయి అమ్మకం చాలా తక్కువ ధరలో". Intelligence Analyst (preferred language: English) is logged in. | 1. Navigate to alert detail. 2. Verify detected_language = "te" (Telugu) with detection_confidence = 0.96. 3. Click the "Translate" button. 4. Verify `POST /api/v1/translations` is called with source_object_type = ALERT, target_language = "en". 5. Verify translation result appears: "Ganja sale at very low prices". 6. Verify translation_status = COMPLETED. | Language is detected as Telugu with high confidence. One-click translation produces English output. TranslationRecord is created with source_language = "te", target_language = "en", provider_name, glossary_version, and COMPLETED status. | P1 |
| TC-SMM-FR09-002 | FR-09 / AC-FR-09-03 / AC-FR-09-04 | Verify original text immutability and side-by-side display | Translation has been completed for a Telugu alert (from TC-SMM-FR09-001). | 1. Navigate to alert detail after translation. 2. Verify the side-by-side view shows: Left panel = original Telugu text, Right panel = English translation. 3. Verify original text cannot be edited (read-only). 4. Verify source timestamp and evidence reference number are displayed above both panels. 5. Attempt to modify the original text via `PUT /api/v1/content/{id}` with modified content_text. 6. Verify the API rejects modification of original content text. | Side-by-side display renders correctly. Original Telugu text is immutable (API rejects modification). Translation is stored as a separate versioned record. Both panels show the source timestamp and reference number. | P2 |
| TC-SMM-FR09-003 | FR-09 / AC-FR-09-05 | Verify department glossary override in translation | Department glossary contains: "maal" -> "narcotic substance" (not the generic translation "goods"). Content contains Hindi text with the word "maal". | 1. Request translation of the Hindi content to English. 2. Verify the translation output uses "narcotic substance" for "maal" (not "goods" or "stuff"). 3. Verify the TranslationRecord includes glossary_version (e.g., "glossary-v5"). 4. Compare with a translation of the same text without glossary override (if configurable) to confirm the difference. | Department glossary term overrides the generic translation. "maal" is translated as "narcotic substance" per the approved glossary. The glossary version used is recorded in the translation record for traceability. | P2 |
| TC-SMM-FR09-004 | FR-09 / Edge Case | Verify handling of mixed-language post | Content contains mixed Hindi-English text: "Bhai mere paas best quality maal hai, DM me for prices, delivery available in Hyderabad." | 1. Ingest the content item. 2. Verify detected_language = "hi" (Hindi as primary) with detection method noted. 3. Request translation to English. 4. Verify the translation handles the mixed content: Hindi portions are translated, English portions are preserved. 5. Verify the output reads naturally (e.g., "Brother, I have the best quality narcotic substance, DM me for prices, delivery available in Hyderabad."). | Mixed-language detection identifies Hindi as the primary language. Translation handles both languages, translating Hindi portions and preserving English portions. Glossary override applies to "maal" in the Hindi portion. | P3 |
| TC-SMM-FR09-005 | FR-09 / Failure Handling | Verify graceful handling of unsupported language and provider failure | Content is in a script not supported by the translation provider (e.g., Manipuri/Meitei script). Provider returns an error. | 1. Ingest the content item. 2. Verify detected_language = "mni" (Manipuri) or UNKNOWN if detection fails. 3. Request translation to English. 4. Verify the translation provider returns an error. 5. Verify the user sees: original text displayed, error message "Translation unavailable for this language", and a "Retry Later" button. 6. Verify TranslationRecord is created with translation_status = UNSUPPORTED. 7. Verify the alert is still created and triageable (BR-FR-09-02). | Unsupported language does not block alert creation. User sees the original text with a clear error message. TranslationRecord stores UNSUPPORTED status. Retry option is available. Alert remains fully functional for triage. | P4 |

---

## 11. FR-10 - Alerts, Escalation, Collaboration, and SLA Management

**BRD Reference:** FR-10, AC-FR-10-01 through AC-FR-10-05, WF-ALT-01

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR10-001 | FR-10 / AC-FR-10-01 / AC-FR-10-02 | Verify alert creation with all required fields and SLA timer | Content item is classified (DRUG_SALE, risk_score = 88, CRITICAL). SLA config: Critical ack = 15 min, disposition = 2 hr. | 1. Verify alert is auto-created from classified content. 2. Call `GET /api/v1/alerts/{alert_id}`. 3. Verify all fields: alert_ref_no (format TEF-ALT-YYYY-NNNNNN), content_id, primary_category_id, risk_score = 88, priority_band = CRITICAL, alert_status = NEW, owner_unit_id, due_at (created_at + 15 min for ack). 4. Verify the React alert card shows SLA countdown: "Acknowledge within 14 min 30 sec". | Alert is created with all mandatory attributes per AC-FR-10-01. Reference number follows the TEF-ALT-YYYY-NNNNNN format. SLA timer starts at creation. Due timestamp is set based on Critical ack SLA (15 min). Countdown is visible in the React UI. | P1 |
| TC-SMM-FR10-002 | FR-10 / AC-FR-10-02 / WF-ALT-01 | Verify SLA breach escalation for Critical alert unacknowledged after 15 minutes | Critical alert (priority_band = CRITICAL) is created at 10:00:00 IST. No analyst has acknowledged it. SLA: Critical ack = 15 min. | 1. Wait until 10:15:00 IST (simulated or actual). 2. Verify the SLA timer service triggers an escalation action. 3. Verify the alert status changes to ESCALATED_CONTROL_ROOM (auto-escalation per WF-ALT-01). 4. Verify a notification is sent to the Supervisor. 5. Verify the SLA breach is recorded in AlertAction with action_type = ESCALATE, action_reason = "SLA_BREACH_ACK_15MIN". 6. Verify the alert appears with a "BREACHED" visual indicator (red badge) in the Control Room dashboard. | At 15 minutes, unacknowledged Critical alert auto-escalates to Control Room. Supervisor is notified. SLA breach is recorded. The React dashboard shows a red "BREACHED" badge. AlertAction log captures the auto-escalation with reason. | P1 |
| TC-SMM-FR10-003 | FR-10 / AC-FR-10-04 | Verify collaboration features: comment, tag, false positive with reason | Alert is in IN_REVIEW status. Analyst-A and Analyst-B are in the same unit. | 1. Analyst-A adds a comment: "Possible link to Warangal trafficking ring - need Analyst-B review." 2. Analyst-A tags @AnalystB in the comment. 3. Verify Analyst-B receives a notification. 4. Analyst-B adds an action note: "Confirmed - same actor as case TEF-CAS-2026-000123." 5. Analyst-A marks the alert as FALSE_POSITIVE. 6. Verify a mandatory reason field appears. Enter: "Duplicate of existing alert TEF-ALT-2026-000456." 7. Verify the false positive disposition is recorded. | Comments are saved and visible to both analysts. Tagging triggers a notification. Action notes are appended to the alert timeline. False positive marking requires a mandatory reason (empty reason is rejected). All actions create AlertAction records with timestamps and actor identities. | P1 |
| TC-SMM-FR10-004 | FR-10 / AC-FR-10-03 | Verify external sharing via approved channel with watermark and audit | Alert (CRITICAL) needs to be shared externally. Supervisor approval is required. Approved channel = WHATSAPP_APPROVED. | 1. Analyst clicks "Share" on the alert. 2. Select channel = WHATSAPP_APPROVED. 3. Verify approval request is sent to Supervisor. 4. Supervisor approves the share action. 5. Verify the shared content includes watermark (e.g., "TEF-CONFIDENTIAL - TEF-ALT-2026-000789"). 6. Verify share is access-controlled (link/message has expiry or access restrictions). 7. Verify audit log entry: action_type = SHARE, channel = WHATSAPP_APPROVED, actor, approver, timestamp. | External sharing requires supervisor approval. Shared content is watermarked with alert reference. Share is access-controlled. Complete audit trail is recorded including channel, approver, and timestamp. | P2 |
| TC-SMM-FR10-005 | FR-10 / BR-FR-10-02 | Verify alert cannot be closed with pending open tasks | Alert is in IN_REVIEW status. A linked CaseTask exists with task_status = IN_PROGRESS (VERIFY_EVIDENCE task). | 1. Analyst attempts to close the alert with disposition "CLOSED_NO_ACTION". 2. Verify closure is blocked with message: "Cannot close alert: 1 linked task is still open (VERIFY_EVIDENCE)." 3. Complete the open task (mark as COMPLETED). 4. Attempt closure again with disposition code and reason: "No further action - evidence verified, no actionable content." 5. Verify closure succeeds and alert_status = CLOSED_NO_ACTION. | Closure is blocked when open tasks exist. Error message identifies the specific blocking task. After task completion, closure succeeds with mandatory disposition code and reason. Closed alert records disposition_code, reason, and closed_at timestamp. | P2 |
| TC-SMM-FR10-006 | FR-10 / Edge Case / Failure Handling | Verify SLA state displays UNKNOWN when timer service fails | SLA timer service is simulated as unavailable. Alert exists with priority_band = HIGH. | 1. Navigate to the alert detail. 2. Verify the SLA countdown area displays "SLA: UNKNOWN" instead of a countdown timer. 3. Verify an operational incident has been raised (visible in Admin > Incidents). 4. Verify the alert remains functional for all other operations (acknowledge, comment, escalate). 5. Restore the SLA timer service and verify the countdown resumes correctly. | SLA state is displayed as UNKNOWN (not a crash or blank). Operational incident is raised automatically. Alert remains fully functional for analyst actions. Timer resumes when service is restored. | P3 |
| TC-SMM-FR10-007 | FR-10 / AC-FR-10-01 | Verify AUTO_DETECTED alert includes full source attribution from connector ingestion | Connector scheduler has run a poll cycle. Auto-detected alerts exist from ingested Reddit and YouTube content. | 1. Query `SELECT alert_id, alert_ref, alert_type, priority, title, description, content_id FROM sm_alert WHERE alert_type = 'AUTO_DETECTED' LIMIT 5`. 2. Verify alert_ref follows format `SM-ALERT-YYYY-NNNNNN` (using sm_alert_ref_seq). 3. Verify content_id is NOT NULL and references a valid content_item. 4. Join to content_item and verify platform, author_handle, and content_url are populated. 5. Verify alert title contains `[CATEGORY] platform — excerpt` pattern. 6. Verify alert description includes risk score, author handle, and content URL. 7. Verify state_id = 'NEW' and alert_type = 'AUTO_DETECTED'. | AUTO_DETECTED alerts have full source attribution: content_id FK to content_item, platform, author, URL in description. Alert ref is auto-generated from sequence. Alerts start in NEW state and follow the standard sm_alert workflow. Entity extraction on the source content creates extracted_entity records linked to the content_item. | P1 |

---

## 12. FR-11 - Evidence Preservation and Chain of Custody

**BRD Reference:** FR-11, AC-FR-11-01 through AC-FR-11-05, WF-EVD-01, ENT-10 (EvidenceItem)

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR11-001 | FR-11 / AC-FR-11-01 / AC-FR-11-02 | Verify evidence capture with all required metadata and unique reference number | Alert exists linked to an X/Twitter post. Analyst (ROL-03) is logged in. | 1. Navigate to Alert detail. Click "Capture Evidence". 2. Verify evidence capture initiates and stores: source_content_id, capture_mode = SNAPSHOT, captured_at (UTC), captured_by = Analyst user_id, source_url, posted_at, metadata_json (including source_url, posted_at, captured_at, operator_id). 3. Verify SHA-256 hash is computed for the preserved media/files. 4. Verify evidence_ref_no is generated in format TEF-EVD-YYYY-NNNNNN. 5. Verify the reference number appears on the UI card and is searchable. | Evidence item is created with all required metadata. SHA-256 hash is stored. Unique reference number (TEF-EVD-YYYY-NNNNNN) is generated and displayed. All fields match AC-FR-11-01 requirements. Captured_at timestamp is in UTC. | P1 |
| TC-SMM-FR11-002 | FR-11 / AC-FR-11-03 | Verify master evidence immutability after verification | Evidence item is in CAPTURED status. Evidence Custodian (ROL-08) is logged in. | 1. Evidence Custodian navigates to Evidence detail. 2. Clicks "Verify Integrity". 3. System re-computes SHA-256 hash and compares with stored hash. 4. Hashes match: evidence_status changes to VERIFIED. 5. Attempt to modify the master evidence object (e.g., replace the stored file). 6. Verify modification is rejected: "Master evidence is immutable after verification." 7. Verify working copies can be created separately from the master. | Hash verification succeeds and status moves to VERIFIED. Master evidence object is locked (immutable). Any attempt to modify the master is rejected. Working/redacted copies are stored at separate object keys (preview_object_key), distinct from master_object_key. | P1 |
| TC-SMM-FR11-003 | FR-11 / AC-FR-11-04 | Verify chain-of-custody log for all evidence events | Evidence item (TEF-EVD-2026-000001) exists in VERIFIED status. Multiple users will interact with it. | 1. Analyst views the evidence (VIEW event). 2. Investigator exports the evidence (EXPORT event). 3. Legal Reviewer applies a legal hold (LEGAL_HOLD event). 4. Evidence Custodian releases the legal hold (RELEASE event with mandatory reason). 5. Navigate to Evidence > Chain of Custody log. 6. Verify each event is recorded with: event_type, actor_user_id, actor_role, timestamp, IP address, and details. | Chain-of-custody log contains all 4 events in chronological order. Each entry has actor identity, role, timestamp, source IP, and event details. Legal hold release includes the mandatory reason. The log is append-only (no edits or deletions). | P1 |
| TC-SMM-FR11-004 | FR-11 / AC-FR-11-05 | Verify evidence package export with manifest and hash list | Case has 3 verified evidence items. Investigator is logged in. Export approval is granted. | 1. Navigate to Case > Evidence > "Package for Export". 2. Select all 3 evidence items. 3. Click "Generate Export Package". 4. Verify the package contains: manifest (JSON/PDF listing all items), hash list (SHA-256 for each file), index page, and optional translated excerpts. 5. Verify each item in the manifest has its evidence_ref_no. 6. Download the package and verify file integrity by comparing hashes. | Export package is generated with manifest, hash list, index, and evidence files. Manifest cross-references evidence_ref_no for each item. Hash list matches the stored SHA-256 values. Package can be verified independently using the hash list. | P2 |
| TC-SMM-FR11-005 | FR-11 / Failure Handling | Verify partial capture handling and retry | Evidence capture for a YouTube video fails partially: metadata is captured but video download times out. | 1. Trigger evidence capture for a YouTube video post. 2. Simulate video download timeout (media not fully downloaded). 3. Verify evidence_status = PARTIAL_CAPTURE. 4. Verify the evidence item records which components are missing (e.g., "video_media: FAILED"). 5. Verify the item appears in the "Retry Capture" queue. 6. Retry the capture after connectivity is restored. 7. Verify evidence_status changes to CAPTURED after successful retry. | Partial capture is handled gracefully. Status is PARTIAL_CAPTURE with a missing component list. Item is available for retry. After successful retry, status moves to CAPTURED with complete metadata and hash. No loss of initially captured metadata. | P2 |
| TC-SMM-FR11-006 | FR-11 / Failure Handling | Verify hash verification failure marks INTEGRITY_EXCEPTION and blocks export | Evidence item exists. The stored file has been corrupted (simulated by altering a byte in storage). Evidence Custodian runs verification. | 1. Evidence Custodian clicks "Verify Integrity" on the evidence item. 2. System re-computes SHA-256 hash: computed hash differs from stored hash. 3. Verify evidence_status = INTEGRITY_EXCEPTION. 4. Verify the evidence is immediately hidden from export workflows. 5. Attempt to include this item in an export package. 6. Verify it is excluded with message: "Evidence TEF-EVD-2026-000005 has integrity exception - excluded from export." 7. Verify an alert is raised for the Evidence Custodian. | Hash mismatch triggers INTEGRITY_EXCEPTION status. Evidence is blocked from export until reviewed and resolved. Clear error message identifies the affected item. Custodian is alerted for investigation. | P3 |

---

## 13. FR-12 - Case, Task, and Workflow Management

**BRD Reference:** FR-12, AC-FR-12-01 through AC-FR-12-05, WF-CAS-01

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR12-001 | FR-12 / AC-FR-12-01 | Verify case creation from alerts with linked evidence | Two related alerts exist: TEF-ALT-2026-000100 and TEF-ALT-2026-000101. One evidence item (TEF-EVD-2026-000050) is linked to the first alert. Analyst is logged in. | 1. Select both alerts. Click "Create Case". 2. Enter title: "Hyderabad Ganja Distribution Network - March 2026". 3. Verify case is created with case_ref_no (format TEF-CAS-YYYY-NNNNNN). 4. Verify both alerts are linked (originating_alert_id for the first, related reference for the second). 5. Verify the evidence item is automatically linked to the case. 6. Add a case note: "Initial triage indicates organized distribution network." 7. Verify case_status = OPEN and opened_at timestamp is set. | Case is created with unique reference number. Both alerts are linked. Evidence is inherited from linked alerts. Case note is appended. Status is OPEN. Timeline shows case creation event with all linked objects. | P1 |
| TC-SMM-FR12-002 | FR-12 / AC-FR-12-02 / AC-FR-12-03 | Verify case assignment, investigation, and timeline tracking | Case (TEF-CAS-2026-000001) is in OPEN status. Supervisor and Investigator are in the same unit. | 1. Supervisor assigns the case to Investigator-A with due_date = 2026-03-10. 2. Verify case_status = ASSIGNED and assigned_to_user_id = Investigator-A. 3. Investigator-A accepts work: case_status = UNDER_INVESTIGATION. 4. Investigator-A captures additional evidence and adds a note. 5. Navigate to Case > Timeline. 6. Verify timeline includes: case creation, assignment (with assignee and due date), investigation start, evidence capture, and note addition. | Case moves through OPEN -> ASSIGNED -> UNDER_INVESTIGATION states. Timeline records all events with actor, timestamp, and details. Due date is set and visible. Each state transition is logged. | P1 |
| TC-SMM-FR12-003 | FR-12 / AC-FR-12-04 | Verify case closure requires reason, outcome code, and supervisory approval | Case is in AWAITING_REVIEW status. Supervisor is logged in. Closure requires supervisory approval per workflow config. | 1. Supervisor navigates to Case > Review. 2. Attempt to close without closure_reason. Verify validation error: "Closure reason is mandatory." 3. Attempt to close without outcome_code. Verify validation error: "Outcome code is required." 4. Enter closure_reason = "Investigation complete - suspects identified and reported to district unit", outcome_code = "REFERRED_FOR_ACTION". 5. Approve closure. 6. Verify case_status = CLOSED and closed_at timestamp is set. | Closure without mandatory fields is rejected with specific validation messages. With all required fields, closure succeeds. Case status = CLOSED. Closure metadata (reason, outcome_code, approver, timestamp) is permanently recorded. | P2 |
| TC-SMM-FR12-004 | FR-12 / AC-FR-12-05 | Verify case reopen preserves original closure metadata | Case (TEF-CAS-2026-000001) is in CLOSED status with closure_reason and outcome_code recorded. New evidence emerges. Supervisor is logged in. | 1. Supervisor clicks "Reopen Case" with reason: "New evidence from Reddit post links additional suspects." 2. Verify case_status = REOPENED. 3. Verify the original closure metadata (closure_reason, outcome_code, closed_at, approver) is PRESERVED (not overwritten). 4. Verify a new timeline event is appended: "Case reopened by Supervisor - reason: New evidence from Reddit post links additional suspects." 5. Investigator acknowledges: case_status = UNDER_INVESTIGATION. | Case reopens successfully. Original closure metadata is preserved in the record. New timeline event is appended. Status transitions through REOPENED -> UNDER_INVESTIGATION. Complete audit history is maintained. | P2 |
| TC-SMM-FR12-005 | FR-12 / Failure Handling / Edge Case | Verify case routing when assigned user becomes inactive | Case is ASSIGNED to Investigator-A. Platform Admin disables Investigator-A's account. | 1. Platform Admin disables Investigator-A. 2. Verify the system detects the inactive assignee for the open case. 3. Verify the case is routed to the owning unit queue (not left in limbo). 4. Verify the Supervisor of the unit is notified: "Case TEF-CAS-2026-000001 unassigned - Investigator-A deactivated." 5. Supervisor reassigns to Investigator-B. 6. Verify timeline records the reassignment event. | Case does not get stuck when the assignee is deactivated. It routes to the unit queue. Supervisor is notified. Reassignment is tracked in the timeline. No data loss or workflow deadlock. | P3 |

---

## 14. FR-13 - Reporting, Template Management, and MIS Analytics

**BRD Reference:** FR-13, AC-FR-13-01 through AC-FR-13-05, WF-RPT-01

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR13-001 | FR-13 / AC-FR-13-01 | Verify report template creation, placeholder definition, and approval workflow | Platform Admin or authorized user is logged in. No custom templates exist yet. | 1. Navigate to Admin > Report Templates > Create New. 2. Upload a DOCX template with placeholders: `{{case_ref_no}}`, `{{alert_summary}}`, `{{evidence_annexure}}`, `{{legal_provisions}}`, `{{chronology}}`, `{{translated_excerpts}}`. 3. Define placeholder mappings to data sources. 4. Save as DRAFT. Submit for approval. 5. Legal Reviewer approves the template (version v1, config_status = APPROVED_PROD). 6. Verify template appears in the production template list for report generation. | Template is created with defined placeholders. Approval workflow is followed (DRAFT -> APPROVED_PROD). Only APPROVED template versions appear in the production report generation list. Template version v1 is recorded with approver identity and timestamp. | P1 |
| TC-SMM-FR13-002 | FR-13 / AC-FR-13-02 / AC-FR-13-03 | Verify report generation with evidence annexures and PDF/DOCX export | Case has 2 evidence items, legal mapping results, and translated excerpts. Approved template (v1) exists. Investigator is logged in. | 1. Navigate to Case > Generate Report. 2. Select approved template v1. 3. Click "Generate". 4. Verify the generated report includes: chronology, evidence annexures (with TEF-EVD reference numbers), translated excerpts, legal references (with provision codes), and reference-number cross-links. 5. Export as PDF. Verify formatting is preserved and metadata summary page is included. 6. Export as DOCX. Verify formatting is preserved. | Report is generated with all sections populated from case data. Evidence annexures reference TEF-EVD numbers. Legal sections reference provision codes. PDF and DOCX exports have preserved formatting. Metadata summary page lists generation timestamp, template version, and case reference. | P2 |
| TC-SMM-FR13-003 | FR-13 / AC-FR-13-04 | Verify MIS catalog reports are available and filterable | System has operational data: alerts, cases, SLA breaches, and evidence exports across multiple districts and platforms. Leadership Read Only user is logged in. | 1. Navigate to Reports > MIS Dashboard. 2. Verify the following reports are available: Daily Alert Summary, SLA Breach Report, Platform Trend Report, Category Heat Map, Case Progress Report, Evidence Export Register. 3. Open the SLA Breach Report. 4. Filter by date_range = "2026-03-01 to 2026-03-03", district = "Hyderabad", priority = CRITICAL. 5. Verify filtered results show only Critical SLA breaches in Hyderabad for the specified dates. | All 6 MIS catalog reports are listed and accessible. SLA Breach Report renders with correct data. Filters (date range, platform, district, category, priority, status, owner unit) work correctly. Leadership Read Only user can view but not modify reports. | P2 |
| TC-SMM-FR13-004 | FR-13 / BR-FR-13-01 | Verify only APPROVED template versions can be used in Production | Template "Investigation Report" has v1 (APPROVED_PROD) and v2 (DRAFT). Investigator attempts to generate a report. | 1. Navigate to Case > Generate Report. 2. Verify the template dropdown shows only v1 (APPROVED_PROD). 3. Verify v2 (DRAFT) is NOT available in the dropdown. 4. Attempt to call `POST /api/v1/reports/generate` with template_version = v2 directly via API. 5. Verify the API rejects the request: "Template version v2 is not approved for production use." | Only APPROVED_PROD template versions appear in the UI. API enforces the same restriction. DRAFT templates cannot be used for production report generation. | P3 |
| TC-SMM-FR13-005 | FR-13 / Edge Case / Failure Handling | Verify handling when template placeholder has missing source data | Template has placeholder `{{witness_statement}}` but the case has no witness statement data. Report generation is attempted. | 1. Generate report for a case that lacks witness statement data. 2. Verify the report is generated with the `{{witness_statement}}` section showing "[Data not available]" or equivalent placeholder text. 3. Verify no error or crash occurs. 4. Verify the report status = DRAFT (not GENERATION_FAILED). 5. Verify the analyst can manually fill in the missing section before submission. | Missing placeholder data does not crash report generation. The section shows a clear "Data not available" indicator. Report is generated as DRAFT for manual completion. Analyst can edit before submitting for review. | P4 |

---

## 15. FR-14 - Administration, Taxonomy, Legal Rules, and Configuration Governance

**BRD Reference:** FR-14, AC-FR-14-01 through AC-FR-14-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR14-001 | FR-14 / AC-FR-14-01 / AC-FR-14-02 | Verify configuration object versioning and metadata | Platform Admin is logged in. Taxonomy category "DRUG_SALE" exists at version v2 (APPROVED_PROD). | 1. Navigate to Admin > Taxonomy > DRUG_SALE. 2. Edit the description to include "Includes online marketplace solicitation." 3. Save changes as v3 (DRAFT). 4. Verify v3 stores: version_number = 3, approval_status = DRAFT, effective_dates, change_reason = "Added online marketplace solicitation scope", changed_by = Admin user_id. 5. Submit for approval. Supervisor approves. 6. Verify v3 status = APPROVED_UAT. 7. Verify v2 remains the active Production version until promotion. | Configuration versioning works correctly. Each version stores version number, approval status, effective dates, change reason, and approver identity. DRAFT -> APPROVED_UAT -> APPROVED_PROD workflow is enforced. Prior versions are retained. | P1 |
| TC-SMM-FR14-002 | FR-14 / AC-FR-14-03 | Verify UAT validation and Production promotion create distinct audit events | Configuration item (risk weight for "DRUG_TRAFFICKING") is updated in UAT and ready for Production promotion. | 1. Validate the configuration in UAT (test with sample alerts). 2. Verify audit event: "UAT_VALIDATION_COMPLETE" with validator identity and timestamp. 3. Submit for Production promotion. 4. Approver approves promotion. 5. Verify distinct audit event: "PRODUCTION_PROMOTION" with promoter identity, approver identity, before_version, after_version, and timestamp. 6. Verify the two events are separate entries in the audit log. | UAT validation and Production promotion generate separate, distinct audit events. Each event captures different actors and contexts. Release artifacts (version snapshots) are created for both stages. | P1 |
| TC-SMM-FR14-003 | FR-14 / AC-FR-14-04 | Verify conflicting configuration definitions are blocked | Two taxonomy categories are being created with the same category_code "SYNTH_DRUG" but different definitions. | 1. Create taxonomy category: code = "SYNTH_DRUG", name = "Synthetic Drugs". Save successfully. 2. Attempt to create another category: code = "SYNTH_DRUG", name = "Synthetic Drug Substances". 3. Verify the system blocks the creation: "Conflicting configuration: category_code 'SYNTH_DRUG' already exists." 4. Attempt to create a legal rule with effective_date overlap for the same scope. 5. Verify overlap is blocked: "Effective date overlap detected for rule scope." | Duplicate category codes are rejected. Effective date overlaps for the same scope are blocked. Validation runs before publication. Precise field-level error messages are returned. | P2 |
| TC-SMM-FR14-004 | FR-14 / AC-FR-14-05 | Verify one-action rollback to previous configuration version | Taxonomy category "DRUG_PROMOTION" was updated from v4 to v5. v5 causes issues (e.g., too broad, generating false positives). Platform Admin needs to rollback. | 1. Navigate to Admin > Taxonomy > DRUG_PROMOTION. 2. Click "Rollback to Previous Version". 3. Verify confirmation dialog shows: "Rollback from v5 to v4. This will restore the previous approved configuration." 4. Confirm rollback. 5. Verify the active version is now v4 (config_status = APPROVED_PROD). 6. Verify v5 is marked as ROLLED_BACK. 7. Verify no application code changes were required. 8. Verify new alerts use v4 classification rules. | Rollback is completed in one action (single click + confirmation). v4 is restored as active. v5 is marked ROLLED_BACK. No code changes required. New alerts immediately use the rolled-back version. Audit trail records the rollback. | P2 |
| TC-SMM-FR14-005 | FR-14 / BR-FR-14-01 / Edge Case | Verify emergency change override bypasses UAT with logging | A critical legal rule update is needed immediately in Production (e.g., new BNS amendment). Normal UAT validation cannot wait. | 1. Platform Admin initiates an emergency configuration change. 2. Verify the system prompts for "Emergency Change Override" approval. 3. Approver (senior supervisor) authorizes the emergency change with reason: "BNS Amendment 2026 - effective immediately per Department directive." 4. Verify the change is applied directly to Production without UAT validation. 5. Verify the audit log records: emergency_override = true, approver, reason, timestamp. 6. Verify the change is flagged for post-deployment UAT validation. | Emergency override bypasses UAT but requires explicit approval and logging. The change is applied to Production immediately. Audit trail clearly marks it as an emergency override. Post-deployment UAT validation is flagged as a follow-up task. | P3 |
| TC-SMM-FR14-006 | FR-14 / AC-FR-14-01 / Security | Verify connector CRUD routes require PLATFORM_ADMINISTRATOR role | Users exist: admin (PLATFORM_ADMINISTRATOR), analyst1 (INTELLIGENCE_ANALYST). Both have valid auth tokens. Five connectors exist in source_connector table. | 1. Call `GET /api/v1/connectors` as analyst1. Verify HTTP 403 "Admin access required". 2. Call `POST /api/v1/connectors` as analyst1 with body `{"platform":"telegram","connectorType":"Polling"}`. Verify HTTP 403. 3. Call `PUT /api/v1/connectors/{id}` as analyst1 with body `{"isActive":false}`. Verify HTTP 403. 4. Repeat all three calls as admin. 5. Verify GET returns 5 connectors with pagination. 6. Verify POST creates a new connector (HTTP 201). 7. Verify PUT updates the connector (isActive toggled). | Non-admin roles receive HTTP 403 on all connector CRUD endpoints. Admin role can list, create, and update connectors. This enforces AC-FR-14-01 (configuration objects managed by administrators) and prevents unauthorized connector manipulation. | P1 |
| TC-SMM-FR14-007 | FR-14 / AC-FR-14-01 | Verify connector toggle active/inactive stops and resumes ingestion | Admin is logged in. Reddit connector exists and is ACTIVE. Content is being ingested from Reddit. | 1. Call `PUT /api/v1/connectors/{reddit_connector_id}` with body `{"isActive": false}`. 2. Verify response shows `is_active = false`. 3. Wait for next poll cycle. 4. Verify no new Reddit content is ingested (connector skipped because is_active = FALSE in DB). 5. Call `PUT /api/v1/connectors/{reddit_connector_id}` with body `{"isActive": true}`. 6. Wait for next poll cycle. 7. Verify Reddit content ingestion resumes. | Toggling is_active to false immediately stops the connector from being polled. Toggling back to true resumes ingestion on the next cycle. This provides administrators with runtime control over individual connectors without restarting the application. | P2 |

---

## 16. FR-15 - Notifications, Sharing, and External Integrations

**BRD Reference:** FR-15, AC-FR-15-01 through AC-FR-15-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR15-001 | FR-15 / AC-FR-15-01 / AC-FR-15-02 | Verify outbound share message with watermark and access control | Critical alert needs to be shared via WHATSAPP_APPROVED channel. Integration adapter is configured. Supervisor has approved sharing. | 1. Trigger share action from alert detail. 2. Verify the outbound message includes: watermark ("TEF-CONFIDENTIAL"), source reference (alert_ref_no), category, priority = CRITICAL, and access-controlled link. 3. Verify the link has an expiry time (e.g., 24 hours). 4. Verify the notification event is logged: channel = WHATSAPP_APPROVED, delivery_status = SENT, delivery_attempts = 1. | Outbound message contains watermark, reference, category, and priority. Link is access-controlled with expiry. NotificationEvent record is created with SENT status. All fields per AC-FR-15-02 are populated. | P1 |
| TC-SMM-FR15-002 | FR-15 / AC-FR-15-03 | Verify idempotency key prevents duplicate external actions on retry | Integration write operation (e.g., sending a notification) fails on first attempt due to network timeout. The system retries. | 1. Trigger a notification send that fails with a timeout. 2. Verify the system retries with the same idempotency_key. 3. Verify the external system (or mock) receives only ONE notification despite the retry. 4. Verify delivery_status updates from PENDING to SENT after successful retry. 5. Verify delivery_attempts = 2. | Idempotency key ensures no duplicate external actions. Despite retry, only one notification is delivered. The delivery status correctly reflects the final outcome. Retry count is tracked. | P1 |
| TC-SMM-FR15-003 | FR-15 / AC-FR-15-04 | Verify failed integration moves to dead-letter queue after retry exhaustion | Email notification adapter is configured with retry policy: maxRetries = 3. Email server is down. | 1. Trigger email notification for an SLA breach alert. 2. Verify 3 retry attempts are made. 3. Verify after retry exhaustion, the notification moves to DLQ with delivery_status = DLQ. 4. Navigate to Admin > Integration DLQ. 5. Verify the failed notification is visible with error details, retry count, and last_attempt_at timestamp. 6. Manually retry from DLQ after email server recovery. 7. Verify delivery succeeds and status updates to SENT. | Failed integration retries up to the configured limit, then moves to DLQ. DLQ entries are visible to operators with full error details. Manual retry from DLQ succeeds after recovery. Status transitions: PENDING -> FAILED (retries) -> DLQ -> SENT. | P2 |
| TC-SMM-FR15-004 | FR-15 / AC-FR-15-05 | Verify security event forwarding to SIEM/SOC | SIEM integration adapter is configured. A privileged action occurs (Platform Admin changes RBAC configuration). | 1. Platform Admin modifies a role's permission level. 2. Verify the audit event is generated locally. 3. Verify the audit event is forwarded to the SIEM endpoint. 4. Check the SIEM (or mock) for the received event containing: actor, action, object_type, timestamp, and outcome. 5. Verify the event format matches the SIEM integration schema. | Security events are forwarded to SIEM in real-time. Event contains all required audit fields. Local audit logging and SIEM forwarding both succeed. Integration uses the documented adapter schema. | P2 |
| TC-SMM-FR15-005 | FR-15 / Edge Case / Failure Handling | Verify local audit remains authoritative when SIEM forwarding fails | SIEM integration is down. A privileged action occurs. | 1. Perform a privileged action while SIEM integration is unavailable. 2. Verify the local audit log captures the event immediately. 3. Verify the SIEM forwarding failure is logged but does NOT block the primary action. 4. Verify the event is queued for later SIEM forwarding. 5. Restore SIEM integration. 6. Verify queued events are forwarded successfully. | Local audit logging is authoritative and never blocked by SIEM failures. Events are queued for later forwarding. After SIEM recovery, queued events are delivered. No audit data is lost. | P3 |

---

## 17. FR-16 - Audit Logging, Observability, and Data Retention

**BRD Reference:** FR-16, AC-FR-16-01 through AC-FR-16-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR16-001 | FR-16 / AC-FR-16-01 | Verify comprehensive audit log capture for CRUD and approval actions | Platform Admin creates a new organization unit. Analyst acknowledges an alert. Supervisor approves a case closure. | 1. Admin creates organization unit "Warangal District Team". 2. Query audit log for this action. Verify entry contains: actor = Admin user_id, role = PLATFORM_ADMINISTRATOR, action = CREATE, object_type = OrganizationUnit, object_id = new UUID, timestamp (UTC), source_ip, outcome = SUCCESS, after_values (unit_name, unit_code, etc.). 3. Analyst acknowledges alert TEF-ALT-2026-000200. Verify audit entry with before_values (status=NEW) and after_values (status=IN_REVIEW). 4. Supervisor approves case closure. Verify audit entry with approver identity and closure metadata. | Every material action generates an audit log entry. Entries capture actor, role, action, object type/ID, timestamp, source IP, outcome, and before/after values for state changes. Create, update, delete, and approval actions are all logged. | P1 |
| TC-SMM-FR16-002 | FR-16 / AC-FR-16-02 | Verify audit records are append-only and tamper-evident | Audit log contains 100 entries. An attempt is made to modify or delete an entry. | 1. Identify audit entry #50 (a case closure approval). 2. Attempt to modify the entry via direct database UPDATE (simulated DBA action). 3. Verify the audit system detects the tampering (chained hash/sequence break). 4. Attempt to delete audit entry #50 via `DELETE` API call. 5. Verify the API returns HTTP 403: "Audit records are immutable." 6. Verify no UI path exists to edit or delete audit entries. | Audit records are append-only. Direct database modification is detectable via chain integrity check. API and UI do not expose edit or delete operations. Tamper evidence is maintained through chaining or equivalent mechanism. | P1 |
| TC-SMM-FR16-003 | FR-16 / AC-FR-16-03 | Verify operational health dashboards show real-time metrics | All 5 source connectors are running. Queue processing is active. System has been operational for 24 hours. | 1. Navigate to Admin > Operational Dashboard. 2. Verify connector status widgets show: X (ACTIVE, last sync 2 min ago), Instagram (ACTIVE), Facebook (ACTIVE), YouTube (ACTIVE), Reddit (ACTIVE). 3. Verify queue depth metric shows current items pending processing. 4. Verify job latency metric shows p50/p95 processing times. 5. Verify export backlog shows pending export requests. 6. Verify storage utilization shows current usage and threshold. 7. Verify error-rate trend graph shows last 24 hours. | Operational dashboards display all metrics per AC-FR-16-03: connector status, queue depth, job latency, export backlog, storage utilization, and error-rate trends. Data refreshes in near-real-time. | P2 |
| TC-SMM-FR16-004 | FR-16 / AC-FR-16-04 | Verify retention policy configuration and legal-hold purge exclusion | Retention policy: alert records = 7 years, evidence records = 7 years. Evidence item TEF-EVD-2026-000010 has legal_hold_flag = true. Another item TEF-EVD-2019-000001 has retention expired and no legal hold. | 1. Navigate to Admin > Retention Policies. 2. Verify policies are configured by object type (alerts: 7 years, evidence: 7 years, audit: 7 years). 3. Trigger a retention purge cycle. 4. Verify TEF-EVD-2019-000001 (retention expired, no legal hold) is eligible for purge. 5. Verify purge requires approval before execution. 6. Approve purge. Verify the item is purged with audit record. 7. Verify TEF-EVD-2026-000010 (legal hold active) is EXCLUDED from the purge cycle entirely. | Retention policies are configurable by object type. Legal-hold items are excluded from purge (BR-FR-16-02). Purge requires approval and generates an audit record. Purged items retain a purge record (not silently deleted). | P2 |
| TC-SMM-FR16-005 | FR-16 / Failure Handling | Verify audit-log write failure causes privileged action to fail closed | Audit logging subsystem is simulated as unavailable. Admin attempts a privileged action (role permission change). | 1. Simulate audit log write failure (e.g., audit DB connection down). 2. Admin attempts to change a role's permission level. 3. Verify the role change is REJECTED: "Action denied: audit logging unavailable. Privileged actions require audit trail." 4. Verify the attempted action is NOT applied (fail-closed behavior). 5. Verify a severity-1 operational alert is raised for the audit subsystem failure. | Privileged action fails closed when audit logging is unavailable. The action is not applied. Severity-1 alert is raised. This ensures no unaudited privileged changes occur (NFR-11). | P3 |

---

## 18. FR-17 - Security, Privacy, and Responsible AI Controls

**BRD Reference:** FR-17, AC-FR-17-01 through AC-FR-17-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR17-001 | FR-17 / AC-FR-17-01 | Verify encryption at rest and in transit | Production environment is deployed. Database, object storage, and backup infrastructure are accessible for verification. | 1. Verify all API endpoints use TLS 1.2 or higher (test with `openssl s_client -tls1_1` which should fail). 2. Verify database encryption at rest is enabled (AES-256). 3. Verify object storage encryption at rest is enabled. 4. Verify backup files are encrypted. 5. Attempt to access the API over TLS 1.1 or lower. 6. Verify the connection is rejected. | TLS 1.2+ is enforced; TLS 1.1 and below are rejected. Database, object storage, and backups use AES-256 or equivalent encryption at rest. No unencrypted data pathways exist. | P1 |
| TC-SMM-FR17-002 | FR-17 / AC-FR-17-02 | Verify MFA requirement for privileged roles (PL3/PL4) | Identity platform supports MFA. Supervisor (PL3) and Platform Admin (PL4) accounts exist. Analyst (PL2) account exists. | 1. Attempt to log in as Platform Admin (PL4) with username/password only (no MFA). 2. Verify MFA challenge is presented (e.g., TOTP code required). 3. Complete MFA and verify successful login. 4. Log in as Analyst (PL2) with username/password only. 5. Verify Analyst login succeeds without MFA (PL2 does not require MFA). 6. Verify secrets rotation dates are tracked in Admin > Security > Secrets Management. | MFA is required for PL3 (Supervisor, Legal Reviewer, Evidence Custodian, Security Auditor) and PL4 (Platform Admin) roles. PL2 roles do not require MFA. Secrets rotation tracking is active. | P1 |
| TC-SMM-FR17-003 | FR-17 / AC-FR-17-03 | Verify AI-generated text is labeled DRAFT and requires approval before export | AI generates a legal summary and investigation narrative for an alert. Analyst views the output. | 1. Trigger AI-generated legal summary for an alert. 2. Verify the generated text is labeled "DRAFT - AI Generated" in the React UI. 3. Verify the DRAFT label is prominent (e.g., red banner or watermark on the text). 4. Attempt to export the draft text directly. 5. Verify export is blocked: "AI-generated content requires authorized approval before export or sharing." 6. Legal Reviewer approves the text. 7. Verify the label changes to "APPROVED" and export is now permitted. | AI-generated text is always labeled DRAFT. Export and sharing are blocked until human approval. After approval, the label changes and export is enabled. This ensures human-in-the-loop control (BR-FR-17-01). | P2 |
| TC-SMM-FR17-004 | FR-17 / AC-FR-17-04 | Verify model/prompt/rule version traceability on AI outputs | Multiple AI classifications have been performed with different model versions (llm-v2.3, llm-v2.4) and prompt versions (p-v1.7, p-v1.8). | 1. Navigate to an alert classified by model llm-v2.3 and prompt p-v1.7. 2. Verify the classification detail shows model_version = "llm-v2.3", prompt_version = "p-v1.7", rule_version = "r-v5". 3. Navigate to another alert classified by model llm-v2.4 and prompt p-v1.8. 4. Verify updated version identifiers are stored. 5. Run a query to find all alerts classified by model llm-v2.3 (for regression analysis). 6. Verify the query returns only alerts processed by that specific model version. | Every AI-derived output stores model_version, prompt_version, and rule_version. Versions are queryable for regression analysis. Version traceability enables impact assessment when models are updated. | P2 |
| TC-SMM-FR17-005 | FR-17 / Failure Handling / Edge Case | Verify watermarking failure blocks policy-enforced export | Export policy requires watermarking for all external-facing evidence exports. Watermarking service is unavailable. | 1. Analyst attempts to export an evidence package that requires watermarking. 2. Verify the watermarking service is called and fails. 3. Verify the export is BLOCKED (not delivered without watermark). 4. Verify the denial is recorded: "Export denied: watermarking service unavailable for policy-enforced export." 5. Verify the analyst receives a clear error message with retry option. | When watermarking is policy-enforced, export is blocked if watermarking fails. No unwatermarked export is allowed to proceed. Denial is logged. Analyst can retry when service recovers. | P3 |

---

## 19. FR-18 - Implementation Delivery, Training, Support, Warranty, and Ownership

**BRD Reference:** FR-18, AC-FR-18-01 through AC-FR-18-05

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-SMM-FR18-001 | FR-18 / AC-FR-18-01 / AC-FR-18-02 | Verify delivery artifacts are complete at go-live readiness | Go-live readiness checkpoint is reached. All implementation artifacts should be delivered. | 1. Verify deployment plan document exists with milestone dates: D+42 build delivery, D+49 installation, UAT sign-off, hypercare start, warranty start. 2. Verify hosting infrastructure specification (BOM) is delivered listing: compute, storage, network, OS, middleware, DB, object-store, queue, and external service dependencies. 3. Verify deployment runbook is available with step-by-step instructions. 4. Verify each artifact is version-controlled and signed off by the vendor. | All delivery artifacts are present and complete. Infrastructure BOM covers all dependencies. Deployment runbook is actionable. Milestone dates are documented relative to PO issue date. | P1 |
| TC-SMM-FR18-002 | FR-18 / AC-FR-18-03 | Verify role-based training completion and evidence | Training schedule is planned for all 8 system roles. Training materials are prepared. | 1. Verify training sessions are scheduled for each role: Intelligence Analyst, Control Room Operator, Investigator, Supervisor/Approver, Legal Reviewer, Evidence Custodian, Security Auditor, Platform Administrator. 2. Verify attendance records exist for each session. 3. Verify training materials (presentations, quick-reference guides) are handed over. 4. Verify at least one quick-reference guide exists per role. | Training is completed for all roles before go-live. Attendance is documented. Materials and quick-reference guides are delivered. Training completion is evidenced for acceptance criteria. | P2 |
| TC-SMM-FR18-003 | FR-18 / AC-FR-18-04 | Verify hypercare support roster and escalation contacts | Hypercare period has started (2 weeks post-deployment). | 1. Verify hypercare roster document exists with 1-2 named technical resources. 2. Verify duty hours are defined (e.g., 9:00 AM - 9:00 PM IST or 24x7 for critical issues). 3. Verify escalation contact matrix is available with names, phone numbers, and email addresses. 4. Simulate a severity-1 issue and verify the escalation path works (contact is reachable). | Hypercare roster is documented with named resources. Duty hours are defined. Escalation contacts are reachable. Support coverage matches the 2-week post-deployment commitment. | P3 |
| TC-SMM-FR18-004 | FR-18 / AC-FR-18-05 | Verify Department ownership documentation for data, configuration, and solution assets | Warranty period documentation is being reviewed. | 1. Verify warranty document covers break/fix, maintenance, and upgrades for one year from acceptance. 2. Verify Department ownership is documented for: data, configuration, templates, taxonomies, and custom-developed solution assets. 3. Verify data export capability in standard formats (JSON, CSV, PDF) is demonstrated. 4. Verify configuration export is available without proprietary lock-in. | Warranty terms are documented for one year. Department ownership is explicitly stated for all solution assets. Data and configuration can be exported in standard formats, ensuring no vendor lock-in (NFR-19). | P4 |

---

## 20. Traceability Matrix

### FR to Test Case Mapping

| FR ID | FR Name | Test Case IDs | Coverage |
|---|---|---|---|
| FR-01 | Platform Deployment & Environment Management | TC-SMM-FR01-001, TC-SMM-FR01-002, TC-SMM-FR01-003, TC-SMM-FR01-004, TC-SMM-FR01-005 | AC-FR-01-01 through AC-FR-01-05, BR-FR-01-01, BR-FR-01-02, Edge Cases, Failure Handling |
| FR-02 | Identity, RBAC, and Organization Hierarchy | TC-SMM-FR02-001, TC-SMM-FR02-002, TC-SMM-FR02-003, TC-SMM-FR02-004, TC-SMM-FR02-005, TC-SMM-FR02-006 | AC-FR-02-01 through AC-FR-02-05, BR-FR-02-01, BR-FR-02-02, Edge Cases, Failure Handling |
| FR-03 | Source Onboarding and Lawful Ingestion | TC-SMM-FR03-001 through TC-SMM-FR03-014 | AC-FR-03-01 through AC-FR-03-05, BR-FR-03-01, BR-FR-03-02, Platform-specific connectors (Reddit, YouTube, Twitter, Instagram, Facebook), Dedup index, Advisory lock, Graceful degradation, Rate limiting |
| FR-04 | Unified Monitoring Dashboard and Search Workspace | TC-SMM-FR04-001, TC-SMM-FR04-002, TC-SMM-FR04-003, TC-SMM-FR04-004, TC-SMM-FR04-005, TC-SMM-FR04-006 | AC-FR-04-01 through AC-FR-04-05, BR-FR-04-01, BR-FR-04-02, Edge Cases, Failure Handling |
| FR-05 | Keyword, Slang, Entity, OCR, and Transcript Analysis | TC-SMM-FR05-001, TC-SMM-FR05-002, TC-SMM-FR05-003, TC-SMM-FR05-004, TC-SMM-FR05-005 | AC-FR-05-01 through AC-FR-05-05, BR-FR-05-01, BR-FR-05-02, Edge Cases, Failure Handling |
| FR-06 | AI Categorization and Model Operations | TC-SMM-FR06-001, TC-SMM-FR06-002, TC-SMM-FR06-003, TC-SMM-FR06-004, TC-SMM-FR06-005, TC-SMM-FR06-006 | AC-FR-06-01 through AC-FR-06-05, BR-FR-06-01, BR-FR-06-02, Edge Cases, Failure Handling |
| FR-07 | Risk Scoring and Prioritization | TC-SMM-FR07-001 through TC-SMM-FR07-007 | AC-FR-07-01 through AC-FR-07-05, BR-FR-07-01, BR-FR-07-02, Auto-alert threshold, Priority band mapping, Edge Cases, Failure Handling |
| FR-08 | Legal and Policy Mapping | TC-SMM-FR08-001, TC-SMM-FR08-002, TC-SMM-FR08-003, TC-SMM-FR08-004, TC-SMM-FR08-005 | AC-FR-08-01 through AC-FR-08-05, BR-FR-08-01, BR-FR-08-02, Edge Cases, Failure Handling |
| FR-09 | Translation and Language Intelligence | TC-SMM-FR09-001, TC-SMM-FR09-002, TC-SMM-FR09-003, TC-SMM-FR09-004, TC-SMM-FR09-005 | AC-FR-09-01 through AC-FR-09-05, BR-FR-09-01, BR-FR-09-02, Edge Cases, Failure Handling |
| FR-10 | Alerts, Escalation, Collaboration, and SLA Management | TC-SMM-FR10-001 through TC-SMM-FR10-007 | AC-FR-10-01 through AC-FR-10-05, BR-FR-10-01, BR-FR-10-02, WF-ALT-01 SLA Timers, AUTO_DETECTED alert attribution, Edge Cases, Failure Handling |
| FR-11 | Evidence Preservation and Chain of Custody | TC-SMM-FR11-001, TC-SMM-FR11-002, TC-SMM-FR11-003, TC-SMM-FR11-004, TC-SMM-FR11-005, TC-SMM-FR11-006 | AC-FR-11-01 through AC-FR-11-05, BR-FR-11-01, BR-FR-11-02, WF-EVD-01, Edge Cases, Failure Handling |
| FR-12 | Case, Task, and Workflow Management | TC-SMM-FR12-001, TC-SMM-FR12-002, TC-SMM-FR12-003, TC-SMM-FR12-004, TC-SMM-FR12-005 | AC-FR-12-01 through AC-FR-12-05, BR-FR-12-01, BR-FR-12-02, WF-CAS-01, Edge Cases, Failure Handling |
| FR-13 | Reporting, Template Management, and MIS Analytics | TC-SMM-FR13-001, TC-SMM-FR13-002, TC-SMM-FR13-003, TC-SMM-FR13-004, TC-SMM-FR13-005 | AC-FR-13-01 through AC-FR-13-05, BR-FR-13-01, BR-FR-13-02, WF-RPT-01, Edge Cases, Failure Handling |
| FR-14 | Administration, Taxonomy, Legal Rules, and Config Governance | TC-SMM-FR14-001 through TC-SMM-FR14-007 | AC-FR-14-01 through AC-FR-14-05, BR-FR-14-01, BR-FR-14-02, Connector CRUD admin access, Connector toggle, Edge Cases, Failure Handling |
| FR-15 | Notifications, Sharing, and External Integrations | TC-SMM-FR15-001, TC-SMM-FR15-002, TC-SMM-FR15-003, TC-SMM-FR15-004, TC-SMM-FR15-005 | AC-FR-15-01 through AC-FR-15-05, BR-FR-15-01, BR-FR-15-02, Edge Cases, Failure Handling |
| FR-16 | Audit Logging, Observability, and Data Retention | TC-SMM-FR16-001, TC-SMM-FR16-002, TC-SMM-FR16-003, TC-SMM-FR16-004, TC-SMM-FR16-005 | AC-FR-16-01 through AC-FR-16-05, BR-FR-16-01, BR-FR-16-02, Edge Cases, Failure Handling |
| FR-17 | Security, Privacy, and Responsible AI Controls | TC-SMM-FR17-001, TC-SMM-FR17-002, TC-SMM-FR17-003, TC-SMM-FR17-004, TC-SMM-FR17-005 | AC-FR-17-01 through AC-FR-17-05, BR-FR-17-01, BR-FR-17-02, Edge Cases, Failure Handling |
| FR-18 | Implementation Delivery, Training, Support, Warranty | TC-SMM-FR18-001, TC-SMM-FR18-002, TC-SMM-FR18-003, TC-SMM-FR18-004 | AC-FR-18-01 through AC-FR-18-05, BR-FR-18-01, BR-FR-18-02, Edge Cases |

### Role Coverage Matrix

| Role | Role ID | Permission Level | Test Cases Covering This Role |
|---|---|---|---|
| Integration Service Account | ROL-01 | PL0 | TC-SMM-FR03-001, TC-SMM-FR03-002 |
| Leadership Read Only | ROL-02 | PL1 | TC-SMM-FR02-002, TC-SMM-FR13-003 |
| Intelligence Analyst | ROL-03 | PL2 | TC-SMM-FR02-001, TC-SMM-FR04-001, TC-SMM-FR04-003, TC-SMM-FR05-001, TC-SMM-FR05-004, TC-SMM-FR06-002, TC-SMM-FR07-003, TC-SMM-FR09-001, TC-SMM-FR10-003, TC-SMM-FR11-001 |
| Control Room Operator | ROL-04 | PL2 | TC-SMM-FR04-002, TC-SMM-FR07-002, TC-SMM-FR10-001, TC-SMM-FR10-002 |
| Investigator | ROL-05 | PL2 | TC-SMM-FR08-002, TC-SMM-FR11-004, TC-SMM-FR12-001, TC-SMM-FR12-002, TC-SMM-FR13-002 |
| Supervisor / Approver | ROL-06 | PL3 | TC-SMM-FR02-004, TC-SMM-FR10-004, TC-SMM-FR12-003, TC-SMM-FR12-004 |
| Legal Reviewer | ROL-07 | PL3 | TC-SMM-FR08-001, TC-SMM-FR08-002, TC-SMM-FR08-003, TC-SMM-FR13-001, TC-SMM-FR17-003 |
| Evidence Custodian | ROL-08 | PL3 | TC-SMM-FR11-002, TC-SMM-FR11-003, TC-SMM-FR11-006 |
| Security Auditor | ROL-09 | PL3 | TC-SMM-FR16-001, TC-SMM-FR16-002, TC-SMM-FR17-002 |
| Platform Administrator | ROL-10 | PL4 | TC-SMM-FR01-003, TC-SMM-FR02-003, TC-SMM-FR02-005, TC-SMM-FR02-006, TC-SMM-FR03-001, TC-SMM-FR03-004, TC-SMM-FR06-004, TC-SMM-FR14-001, TC-SMM-FR14-002, TC-SMM-FR14-005, TC-SMM-FR14-006, TC-SMM-FR14-007, TC-SMM-FR16-005, TC-SMM-FR17-002 |

### Platform Coverage Matrix

| Platform | Platform Code | Test Cases Referencing This Platform |
|---|---|---|
| X / Twitter | X | TC-SMM-FR03-001, TC-SMM-FR03-002, TC-SMM-FR03-003, TC-SMM-FR03-008, TC-SMM-FR04-001, TC-SMM-FR05-001, TC-SMM-FR06-001, TC-SMM-FR11-001 |
| Instagram | INSTAGRAM | TC-SMM-FR03-001, TC-SMM-FR03-004, TC-SMM-FR03-009, TC-SMM-FR04-001, TC-SMM-FR04-004, TC-SMM-FR05-002, TC-SMM-FR05-003 |
| Facebook | FACEBOOK | TC-SMM-FR03-001, TC-SMM-FR03-010, TC-SMM-FR04-001, TC-SMM-FR04-004, TC-SMM-FR05-005 |
| YouTube | YOUTUBE | TC-SMM-FR03-001, TC-SMM-FR03-007, TC-SMM-FR04-001, TC-SMM-FR04-003, TC-SMM-FR10-007, TC-SMM-FR11-005 |
| Reddit | REDDIT | TC-SMM-FR03-001, TC-SMM-FR03-005, TC-SMM-FR03-006, TC-SMM-FR03-011, TC-SMM-FR04-001, TC-SMM-FR10-007, TC-SMM-FR12-004, TC-SMM-FR14-007 |

### SLA Targets Reference

| Priority Band | Acknowledge SLA | Disposition SLA | Escalation Rule | Test Cases |
|---|---|---|---|---|
| Critical | 15 min | 2 hr | Auto-escalate to Control Room immediately; notify Supervisor at 15 min if unacknowledged | TC-SMM-FR10-001, TC-SMM-FR10-002 |
| High | 30 min | 4 hr | Escalate to Supervisor on breach | TC-SMM-FR10-005 |
| Medium | 2 hr | 1 business day | Escalate to Supervisor on breach | TC-SMM-FR07-005 |
| Low | 1 business day | 3 business days | Supervisor dashboard flag on breach | TC-SMM-FR10-006 |

---

*End of Document*
