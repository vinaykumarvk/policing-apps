# Functional Test Case Document
# AI-Powered Centralized Digital Forensic Analysis & Reporting System (Forensic AI Platform)

---

| Field | Value |
|---|---|
| **Document ID** | TC-FORENSIC-AI-FUN-001 |
| **Version** | 1.0 |
| **Date** | 2026-03-03 |
| **Project** | Forensic AI Platform -- EAGLE FORCE Telangana / TGTSL |
| **BRD Reference** | BRD-R2-FORENSIC-AI v2.0 |
| **Prepared By** | QA Engineering |
| **Status** | Draft |
| **Technology Stack** | React (Frontend), Node.js / Fastify (API), PostgreSQL (DB) |
| **Total FRs Covered** | 17 (FR-01 through FR-17) |
| **Total Test Cases** | 85 |

---

## Table of Contents

1. [Test Case Summary](#1-test-case-summary)
2. [Detailed Test Cases by Functional Requirement](#2-detailed-test-cases-by-functional-requirement)
   - [FR-01 -- Case Management & Assignment](#fr-01--case-management--assignment)
   - [FR-02 -- Evidence Intake & Registration](#fr-02--evidence-intake--registration)
   - [FR-03 -- Multi-Tool Ingestion, Parsing & Normalization](#fr-03--multi-tool-ingestion-parsing--normalization)
   - [FR-04 -- Evidence Preservation, Audit Trail & Chain of Custody](#fr-04--evidence-preservation-audit-trail--chain-of-custody)
   - [FR-05 -- Artifact Repository, Search & Review Workspace](#fr-05--artifact-repository-search--review-workspace)
   - [FR-06 -- OCR & Derived Artifact Generation](#fr-06--ocr--derived-artifact-generation)
   - [FR-07 -- AI Suspicious Content Detection & Classification](#fr-07--ai-suspicious-content-detection--classification)
   - [FR-08 -- Entity Extraction, Resolution & Link Analysis](#fr-08--entity-extraction-resolution--link-analysis)
   - [FR-09 -- Risk Scoring & Prioritization](#fr-09--risk-scoring--prioritization)
   - [FR-10 -- Legal Mapping & Statutory Reference Management](#fr-10--legal-mapping--statutory-reference-management)
   - [FR-11 -- Report Composition, Approval, Redaction & Export](#fr-11--report-composition-approval-redaction--export)
   - [FR-12 -- DOPAMS Integration & Synchronization](#fr-12--dopams-integration--synchronization)
   - [FR-13 -- Alerts, Notifications & Escalations](#fr-13--alerts-notifications--escalations)
   - [FR-14 -- Identity, RBAC & Session Security](#fr-14--identity-rbac--session-security)
   - [FR-15 -- MIS, Analytics & Operational Reporting](#fr-15--mis-analytics--operational-reporting)
   - [FR-16 -- Configuration, Template & Model Governance](#fr-16--configuration-template--model-governance)
   - [FR-17 -- Retention, Archive & Purge Approval](#fr-17--retention-archive--purge-approval)
3. [Traceability Matrix](#3-traceability-matrix)

---

## 1. Test Case Summary

| FR Ref | Functional Requirement | Test Case Count | Priority Breakdown |
|---|---|---|---|
| FR-01 | Case Management & Assignment | 5 | 2x P1, 2x P2, 1x P3 |
| FR-02 | Evidence Intake & Registration | 5 | 2x P1, 2x P2, 1x P3 |
| FR-03 | Multi-Tool Ingestion, Parsing & Normalization | 5 | 2x P1, 2x P2, 1x P3 |
| FR-04 | Evidence Preservation, Audit Trail & Chain of Custody | 5 | 2x P1, 2x P2, 1x P3 |
| FR-05 | Artifact Repository, Search & Review Workspace | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-06 | OCR & Derived Artifact Generation | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-07 | AI Suspicious Content Detection & Classification | 5 | 2x P1, 2x P2, 1x P3 |
| FR-08 | Entity Extraction, Resolution & Link Analysis | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-09 | Risk Scoring & Prioritization | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-10 | Legal Mapping & Statutory Reference Management | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-11 | Report Composition, Approval, Redaction & Export | 5 | 2x P1, 2x P2, 1x P3 |
| FR-12 | DOPAMS Integration & Synchronization | 5 | 2x P1, 2x P2, 1x P3 |
| FR-13 | Alerts, Notifications & Escalations | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-14 | Identity, RBAC & Session Security | 5 | 2x P1, 2x P2, 1x P3 |
| FR-15 | MIS, Analytics & Operational Reporting | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-16 | Configuration, Template & Model Governance | 5 | 1x P1, 2x P2, 1x P3, 1x P4 |
| FR-17 | Retention, Archive & Purge Approval | 5 | 2x P1, 2x P2, 1x P3 |
| **Total** | | **85** | **24x P1, 34x P2, 17x P3, 10x P4** |

---

## 2. Detailed Test Cases by Functional Requirement

---

### FR-01 -- Case Management & Assignment

**BRD Reference:** FR-01, AC-FR-01-01 through AC-FR-01-06, BR-FR-01-01 through BR-FR-01-03
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR01-001 | FR-01 (AC-FR-01-01, AC-FR-01-02) | **Happy Path: Create a case with all mandatory fields and verify UUID generation** | Forensic Analyst user is logged in. At least one active user exists for assignment. An investigating unit "EAGLE-FORCE-HYD" exists. | 1. Navigate to Case Management in the React UI. 2. Click "Create Case." 3. Enter case_title: "NDPS Investigation -- Suspect Alpha", case_reference: "EF-HYD-2026-001", investigating_unit: "EAGLE-FORCE-HYD", case_priority: "High", assigned_owner_user_id: (select active analyst), case_source: "Manual". 4. Submit `POST /api/v1/cases` with Idempotency-Key header. 5. Verify the response. | The API returns HTTP 201 with a system-generated non-sequential UUID for case_id, status = "Draft", and created_at timestamp. The case appears in the React case list view. The case_reference "EF-HYD-2026-001" is unique within investigating_unit + case_year. An audit event with action = "create" is logged. | P1 |
| TC-FOR-FR01-002 | FR-01 (AC-FR-01-02, AC-FR-01-03) | **Negative: Attempt Draft-to-Active transition with missing mandatory fields** | A case exists in Draft status with case_title and case_reference populated but assigned_owner_user_id is NULL. | 1. Call `PATCH /api/v1/cases/{caseId}` with body `{ "status": "Active" }`. 2. Observe the API response. | The API returns HTTP 422 with error body containing the list of missing fields: `["assigned_owner_user_id"]`. The case remains in Draft status. No status_change audit event is created. | P1 |
| TC-FOR-FR01-003 | FR-01 (AC-FR-01-04, BR-FR-01-02) | **Role-Based: Analyst attempts to close a case** | A case exists in Submitted status. The logged-in user has the Forensic Analyst role (PL-3). | 1. Log in as a Forensic Analyst. 2. Navigate to the case detail page in the React UI. 3. Attempt to change status to "Closed" via the status dropdown. 4. Alternatively call `PATCH /api/v1/cases/{caseId}` with body `{ "status": "Closed", "closed_reason": "Investigation complete" }`. | The React UI does not render the "Closed" option in the status dropdown for Analyst role. The API returns HTTP 403 with error_code "ACCESS_DENIED" and a correlation_id. The case status remains unchanged. | P2 |
| TC-FOR-FR01-004 | FR-01 (AC-FR-01-05, FH-FR-01-01) | **Failure Handling: Create case linked to invalid DOPAMS reference** | Forensic Analyst is logged in. DOPAMS integration is configured but the reference "DOPAMS-INVALID-999" does not exist in DOPAMS. | 1. Create a new case via `POST /api/v1/cases` with dopams_case_ref: "DOPAMS-INVALID-999" and all other mandatory fields valid. 2. Verify the response and case record. | The API returns HTTP 201. The case is saved in Draft status with validation_status = "PendingExternalValidation". The case_id is generated. A validation warning is visible in the React case detail UI. The case is usable despite the unvalidated DOPAMS reference. | P2 |
| TC-FOR-FR01-005 | FR-01 (EC-FR-01-02, BR-FR-01-03) | **Edge Case: Reassign case when original owner becomes inactive** | A case "EF-HYD-2026-002" exists in Active status assigned to User-A. User-A is subsequently set to status = "Inactive" by an Administrator. | 1. Administrator deactivates User-A via user management. 2. Attempt any operation on the case (e.g., upload evidence). 3. Observe system behavior. 4. Supervisor navigates to the case and reassigns to active User-B. 5. Verify the case audit trail. | The system prevents the case from progressing (evidence upload returns validation error "CASE_OWNER_INACTIVE" or similar). After reassignment, the case audit trail records an "assign" action with the old and new owner_user_ids. The case resumes normal operations. | P3 |

---

### FR-02 -- Evidence Intake & Registration

**BRD Reference:** FR-02, AC-FR-02-01 through AC-FR-02-06, BR-FR-02-01 through BR-FR-02-03
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR02-001 | FR-02 (AC-FR-02-01, AC-FR-02-05) | **Happy Path: Upload UFED evidence package with full metadata** | A case exists in Active status. Analyst is logged in. A valid UFED export ZIP file (45 MB, SHA-256 checksum pre-computed: "a1b2c3d4...") is ready for upload. | 1. Navigate to the case detail page. 2. Click "Upload Evidence." 3. Select source_tool_code: "UFED", enter source_device_ref: "Samsung-Galaxy-S21-SN12345". 4. Select the ZIP file. 5. Submit via `POST /api/v1/cases/{caseId}/evidence/uploads` with Idempotency-Key and checksum_value: "a1b2c3d4...", checksum_type: "SHA256". 6. Monitor upload progress in the React UI. | The API returns HTTP 201 with evidence_id (UUID), upload_session_id, upload_url, and expires_at. The React UI displays upload progress bar. After upload completes, evidence_status = "UploadAccepted". The evidence record stores source_tool = "UFED", source_file_name, source_device_ref, received_at, received_by (analyst user_id), checksum_type = "SHA256", checksum_value, and size_bytes = 47185920. An import_job_id is created and visible in the UI. | P1 |
| TC-FOR-FR02-002 | FR-02 (AC-FR-02-04) | **Negative: Upload with checksum mismatch triggers quarantine** | A case exists in Active status. A UFED export file is ready. The caller supplies an incorrect checksum_value "bad_checksum_000". | 1. Call `POST /api/v1/cases/{caseId}/evidence/uploads` with checksum_value: "bad_checksum_000", checksum_type: "SHA256". 2. Complete the file upload to the upload_url. 3. System computes the actual checksum after upload. 4. Observe the evidence record status. | The evidence record transitions to evidence_status = "Quarantined". Downstream parsing/normalization is blocked (no import job is queued). The React UI shows a quarantine warning badge on the evidence item. An audit event records the checksum mismatch with outcome = "Failure". | P1 |
| TC-FOR-FR02-003 | FR-02 (AC-FR-02-03) | **Idempotency: Retry upload with same Idempotency-Key returns original evidence record** | A case exists in Active status. An evidence upload was already successfully registered with Idempotency-Key: "idem-key-upload-001". | 1. Call `POST /api/v1/cases/{caseId}/evidence/uploads` again with the same Idempotency-Key: "idem-key-upload-001" and identical metadata (same file_name, size_bytes, source_tool_code, checksum_value). 2. Observe the response. | The API returns HTTP 200 (not 201) with the same evidence_id and upload_session_id as the original request. No duplicate evidence record is created in the database. | P2 |
| TC-FOR-FR02-004 | FR-02 (AC-FR-02-06, FH-FR-02-02) | **Edge Case: Duplicate package detection for same case** | A case exists with one completed evidence upload having checksum_value: "abc123def456" and size_bytes: 52428800. | 1. Attempt to upload a second evidence package to the same case with identical checksum_value: "abc123def456" and size_bytes: 52428800. 2. Observe the React UI response. 3. As the analyst, enter reason_code: "REPROCESSING_REQUIRED" and confirm the duplicate. 4. A supervisor approves the duplicate retention. | The system detects the duplicate (matching checksum_value + size_bytes) and displays a confirmation dialog in the React UI. The dialog requires the analyst to enter a reason_code. After analyst confirmation and supervisor approval, the duplicate evidence record is created with duplicate_of_evidence_id pointing to the original. The decision is recorded in the audit trail. | P2 |
| TC-FOR-FR02-005 | FR-02 (EC-FR-02-01, FH-FR-02-01) | **Failure Handling: Upload interrupted after metadata save but before object-storage commit** | A case exists in Active status. Object storage is configured to simulate a write failure (test environment circuit-breaker or mock). | 1. Call `POST /api/v1/cases/{caseId}/evidence/uploads` with valid metadata. The API creates the evidence metadata record. 2. Begin file upload to the upload_url. 3. Simulate object storage failure mid-upload (connection drop or storage write error). 4. Check the evidence record status. | The evidence record remains in evidence_status = "Registered" with validation_error_code = "OBJECT_STORE_WRITE_FAILED". The original uploaded file (if partially written) does not corrupt the immutable storage. The React UI shows a retry option for the upload. No import job is queued. | P3 |

---

### FR-03 -- Multi-Tool Ingestion, Parsing & Normalization

**BRD Reference:** FR-03, AC-FR-03-01 through AC-FR-03-06, BR-FR-03-01 through BR-FR-03-03
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR03-001 | FR-03 (AC-FR-03-01, AC-FR-03-03, AC-FR-03-04) | **Happy Path: Parse UFED XML export and verify canonical artifact creation** | A case exists with a completed evidence upload (UFED XML export containing 500 messages, 120 call logs, 45 contacts, and 20 images). Import job is queued. | 1. Trigger ingestion via `POST /api/v1/cases/{caseId}/evidence/{evidenceId}/ingest` with reprocess: false, ocr_enabled: true. 2. Poll `GET /api/v1/import-jobs/{importJobId}` until status is terminal. 3. Query artifacts via `POST /api/v1/cases/{caseId}/artifacts/search` filtering by each artifact_type. | The import job transitions: Queued -> Parsing -> Normalized -> Completed. Artifacts are created with types: Message (500), CallLog (120), Contact (45), MediaImage (20). Each artifact record contains source_tool = "UFED", source_file_path, source_record_id, parser_version (semantic version string), import_job_id, artifact_type, and normalization_timestamp. The job summary_json shows artifacts_created = 685. | P1 |
| TC-FOR-FR03-002 | FR-03 (AC-FR-03-01, AC-FR-03-02) | **Happy Path: Parse exports from all six supported forensic tools** | Six evidence packages are prepared, one each from UFED (XML), XRY (XML), Oxygen (JSON), FTK (CSV), Magnet AXIOM (JSON), and Belkasoft (XML). Each package contains at least 10 artifacts of mixed types. | 1. For each tool, upload the evidence package to a dedicated case. 2. Trigger ingestion for each with appropriate parser_profile. 3. Verify each import job completes successfully. 4. Verify artifacts are created with the correct source_tool_code for each. | All six import jobs reach status = "Completed" or "CompletedWithWarnings". Each job produces normalized artifacts. The parser_version recorded for each reflects the correct tool-specific adapter version. Input containers (CSV, JSON, XML) are all handled correctly. | P1 |
| TC-FOR-FR03-003 | FR-03 (FH-FR-03-01, AC-FR-03-05) | **Failure Handling: Mixed package with corrupt HTML but valid CSV components** | An evidence package ZIP contains 3 valid CSV files and 1 corrupt HTML file. The evidence is uploaded and the import job is queued. | 1. Trigger ingestion. 2. Poll job status. 3. Review the job_summary_json. | The import job completes with status = "CompletedWithWarnings". Artifacts from the 3 valid CSV files are created successfully. The corrupt HTML file is recorded in job_summary_json with error details. warning_count >= 1, error_count >= 1. The evidence_status is "CompletedWithWarnings". | P2 |
| TC-FOR-FR03-004 | FR-03 (AC-FR-03-06) | **Edge Case: Reprocessing creates new import_job version without overwriting prior artifacts** | A case has an evidence source with a completed import job (job_version_no = 1) that created 200 artifacts. A new parser version is now available. | 1. Call `POST /api/v1/cases/{caseId}/evidence/{evidenceId}/ingest` with reprocess: true. 2. Verify a new import_job_id is created with job_version_no = 2. 3. After completion, verify artifacts. 4. Verify the original import job and its artifacts are still accessible. | A new import job is created with job_version_no = 2 and the updated parser_version. The original job_version_no = 1 artifacts remain intact and queryable. New artifacts from version 2 have distinct artifact_ids and reference import_job_id of the new job. Version lineage is visible in the evidence detail view. | P2 |
| TC-FOR-FR03-005 | FR-03 (BR-FR-03-03, EC-FR-03-02) | **Edge Case: Artifacts without timestamps default to UTC with timezone_unknown flag** | A Belkasoft export package contains 50 call log records where 30 have timestamps with timezone info and 20 have timestamps without timezone info. | 1. Upload and ingest the Belkasoft package. 2. Query the resulting CallLog artifacts. 3. Inspect the event_timestamp and metadata_json for the 20 timezone-less records. | For the 30 records with timezone info, event_timestamp preserves the original timezone. For the 20 records without timezone, event_timestamp is stored in UTC and metadata_json includes `"timezone_unknown": true`. No artifacts are dropped due to missing timestamps. | P3 |

---

### FR-04 -- Evidence Preservation, Audit Trail & Chain of Custody

**BRD Reference:** FR-04, AC-FR-04-01 through AC-FR-04-05, BR-FR-04-01 through BR-FR-04-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR04-001 | FR-04 (AC-FR-04-01, AC-FR-04-02) | **Happy Path: Verify audit events are created for all critical actions in a case lifecycle** | A case exists. An Analyst, Supervisor, and Auditor user are available. Evidence has been uploaded and parsed. | 1. As Analyst: create case, upload evidence, view artifacts, add annotation, generate report. 2. As Supervisor: approve report, close case. 3. As Auditor: query `GET /api/v1/audit-events?case_id={caseId}`. 4. Verify each audit event record. | Audit events exist for actions: create (case), import (evidence), view (artifact), annotation (add), report_generation, report_approval, and status_change (close). Each event contains audit_event_id (UUID), timestamp_utc, actor_user_id, actor_role_id, case_id, object_type, object_id, action, source_ip, and outcome = "Success". Events are ordered by timestamp_utc. | P1 |
| TC-FOR-FR04-002 | FR-04 (AC-FR-04-03) | **Happy Path: Export chain-of-custody log in PDF and CSV formats** | A case has a complete lifecycle: evidence uploaded, parsed, findings reviewed, report published. Multiple audit events exist spanning at least 3 actors. | 1. As Supervisor, navigate to the case detail page. 2. Click "Export Chain of Custody" in the React UI. 3. Select format: PDF. Download and verify. 4. Repeat selecting format: CSV. Download and verify. | Both PDF and CSV files are generated and downloaded. The PDF contains a formatted chain-of-custody table with timestamp, actor, action, object, and outcome columns. The CSV contains the same data in machine-readable format. Both files include all audit events for the case in chronological order. An audit event for the export action itself is recorded. | P1 |
| TC-FOR-FR04-003 | FR-04 (BR-FR-04-02) | **Role-Based: Analyst attempts to access audit viewer** | An Analyst user (PL-3) is logged in. Audit events exist for multiple cases. | 1. As Analyst, attempt to navigate to the audit viewer page in the React UI. 2. Alternatively, call `GET /api/v1/audit-events`. | The React UI does not render the audit viewer navigation link for the Analyst role. The API returns HTTP 403 with error_code "ACCESS_DENIED". Only Auditor, Supervisor, and Administrator roles can access the audit viewer. Only Supervisor and Administrator can export audit logs. | P2 |
| TC-FOR-FR04-004 | FR-04 (FH-FR-04-01) | **Failure Handling: Audit write failure blocks the triggering transactional action** | The audit event table/service is temporarily unavailable (simulated via test environment configuration). An Analyst is logged in. | 1. Simulate audit service unavailability. 2. As Analyst, attempt to upload evidence to a case (a transactional write action). 3. Observe the API response. 4. Restore audit service. 5. Retry the upload. | While audit service is unavailable, the evidence upload API returns HTTP 500 with an error indicating the audit subsystem failure. No evidence record is created (the transaction is rolled back). After audit service is restored, the retry succeeds and both the evidence record and audit event are created atomically. | P2 |
| TC-FOR-FR04-005 | FR-04 (AC-FR-04-05, AC-FR-04-04) | **Edge Case: Filter audit events by multiple criteria and verify version lineage for reprocessed evidence** | A case has evidence that was processed twice (job_version_no 1 and 2). Multiple actors performed actions. Audit events span 7 days. | 1. As Auditor, navigate to audit viewer. 2. Filter by case_id, action = "import", date_range = last 7 days. 3. Verify both import events (version 1 and version 2) appear. 4. Filter by actor = Analyst-A and object_type = "EvidenceSource". 5. Navigate to evidence detail and verify version lineage. | Filtered audit results show exactly the matching events. Both import job versions appear with distinct import_job_ids. The evidence detail view shows version lineage: version 1 (original) and version 2 (reprocessing), each with their respective parser_version, timestamp, and artifact counts. Prior processing versions are preserved and accessible. | P3 |

---

### FR-05 -- Artifact Repository, Search & Review Workspace

**BRD Reference:** FR-05, AC-FR-05-01 through AC-FR-05-06, BR-FR-05-01 through BR-FR-05-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR05-001 | FR-05 (AC-FR-05-01, AC-FR-05-02) | **Happy Path: Case dashboard displays correct counts and search with faceted filters returns paginated results** | A case has completed ingestion with 1,500 artifacts: 800 Messages, 300 CallLogs, 200 Contacts, 100 MediaImages, 50 Documents, 50 BrowsingHistory. 45 findings with mixed severities. 5 import jobs completed. | 1. Navigate to the case dashboard in the React UI. 2. Verify artifact_type counts, findings by severity, pending reviews, import status, and latest activity. 3. Open the search workspace. 4. Apply filters: artifact_type = "Message", risk_band = ["High", "Critical"], review_status = "Unreviewed", source_tool = "UFED". 5. Sort by risk_score descending. 6. Verify pagination (page_size = 50). | The dashboard displays: Messages: 800, CallLogs: 300, Contacts: 200, MediaImages: 100, Documents: 50, BrowsingHistory: 50. Findings by severity are shown. Search returns only Message artifacts matching all filter criteria. Results are sorted by risk_score descending. Pagination shows page 1 of N, with total_count reflecting the filtered set. Each result includes lineage fields (artifact_id, evidence_id, source_tool, source_file_path). | P1 |
| TC-FOR-FR05-002 | FR-05 (AC-FR-05-03, AC-FR-05-04) | **Happy Path: Navigate between table, timeline, graph, gallery, and finding-detail views** | A case has diverse artifacts including messages with threads, call logs with timeline data, media images, and linked entities forming a relationship graph. | 1. In the React search workspace, switch to Table view. Verify columns and sorting. 2. Switch to Timeline view. Verify chronological ordering. 3. Switch to Graph view. Verify entity nodes and relationship edges. 4. Switch to Gallery view. Verify image thumbnails with object-fit rendering. 5. Click on a finding. Verify the finding-detail view shows source context (surrounding message thread). | All five view modes render correctly. Table view shows artifact metadata columns and is sortable. Timeline view plots artifacts chronologically. Graph view renders entity nodes with relationship edges. Gallery view shows image thumbnails. Finding-detail view opens with the surrounding message thread context (previous and next messages in the same conversation). | P2 |
| TC-FOR-FR05-003 | FR-05 (AC-FR-05-05, BR-FR-05-02) | **Happy Path: Add annotations with different visibility scopes and verify they do not modify artifacts** | An Analyst and a Supervisor are assigned to the same case. A specific Message artifact exists. | 1. As Analyst, select the Message artifact. Add annotation with text: "Suspect communication pattern -- follow up on entity +91-9876543210" and visibility_scope = "CaseTeam". 2. Add a second annotation with visibility_scope = "Private". 3. As Supervisor, view the same artifact. Verify annotations visible. 4. As a different Analyst not on the case team, attempt to view annotations. 5. Verify the artifact's canonical_text and metadata_json are unchanged. | CaseTeam annotation is visible to both the original Analyst and the Supervisor. Private annotation is visible only to the creating Analyst. The Supervisor does not see the Private annotation. An Analyst outside the case team sees neither annotation. The underlying artifact canonical_text and metadata_json remain unmodified. Annotations have separate annotation_ids and author_user_ids. | P2 |
| TC-FOR-FR05-004 | FR-05 (FH-FR-05-01, EC-FR-05-01) | **Edge Case: Preview rendering fails for metadata-only artifact** | An artifact of type "Unknown" exists with metadata_json populated but no canonical_text, no media file, and no previewable content. | 1. In the React search workspace, click on the Unknown artifact. 2. The preview pane attempts to render. 3. Observe fallback behavior. | The React UI displays a metadata-only fallback showing the artifact's metadata_json fields in a structured key-value layout. A "Download Original" link is available (if the user has evidence_export permission). No error modal is shown. The UI does not crash or display a blank panel. | P3 |
| TC-FOR-FR05-005 | FR-05 (EC-FR-05-03, FH-FR-05-02) | **Edge Case: Graph rendering exceeds client threshold and search index unavailability** | A case has 5,000+ extracted entities with dense relationship edges exceeding the default graph render threshold (e.g., >2,000 nodes). | 1. Navigate to the Graph view for this case. 2. Observe behavior when node count exceeds threshold. 3. Separately, simulate search index unavailability and attempt a search. | For the graph: the React UI prompts the user to apply additional filters (e.g., filter by entity_type or date_range) and renders a summarized cluster view instead of individual nodes. The cluster view groups related entities. For search index unavailability: the UI displays an index_status indicator (e.g., "Search index temporarily unavailable") with a retry button. No data is lost. | P4 |

---

### FR-06 -- OCR & Derived Artifact Generation

**BRD Reference:** FR-06, AC-FR-06-01 through AC-FR-06-05, BR-FR-06-01 through BR-FR-06-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR06-001 | FR-06 (AC-FR-06-01, AC-FR-06-02, AC-FR-06-05) | **Happy Path: OCR processes screenshots and identity documents, creating DerivedText artifacts with multilingual support** | A case has ingested evidence containing: 5 screenshot images (English text), 3 identity document scans (Hindi text), and 2 PDF documents (Telugu text). OCR is enabled. | 1. Trigger ingestion with ocr_enabled: true. 2. After import completes, query artifacts of type DerivedText. 3. Inspect each DerivedText artifact's metadata. | 10 DerivedText artifacts are created (one per successfully processed source). Each stores: derived_artifact_id, source_artifact_id (linking to the original MediaImage/Document), extraction_engine, extraction_version, language_hint (en/hi/te as detected), extraction_confidence (numeric 0-1), and extracted_text. Unicode text is properly stored for Hindi and Telugu content. The language_hint reflects the detected language. | P1 |
| TC-FOR-FR06-002 | FR-06 (AC-FR-06-03, BR-FR-06-01, BR-FR-06-02) | **Happy Path: Verify source-to-derived relationship visibility and derived=true marking** | OCR has been completed for a MediaImage artifact. A DerivedText artifact exists linked to it. | 1. In the React artifact detail view, open the source MediaImage artifact. Verify the "Derived Artifacts" section shows the linked DerivedText. 2. Open the DerivedText artifact. Verify the "Source Artifact" section shows the original MediaImage. 3. Navigate to report assembly. Verify the DerivedText is available for inclusion. 4. Check the DerivedText artifact metadata for derived=true flag. | The artifact detail view shows bidirectional navigation between source and derived artifacts. The DerivedText artifact inherits case_id and evidence_id from the source. In report assembly, the DerivedText is selectable. The DerivedText artifact is clearly marked as derived=true in both the UI (visual badge "Derived") and API response metadata. It is not treated as original evidence. | P2 |
| TC-FOR-FR06-003 | FR-06 (AC-FR-06-04) | **Failure Handling: OCR failure on one artifact does not block import completion** | An evidence package contains 10 images: 9 are processable and 1 is a corrupt/unreadable image file. OCR is enabled. | 1. Trigger ingestion with ocr_enabled: true. 2. Monitor import job progress. 3. After completion, verify artifacts. | The import job completes with status = "CompletedWithWarnings". 9 DerivedText artifacts are created for the successful images. The corrupt image is recorded in the job_summary_json with extraction_status = "Failed" and an error reason. The warning_count >= 1. All other artifacts (messages, call logs, etc.) from the same package are unaffected. | P2 |
| TC-FOR-FR06-004 | FR-06 (FH-FR-06-01) | **Failure Handling: OCR service unavailable queues deferred retry** | OCR service is temporarily down (simulated). An evidence package with 5 images is uploaded and ingestion is triggered with ocr_enabled: true. | 1. Trigger ingestion while OCR service is unavailable. 2. Verify the import job completes for non-OCR artifacts. 3. Check the OCR extraction status for image artifacts. 4. Restore OCR service. 5. Verify deferred OCR retry executes. | The import job completes for text-based artifacts (messages, call logs). Image artifacts are marked with extraction_status = "Deferred". After OCR service is restored, a retry mechanism picks up deferred items and creates the DerivedText artifacts. The final artifact count includes the deferred-then-completed OCR outputs. | P3 |
| TC-FOR-FR06-005 | FR-06 (EC-FR-06-01, FH-FR-06-02) | **Edge Case: Low-quality image produces low-confidence OCR and empty OCR output** | An evidence package contains: 1 blurry/low-quality photograph and 1 blank/empty page scan. OCR is enabled. | 1. Trigger ingestion with ocr_enabled: true. 2. After completion, inspect the DerivedText artifacts for these two sources. | For the low-quality image: a DerivedText is created with extraction_confidence < 0.3 (low). The extracted_text may contain partial/garbled content. The UI displays a low-confidence indicator. For the blank page: the system stores extraction_status = "NoTextDetected" rather than failing. No DerivedText artifact is created for the blank page, but the source artifact is not flagged as failed. | P4 |

---

### FR-07 -- AI Suspicious Content Detection & Classification

**BRD Reference:** FR-07, AC-FR-07-01 through AC-FR-07-06, BR-FR-07-01 through BR-FR-07-03
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR07-001 | FR-07 (AC-FR-07-01, AC-FR-07-02, AC-FR-07-03, AC-FR-07-06) | **Happy Path: AI analysis detects narcotics-related content and generates findings with full metadata** | A case has ingested artifacts including Message artifacts containing known narcotics-related keywords (e.g., "maal", "stuff delivery", "50 grams"), a FinancialIndicator artifact with suspicious UPI transactions, and a DerivedText from an IdentityDocument scan. Keyword dictionaries and ML model are published. | 1. Wait for AI analysis pipeline to complete after ingestion (or trigger manually). 2. Query findings via `GET /api/v1/cases/{caseId}/findings`. 3. Inspect finding records. | Findings are generated for: NarcoticsReference (from messages with keywords), FinancialRouting (from UPI transactions), IdentityDocument (from ID scan). Each finding contains: finding_id, artifact_id, category_code, severity, confidence_score (0.0-1.0), reason_codes_json (array of codes and explanations), rule_hits_json (matched keywords/regex), model_version, and finding_status = "Unreviewed". Each finding records whether it was rule_only, model_only, or hybrid. | P1 |
| TC-FOR-FR07-002 | FR-07 (AC-FR-07-04, BR-FR-07-01) | **Happy Path: Analyst reviews findings and sets statuses including FalsePositive with mandatory comment** | Multiple findings exist in Unreviewed status. An Analyst is logged in. | 1. Navigate to the findings review queue in the React UI. 2. Select a NarcoticsReference finding. Set finding_status to "Confirmed" via `PATCH /api/v1/findings/{findingId}/review`. 3. Select another finding. Set to "FalsePositive" with comment: "Generic grocery list, not narcotics reference." 4. Select a third finding. Set to "Escalated" with comment: "Requires supervisor review -- high-value financial pattern." 5. Attempt to set a finding to "FalsePositive" without a comment. | Confirmed finding: status updated, reviewed_by and reviewed_at populated. FalsePositive finding: status updated with comment stored. Escalated finding: status updated, supervisor is notified. When attempting FalsePositive without comment: API returns HTTP 422 with error "COMMENT_REQUIRED". The finding remains in its prior status. | P1 |
| TC-FOR-FR07-003 | FR-07 (AC-FR-07-05, BR-FR-07-03) | **Happy Path: Configurable keyword dictionaries and regex patterns drive rule-based detection** | A Configuration Manager has published a keyword dictionary v2.1 containing narcotics slang terms and a regex pattern library for phone number formats. | 1. Upload evidence containing messages with slang terms matching the published dictionary (e.g., "chitta", "maal", "gaanja"). 2. Trigger ingestion and AI analysis. 3. Verify findings reference the specific dictionary version and matched rules. 4. Verify confidence thresholds are read from configuration (not hardcoded). | Findings are created with rule_hits_json containing matched keyword/regex entries from dictionary v2.1. The model_version or config_version_id is recorded. Confidence thresholds for escalation match the published configuration values, not hardcoded constants. Changing the dictionary version and re-analyzing produces findings referencing the new version. | P2 |
| TC-FOR-FR07-004 | FR-07 (FH-FR-07-01, FH-FR-07-02) | **Failure Handling: ML model inference fails but rule-based analysis still executes** | The ML model service is unavailable (simulated). Evidence with text artifacts is ready for analysis. Rule-based analysis (keyword/regex) is configured and operational. | 1. Trigger AI analysis while ML model service is down. 2. Verify findings are still generated. 3. Check analysis_status for affected artifacts. | Rule-based findings are generated normally (category-specific keyword and regex matches). Each finding from this run is recorded as analysis_type = "rule_only". The artifact-level analysis_status = "PartialSuccess". If a specific artifact_type has no configured model (e.g., Audio), the system logs fallback_reason = "NO_MODEL_MAPPING" and skips model inference for that type without error. | P2 |
| TC-FOR-FR07-005 | FR-07 (EC-FR-07-01, EC-FR-07-03) | **Edge Case: Artifact matches multiple suspicious categories and multilingual slang produces ambiguous classification** | A Message artifact contains text mixing Hindi transliterated narcotics slang ("maal ready hai 50g, paisa UPI se bhej") that matches both NarcoticsReference and FinancialRouting categories. | 1. Ingest and analyze the artifact. 2. Query findings for the artifact. 3. Verify multiple findings are created. 4. Check confidence_score for each finding. | Multiple findings are created for the same artifact_id, one per matching category_code (NarcoticsReference and FinancialRouting). Each has its own finding_id, confidence_score, and reason_codes. The transliterated Hindi content is processed (language detection handles mixed-script text). Ambiguous classifications result in finding_status = "NeedsReview" if confidence_score falls below the configured auto-confirm threshold. | P3 |

---

### FR-08 -- Entity Extraction, Resolution & Link Analysis

**BRD Reference:** FR-08, AC-FR-08-01 through AC-FR-08-05, BR-FR-08-01 through BR-FR-08-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR08-001 | FR-08 (AC-FR-08-01, AC-FR-08-02) | **Happy Path: Extract multiple entity types and create relationship edges from ingested artifacts** | A case has ingested 500 Message artifacts and 100 CallLog artifacts. Messages contain phone numbers (+91-9876543210, +91-8765432109), email addresses (suspect@example.com), UPI IDs (suspect@upi), and social handles (@suspect_handle). Call logs reference the same phone numbers. | 1. After ingestion and entity extraction pipeline completes, query entities via the API. 2. Query relationships. 3. Verify entity types and relationship edges. | Entities are extracted with types: PhoneNumber (+91-9876543210, +91-8765432109), Email (suspect@example.com), UPI (suspect@upi), SocialHandle (@suspect_handle). Relationship edges are created with: relationship_id, from_entity_id, to_entity_id, relationship_type (e.g., "CommunicatedWith"), source_artifact_id, weight, first_seen_at, and last_seen_at. Phone number entities appearing in both Messages and CallLogs have edges from both source artifact types. | P1 |
| TC-FOR-FR08-002 | FR-08 (AC-FR-08-03, AC-FR-08-05) | **Happy Path: Timeline and graph/network views render linked entities with artifact references** | Entity extraction is complete. Multiple entities exist with relationship edges. | 1. In the React UI, navigate to the Link Analysis workspace. 2. Switch to Graph/Network view. Verify entity nodes are rendered with connecting edges. 3. Click on an edge. Verify the originating artifact reference is shown. 4. Switch to Timeline view. Verify entities are plotted chronologically based on first_seen_at and last_seen_at. | Graph view renders entity nodes (color-coded by entity_type) connected by edges (labeled with relationship_type). Clicking an edge shows a panel with the source_artifact_id(s) and a link to the artifact detail. Timeline view plots entity interactions chronologically. All relationships displayed preserve their originating artifact reference(s). | P2 |
| TC-FOR-FR08-003 | FR-08 (AC-FR-08-04, BR-FR-08-01) | **Happy Path: Merge two entities with reason_code and verify audit event; then split them** | Two PhoneNumber entities exist: Entity-A ("+91-9876543210") and Entity-B ("+919876543210") that represent the same number with different formatting. Analyst is logged in. | 1. Call `POST /api/v1/entities/merge` with entity_ids: [Entity-A-id, Entity-B-id], reason_code: "FORMAT_VARIANT", reason_comment: "Same number, different formatting". 2. Verify the merge result. 3. Verify the audit event. 4. Later, call the split API to reverse the merge with a new reason. 5. Verify the split result and audit event. | Merge: API returns surviving_entity_id with merged_entity_ids. The surviving entity has alias_list_json updated to include both format variants. manual_resolution_state = "Merged". An audit event records the merge action with actor, reason_code, and both entity IDs. Split: The entities are separated back into two distinct records. manual_resolution_state for the split entities = "Split". A new audit event records the split action. | P2 |
| TC-FOR-FR08-004 | FR-08 (FH-FR-08-01) | **Edge Case: Entity resolution conflict triggers manual resolution requirement** | Automated entity resolution identifies two entities (a PersonName "Ravi Kumar" and a PersonName "R. Kumar") as potentially the same person with conflicting confidence signals (one source links them, another source shows them as distinct). | 1. After entity extraction, check the entity resolution status for these two entities. 2. Verify the system does not auto-merge. 3. Verify the Analyst's review queue includes these entities. | The system sets review_status = "NeedsManualResolution" for the conflicting entities. resolution_confidence stores the automated value separately from any analyst-confirmed value. No auto-merge occurs. The Analyst's entity review queue shows these entities flagged for manual resolution. | P3 |
| TC-FOR-FR08-005 | FR-08 (EC-FR-08-01, FH-FR-08-02) | **Edge Case: Dense relationship graph with cyclic edges exceeding render threshold** | A case has 3,000 entities with 15,000+ relationship edges including cyclic relationships (A -> B -> C -> A). | 1. Navigate to the Graph view. 2. Observe rendering behavior when the number of nodes/edges exceeds the client threshold. 3. Apply a filter (e.g., entity_type = "PhoneNumber"). | The React UI detects the threshold breach and prompts the user: "Graph contains [N] nodes. Apply filters to refine." A summarized cluster view is rendered, grouping densely connected entities into clusters. After applying the PhoneNumber filter, only phone number entities and their edges are rendered, within the manageable threshold. Cyclic relationships do not cause infinite rendering loops. | P4 |

---

### FR-09 -- Risk Scoring & Prioritization

**BRD Reference:** FR-09, AC-FR-09-01 through AC-FR-09-05, BR-FR-09-01 through BR-FR-09-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR09-001 | FR-09 (AC-FR-09-01, AC-FR-09-02, AC-FR-09-03) | **Happy Path: Risk scores are computed for artifacts, entities, and cases with transparent components** | A case has 100 artifacts with varying numbers of keyword hits, entity connections, and finding severities. Risk rule configuration v1.0 is published with defined weights for keyword_hits, entity_density, repetition_count, document_flags, and rule_weight. | 1. After AI analysis completes, query risk scores via the API for scope_type = "Artifact". 2. Query risk scores for scope_type = "Entity". 3. Query risk scores for scope_type = "Case". 4. Inspect components_json for a Critical-band artifact. | Risk scores are computed for all three scope types. Each score has: risk_score_id, scope_type, scope_id, score_value (0-100), risk_band (Low/Medium/High/Critical), components_json, and score_version. The components_json for a sample Critical artifact shows individual weights: `{"keyword_hits": 15, "entity_density": 8, "repetition_count": 3, "document_flags": 2, "analyst_confirmations": 0, "rule_weight": {"NarcoticsReference": 25}}`. The risk_band is correctly derived from score_value thresholds. | P1 |
| TC-FOR-FR09-002 | FR-09 (AC-FR-09-04, BR-FR-09-02) | **Happy Path: Supervisor overrides a computed risk score with reason and both values are retained** | An artifact has a computed risk_score = 45 (Medium band). A Supervisor is logged in. | 1. As Supervisor, navigate to the artifact in the React UI. 2. Click "Override Risk Score." 3. Enter new score_value: 85, override_reason: "Intelligence from external source confirms high-value target", override_comment: "Cross-referenced with HUMINT report ref HR-2026-045." 4. Submit the override. 5. Query the risk score record. | The risk score record now shows: score_value = 85, risk_band = "Critical", override_by = Supervisor's user_id, override_reason = (as entered), override_comment = (as entered). The original computed score (45, Medium) is retained in the record (e.g., in a computed_score_value field or as a separate historical entry). An audit event records the override action. The artifact now appears in Critical-priority queues and dashboards. | P2 |
| TC-FOR-FR09-003 | FR-09 (AC-FR-09-05) | **Happy Path: Search results, dashboards, and alert queues are sortable and filterable by risk_score and risk_band** | A case has artifacts with all four risk bands: 10 Critical, 30 High, 40 Medium, 20 Low. | 1. In the React search workspace, apply filter: risk_band = ["Critical", "High"]. Verify result count = 40. 2. Sort by risk_score descending. Verify ordering. 3. On the case dashboard, verify the risk distribution chart shows the correct breakdown. 4. Check the alert queue for Critical-risk-triggered alerts. | Search filter returns exactly 40 artifacts (10 Critical + 30 High). Sorting by risk_score descending places the highest-scoring artifact first. Dashboard risk distribution chart shows the 10/30/40/20 breakdown. Alert queue contains alerts triggered by Critical-risk artifacts. | P2 |
| TC-FOR-FR09-004 | FR-09 (EC-FR-09-02, FH-FR-09-01) | **Edge Case: Score recalculation after analyst marks a finding FalsePositive; and score calculation failure handling** | An artifact has score_value = 75 (High) driven by 3 findings. The analyst marks one finding as FalsePositive. Separately, a second artifact has a risk rule set that is missing/corrupt. | 1. Mark one finding on the first artifact as FalsePositive. 2. Trigger score recalculation. 3. Verify the updated score. 4. For the second artifact, trigger score calculation with missing rule set. | First artifact: score recalculates to a lower value (e.g., 55, Medium) reflecting the removal of the FalsePositive finding's contribution. The components_json is updated. Both old and new scores are preserved in history. Second artifact: the system marks risk_status = "Unavailable". The artifact remains searchable and visible in the workspace but displays a "Risk score unavailable" indicator. No crash or data loss occurs. | P3 |
| TC-FOR-FR09-005 | FR-09 (BR-FR-09-01, FH-FR-09-02) | **Edge Case: Risk rule weights are versioned and missing/unpublished rule set blocks recalculation** | Risk rule configuration v1.0 is published with effective_from = "2026-01-01". A new v2.0 is in Draft status (not yet published). | 1. Attempt to trigger score recalculation referencing the unpublished v2.0 rule set. 2. Verify the system response. 3. Publish v2.0 with effective_from = "2026-03-01". 4. Trigger recalculation. Verify it uses v2.0. | When v2.0 is unpublished: the system rejects the recalculation with an admin-facing configuration error: "Rule set v2.0 is not published. Cannot compute risk scores." After v2.0 is published: recalculation succeeds using v2.0 weights. The score_version field on new scores references "v2.0". Scores computed under v1.0 retain score_version = "v1.0". | P4 |

---

### FR-10 -- Legal Mapping & Statutory Reference Management

**BRD Reference:** FR-10, AC-FR-10-01 through AC-FR-10-05, BR-FR-10-01 through BR-FR-10-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR10-001 | FR-10 (AC-FR-10-01, AC-FR-10-02) | **Happy Path: System suggests legal mappings for findings based on versioned mapping table** | A finding exists with category_code = "NarcoticsReference" and reason_code = "KEYWORD_MATCH_NARCOTICS". The legal mapping table v1.0 is published with an entry mapping NarcoticsReference + KEYWORD_MATCH_NARCOTICS to NDPS Act Section 21 (legal_section_id = "LS-NDPS-21"). | 1. After AI analysis generates the finding, verify the legal mapping suggestion. 2. Query legal mappings for the finding via the API. | A LegalMapping record is created with: legal_mapping_id, finding_id, legal_section_id = "LS-NDPS-21", law_name = "NDPS Act", section_code = "Section 21", mapping_rationale = (auto-generated rationale referencing the keyword match and category), source_finding_ids = [finding_id], evidence_reference_count >= 1, mapping_status = "Suggested". The mapping references the mapping table version v1.0. | P1 |
| TC-FOR-FR10-002 | FR-10 (AC-FR-10-03, AC-FR-10-04) | **Role-Based: Analyst accepts a mapping, then Supervisor approves it for report inclusion** | A Suggested legal mapping exists. An Analyst and a Supervisor are logged in. | 1. As Analyst, navigate to the legal mapping in the React UI. Click "Accept." Verify mapping_status changes to "Accepted." 2. As Analyst, attempt to change mapping_status to "Approved." Verify rejection. 3. As Supervisor, navigate to the accepted mapping. Click "Approve." Verify mapping_status changes to "Approved." | Analyst can Accept or Reject mappings but cannot Approve them (API returns HTTP 403 if Analyst attempts Approved status). Supervisor can Approve. After approval: mapping_status = "Approved", approved_by = Supervisor's user_id. The approved mapping becomes available for insertion into report templates. | P2 |
| TC-FOR-FR10-003 | FR-10 (AC-FR-10-05, EC-FR-10-01) | **Edge Case: One finding maps to multiple legal sections and approved mappings appear in report template** | A finding with category_code = "FinancialRouting" maps to both "NDPS Act Section 24" (proceeds of illicit trafficking) and "PMLA Section 3" (money laundering). Both mappings are Approved. | 1. Verify two LegalMapping records exist for the same finding_id. 2. Both are Approved by Supervisor. 3. Generate a report using a template that includes legal mapping sections. 4. Verify both legal sections appear in the report draft. | Two distinct LegalMapping records exist with different legal_section_ids. Both have mapping_status = "Approved". The generated report draft includes structured sections for both "NDPS Act Section 24" and "PMLA Section 3" with their respective mapping_rationale and evidence references. | P2 |
| TC-FOR-FR10-004 | FR-10 (FH-FR-10-01) | **Failure Handling: No rule-based mapping exists, system marks ManualSelectionRequired** | A finding exists with category_code = "DarkWebIndicator" but no entry in the legal mapping table maps this category to any legal section. | 1. After AI analysis, check the legal mapping for this finding. 2. Verify the system behavior when no automatic mapping exists. | No automatic LegalMapping record is created. Instead, the finding is flagged with mapping_status = "ManualSelectionRequired" in the finding detail view. The React UI presents a manual legal section selector allowing the Analyst or Supervisor to manually assign a legal section. The manually assigned mapping stores mapping_status = "ManuallyAssigned". | P3 |
| TC-FOR-FR10-005 | FR-10 (EC-FR-10-02, FH-FR-10-02, BR-FR-10-02) | **Edge Case: Legal section master updated after report draft exists; rejected mappings remain auditable** | A report draft references an Approved legal mapping to "IPC Section 420". The Configuration Manager updates the legal section master, retiring "IPC Section 420" (replaced by "BNS Section 318"). A separate Rejected mapping for another finding also exists. | 1. Configuration Manager publishes updated legal section master retiring IPC 420. 2. Verify the report draft is flagged for review. 3. Check the rejected mapping is still visible in the audit trail. | The report draft that references the retired "IPC Section 420" is flagged with a review warning: "Referenced legal section retired -- update required before publication." The report cannot be published until the legal mapping is updated to reference the successor section. The previously Rejected mapping remains visible in the legal mapping history and audit trail. It cannot be deleted (BR-FR-10-02). | P4 |

---

### FR-11 -- Report Composition, Approval, Redaction & Export

**BRD Reference:** FR-11, AC-FR-11-01 through AC-FR-11-06, BR-FR-11-01 through BR-FR-11-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR11-001 | FR-11 (AC-FR-11-01, AC-FR-11-03, AC-FR-11-06) | **Happy Path: Generate report from reviewed findings, export to PDF and DOCX** | A case has 50 Confirmed findings, 10 Approved legal mappings, analyst annotations, and a completed chain-of-custody. A report template "Standard Forensic Report v3" (template_id) is published. | 1. As Analyst, call `POST /api/v1/cases/{caseId}/reports` with template_id, finding_ids (50 confirmed findings), redaction_profile_id = null, include_annexures = true. 2. Verify the report draft. 3. Export to PDF. 4. Export to DOCX. | Report draft is created: report_id (UUID), version_no = 1, status = "Draft", template_id, generated_by = Analyst user_id, generated_at. The report contains: case metadata, evidence summary, 50 findings, relationship summary, 10 legal mappings, analyst notes, and chain-of-custody summary. PDF export: file is downloaded, watermarked "DRAFT", export_hash and export_timestamp are recorded. DOCX export: same content in Word format. Both exports store export_format ("PDF"/"DOCX") and export_hash. | P1 |
| TC-FOR-FR11-002 | FR-11 (AC-FR-11-02, AC-FR-11-04, BR-FR-11-01) | **Happy Path: Full report approval lifecycle -- Draft to Published to Superseded** | A report draft (version_no = 1) exists with all findings in Confirmed status. A Supervisor is available. | 1. As Analyst, submit report for approval (status -> PendingApproval). 2. As Supervisor, call `POST /api/v1/reports/{reportId}/approve` with step-up re-authentication. Verify status -> Approved. 3. As Supervisor, publish the report. Verify status -> Published, watermark changes to "FINAL." 4. Create a new draft (version_no = 2) from the published report. 5. Verify the original (version_no = 1) transitions to Superseded. | Draft -> PendingApproval -> Approved (with approved_by, approved_at) -> Published (watermark = "FINAL"). Published report is immutable. Creating version_no = 2 sets version_no = 1 status to "Superseded". All state transitions are recorded as audit events. Step-up re-auth was required for the approve action. | P1 |
| TC-FOR-FR11-003 | FR-11 (AC-FR-11-04) | **Negative: Attempt to publish report containing Unreviewed findings** | A report is in PendingApproval status. The report references 50 findings, of which 5 are still in "Unreviewed" status. | 1. As Supervisor, attempt to approve the report via `POST /api/v1/reports/{reportId}/approve`. 2. Observe the API response. | The API returns HTTP 422 with error_code "REPORT_CONTAINS_UNREVIEWED_FINDINGS" and details listing the 5 unreviewed finding_ids. The report status remains "PendingApproval". The Supervisor is directed to the unreviewed findings for disposition before re-attempting approval. | P2 |
| TC-FOR-FR11-004 | FR-11 (AC-FR-11-05) | **Happy Path: Apply redaction profile for role-based sharing** | A published report exists with full content including sensitive annexures (identity documents, financial records). A redaction profile "External Sharing" is configured to hide IdentityDocument annexures and mask BankAccount numbers. | 1. Generate a new report version with redaction_profile_id = "External Sharing" profile. 2. Export to PDF. 3. Verify redacted content. 4. Export without redaction profile for internal use. 5. Compare the two exports. | Redacted PDF: IdentityDocument annexures are hidden (not included in output). BankAccount numbers are masked (e.g., "XXXX-XXXX-1234"). The redaction profile applies field-level and annexure-level controls. Unredacted PDF: Full content is visible. Both exports have distinct export_hash values. The redaction_profile_id is recorded in the report metadata. | P2 |
| TC-FOR-FR11-005 | FR-11 (FH-FR-11-01, EC-FR-11-01) | **Edge Case: Template validation failure and source artifacts reprocessed before approval** | An Analyst attempts to generate a report using a template that references a field "suspect_vehicle_reg" which does not exist in the current schema. Separately, another report draft references artifacts that are reprocessed (new import job version) while the draft is pending approval. | 1. Attempt report generation with the invalid template. 2. Observe error handling. 3. For the second scenario: Analyst triggers reprocessing of evidence while a report draft is PendingApproval. 4. Check the report draft status. | Invalid template: API returns HTTP 422 with template_error details (field "suspect_vehicle_reg" not found in schema). No publishable file is created. Report status does not advance. Reprocessed artifacts: The report draft in PendingApproval is flagged with a warning: "Source artifacts reprocessed since draft generation -- review before approval." The Supervisor sees this warning before approving. | P3 |

---

### FR-12 -- DOPAMS Integration & Synchronization

**BRD Reference:** FR-12, AC-FR-12-01 through AC-FR-12-05, BR-FR-12-01 through BR-FR-12-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR12-001 | FR-12 (AC-FR-12-01, AC-FR-12-02) | **Happy Path: Outbound status sync to DOPAMS with full transaction record** | A case is linked to DOPAMS reference "DOPAMS-2026-5678". The case status changes from UnderReview to ReportReady. DOPAMS integration is configured and reachable. | 1. Supervisor changes case status to ReportReady. 2. System queues outbound DOPAMS sync via `POST /api/v1/integrations/dopams/sync` with event_type = "StatusUpdate". 3. Monitor sync event. | A DOPAMSSyncEvent record is created with: sync_event_id, case_id, direction = "Outbound", event_type = "StatusUpdate", payload_hash (hash of the status update payload), idempotency_key, status = "Queued". After processing: status transitions to "InProgress" then "Succeeded". The record stores attempt_count = 1, last_attempt_at, and endpoint_ref. The versioned field mapping between internal fields (case status) and DOPAMS fields is applied correctly. | P1 |
| TC-FOR-FR12-002 | FR-12 (AC-FR-12-03, BR-FR-12-01) | **Happy Path: Outbound report reference sync after report publication and idempotency verification** | A report is Published for a DOPAMS-linked case. DOPAMS integration is configured. | 1. After report publication, trigger `POST /api/v1/integrations/dopams/sync` with event_type = "ReportReference", idempotency_key = "rpt-sync-001". 2. Verify sync succeeds. 3. Retry the same call with identical idempotency_key. 4. Verify no duplicate is created. | First call: sync_event created with status = "Succeeded", event_type = "ReportReference". The sync payload includes the published report_id, export_hash, and case reference. Second call (retry): API returns the same sync_event_id. No duplicate outbound report reference is created in DOPAMS. attempt_count does not increment for a successful idempotent retry. BR-FR-12-01 is satisfied: report reference sync only occurs for Published reports. | P1 |
| TC-FOR-FR12-003 | FR-12 (AC-FR-12-04, FH-FR-12-01) | **Failure Handling: DOPAMS unavailable triggers retry with configurable backoff, then DeadLetter** | DOPAMS endpoint is unavailable (simulated 503 response). A case status sync is queued. Maximum retry count is configured as 5 with exponential backoff. | 1. Queue outbound sync while DOPAMS is unavailable. 2. Monitor sync event as retries occur. 3. Allow all 5 retries to fail. 4. Verify DeadLetter state and supervisor notification. | Sync event transitions: Queued -> InProgress -> Failed (attempt 1) -> Failed (attempt 2, after backoff delay) -> ... -> Failed (attempt 5). After exceeding max retries: status = "DeadLetter". attempt_count = 5. error_message contains the latest 503 error summary. A supervisor alert is generated: "DOPAMS sync for case [case_reference] has exceeded retry threshold and entered DeadLetter." The sync event is surfaced in the Supervisor and Administrator dashboards. | P2 |
| TC-FOR-FR12-004 | FR-12 (BR-FR-12-02, FH-FR-12-02) | **Negative: Inbound DOPAMS update with invalid schema version is rejected** | An inbound webhook or API call from DOPAMS delivers a case update payload using schema version "v0.9" while the platform expects "v1.0". | 1. DOPAMS (Integration Service account) sends inbound sync with schema_version = "v0.9". 2. The platform validates the payload against the expected schema version. 3. Observe the sync event result. | The inbound sync event is created with status = "Failed" and error_message = "Schema version mismatch: expected v1.0, received v0.9". The payload is not written to the case record. The sync attempt does not retry automatically (FH-FR-12-02: retry is blocked until mapping or payload is corrected). An alert is raised for the Administrator to investigate the schema mismatch. | P2 |
| TC-FOR-FR12-005 | FR-12 (EC-FR-12-02) | **Edge Case: Same DOPAMS case linked by two local cases in error** | Two separate local cases (Case-A and Case-B) both attempt to link to the same DOPAMS reference "DOPAMS-2026-9999". | 1. Case-A is created and linked to DOPAMS-2026-9999. Sync succeeds. 2. Case-B is created and attempts to link to DOPAMS-2026-9999. 3. The system detects the duplicate DOPAMS linkage. | The system detects that DOPAMS-2026-9999 is already linked to Case-A. Case-B receives a validation warning or error: "DOPAMS reference DOPAMS-2026-9999 is already linked to case [Case-A reference]." Depending on configuration: either the linkage is blocked (returning HTTP 409 Conflict), or it is allowed with a warning that requires supervisor review. An audit event records the conflict detection. | P3 |

---

### FR-13 -- Alerts, Notifications & Escalations

**BRD Reference:** FR-13, AC-FR-13-01 through AC-FR-13-05, BR-FR-13-01 through BR-FR-13-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR13-001 | FR-13 (AC-FR-13-01, AC-FR-13-02) | **Happy Path: Critical finding triggers alert within 30 seconds with correct lifecycle metadata** | AI analysis has just completed, generating a Critical-severity finding (NarcoticsReference with confidence_score = 0.95). Alert rules are configured to trigger on risk_band = "Critical". | 1. Record the timestamp when the finding is created. 2. Monitor the alerts API or React UI notification panel. 3. Verify alert creation time is within 30 seconds. 4. Inspect the alert record. | An Alert record is created within 30 seconds of the finding being recorded. Alert fields: alert_id (UUID), case_id, alert_type = "Risk", severity = "Critical", status = "Open", sla_due_at = (created_at + 15 minutes, per BR-FR-13-01 default for Critical), assigned_to_role_id = Supervisor role. In-app notification appears in the React UI notification bell for the assigned role. The alert references the triggering finding_id. | P1 |
| TC-FOR-FR13-002 | FR-13 (AC-FR-13-03, BR-FR-13-02) | **Happy Path: Alert lifecycle -- Open to Acknowledged to Resolved with dismissal requiring reason** | A Critical alert exists in Open status assigned to Supervisor-A. | 1. As Supervisor-A, click "Acknowledge" in the React UI. Verify status -> Acknowledged. 2. Work on the underlying issue. Change status to "InProgress." 3. Resolve the alert with resolution_note: "Finding confirmed, case priority elevated, analyst notified." 4. Separately, attempt to Dismiss a different alert without providing dismissal_reason. | Acknowledged: alert status = "Acknowledged", timestamp recorded. InProgress: status updated. Resolved: status = "Resolved", resolution_note stored. Dismiss without reason: API returns HTTP 422 requiring dismissal_reason. The alert remains in its current status until a reason is provided. All state transitions are recorded in audit events. | P2 |
| TC-FOR-FR13-003 | FR-13 (AC-FR-13-04, AC-FR-13-05) | **Happy Path: Alert assignment and SLA escalation for unacknowledged High-severity alert** | A High-severity alert is created and assigned to Analyst-A. SLA for High is 4 hours (BR-FR-13-01). The alert is not acknowledged within 4 hours. | 1. Alert is created with sla_due_at = created_at + 4 hours. 2. Wait for SLA to expire (simulated by advancing time in test environment). 3. Verify escalation occurs. 4. Check the escalated_at field. | After SLA expiry: the alert's escalated_at field is populated. The alert is reassigned or additionally assigned to the Supervisor role. An in-app notification is sent to the Supervisor. If email/webhook notifications are configured, those are also triggered. The original assignment to Analyst-A is preserved in the alert history. | P2 |
| TC-FOR-FR13-004 | FR-13 (EC-FR-13-01, FH-FR-13-02) | **Edge Case: Same event matches multiple alert rules; duplicate suppression links events** | A single event (Critical finding on a high-priority case with 3 failed integration retries) matches three alert rules: (1) risk threshold rule, (2) case priority rule, (3) integration failure rule. | 1. Trigger the event. 2. Monitor alert creation. 3. Verify duplicate suppression behavior. | The system creates a single alert for the primary trigger (e.g., the Critical risk threshold). The other two rule matches are linked to the existing alert record rather than creating duplicate alerts (FH-FR-13-02). The alert's metadata or linked_events field references all three triggering rule matches. The alert severity reflects the highest applicable level (Critical). | P3 |
| TC-FOR-FR13-005 | FR-13 (EC-FR-13-02, FH-FR-13-01) | **Edge Case: Assigned user becomes inactive before acknowledgement; external notification delivery failure** | An alert is assigned to Analyst-A. Analyst-A is deactivated by an Administrator before acknowledging the alert. Separately, an alert's email notification encounters a delivery failure. | 1. Create an alert assigned to Analyst-A. 2. Administrator deactivates Analyst-A. 3. SLA approaches. Observe reassignment behavior. 4. For the notification failure: trigger an alert with email notification configured to a bad address. | When the assigned user becomes inactive: the system detects the inactive assignment (either immediately or at SLA check time) and reassigns the alert to the Supervisor role or another active user in the same unit. An audit event records the reassignment reason = "ASSIGNED_USER_INACTIVE". For notification failure: the alert remains Open (not marked as delivered). The notification subsystem retries per the notification policy. The in-app notification still appears correctly. | P4 |

---

### FR-14 -- Identity, RBAC & Session Security

**BRD Reference:** FR-14, AC-FR-14-01 through AC-FR-14-06, BR-FR-14-01 through BR-FR-14-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR14-001 | FR-14 (AC-FR-14-01, AC-FR-14-06) | **Happy Path: Local authentication with MFA for privileged role** | An Administrator user exists with local authentication (auth_provider = "LOCAL"), mfa_enabled = true. OTP generator is configured. | 1. Navigate to the login page in the React UI. 2. Enter username and password. 3. Submit `POST /api/v1/auth/token` with username and password only (no otp_code). 4. Receive MFA challenge. 5. Enter valid OTP code and resubmit with otp_code. 6. Verify the token response. | Step 3: API returns HTTP 200 with `mfa_required: true` and no access_token. Step 5: API returns HTTP 200 with access_token (JWT), refresh_token, expires_in = 3600, token_type = "Bearer", mfa_required = false. The access_token contains role claims for Administrator. An audit event records the login action with outcome = "Success". | P1 |
| TC-FOR-FR14-002 | FR-14 (AC-FR-14-02, AC-FR-14-03) | **Role-Based: Verify permission matrix across all eight roles** | Users exist for all eight roles: Administrator, Supervisor, Analyst, InvestigatorReadOnly, Auditor, IntegrationService, ConfigurationManager, SupportOperator. | 1. For each role, attempt the following operations via API: case_view, case_edit, evidence_upload, evidence_export, report_generate, report_approve, config_manage, user_manage, audit_view, integration_manage. 2. Record HTTP status code for each. | Permission matrix matches the BRD specification: Administrator has all permissions. Supervisor: all except user_manage. Analyst: case_view/edit, evidence_upload, report_generate, no report_approve/config_manage/user_manage. InvestigatorReadOnly: case_view only. Auditor: case_view, audit_view. IntegrationService: integration_manage only. ConfigurationManager: config_manage, case_view. SupportOperator: case_view, audit_view (no evidence_export). Every denied operation returns HTTP 403. | P1 |
| TC-FOR-FR14-003 | FR-14 (AC-FR-14-04, AC-FR-14-05) | **Happy Path: Case access scoping and step-up re-authentication for privileged operations** | Analyst-A is assigned to Case-1 (assignment-based scope). Analyst-B is in the same unit as Case-2 (unit-based scope). Neither has access to Case-3 (different unit, not explicitly granted). | 1. As Analyst-A, access Case-1. Verify success. 2. As Analyst-A, access Case-3. Verify denial. 3. As Analyst-B, access Case-2 (same unit). Verify success based on unit-based scope. 4. As Analyst-A, attempt evidence export on Case-1 (privileged operation). Verify step-up re-authentication is required. | Case-1: Analyst-A can view and edit (assignment-based). Case-3: API returns HTTP 403 (no access). Case-2: Analyst-B can view (unit-based scope). Evidence export: The React UI prompts for re-authentication (password re-entry or MFA). After successful step-up, the export proceeds. Without step-up, the API returns HTTP 401 with error "STEP_UP_AUTH_REQUIRED". Privileged operations requiring step-up: evidence_export, report_publication, configuration_publish, role_change. | P2 |
| TC-FOR-FR14-004 | FR-14 (BR-FR-14-01, BR-FR-14-02) | **Negative: Session expiry and service account authentication restrictions** | A Supervisor user (privileged) is logged in. Session inactivity timeout is 15 minutes. A service account (IntegrationService) exists. | 1. As Supervisor, log in and perform actions. 2. Leave the session idle for 16 minutes. 3. Attempt any API call. Verify session expiry. 4. Attempt to log in with the service account via the interactive login form (`POST /api/v1/auth/token`). | Session expiry: After 16 minutes idle, the API returns HTTP 401 with error "SESSION_EXPIRED". The React UI redirects to the login page. For non-privileged roles, the timeout is 30 minutes. Service account login: The `POST /api/v1/auth/token` endpoint rejects the service account with error "AUTH_INVALID_CREDENTIALS" or "AUTH_NON_INTERACTIVE_ONLY". Service accounts can authenticate only with mTLS or signed JWT, not via the interactive login API. | P2 |
| TC-FOR-FR14-005 | FR-14 (FH-FR-14-01, EC-FR-14-02) | **Failure Handling: SSO unavailable with local fallback disabled, then enabled** | SSO is configured as the primary authentication provider. SSO identity provider is down (simulated). Local fallback is initially disabled. | 1. Attempt SSO login while IdP is down. Verify access denied. 2. Check that auth_failure_reason = "SSO_PROVIDER_UNAVAILABLE" is logged. 3. Administrator enables local fallback authentication via a separate admin channel. 4. Users can now log in with local credentials. 5. Verify audit trail records the fallback activation. | With SSO down and fallback disabled: all login attempts fail. The system logs auth_failure_reason with the SSO error details. No sensitive object data is exposed in the error response. After local fallback is enabled: users with local credentials can authenticate. The audit trail records: (a) the SSO failure events, (b) the configuration change enabling local fallback, (c) subsequent successful local logins. | P3 |

---

### FR-15 -- MIS, Analytics & Operational Reporting

**BRD Reference:** FR-15, AC-FR-15-01 through AC-FR-15-05, BR-FR-15-01 through BR-FR-15-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR15-001 | FR-15 (AC-FR-15-01, AC-FR-15-02) | **Happy Path: Generate case volume MIS report with filters and verify data accuracy** | 50 cases exist across 3 investigating units, spanning the last 90 days. Cases have various statuses (Active, UnderReview, Closed). | 1. Call `GET /api/v1/mis/reports/case-volume?date_from=2025-12-01&date_to=2026-03-01&unit=EAGLE-FORCE-HYD`. 2. Verify the response dimensions and row data. 3. Cross-reference with actual case records in the database. | The response contains dimensions: ["date", "unit", "status"]. Rows show daily case counts grouped by unit and status. Filtering by unit = "EAGLE-FORCE-HYD" returns only that unit's data. The total case count matches the actual database records (BR-FR-15-01: data derived from production records, not manually entered). Timestamps are non-editable (BR-FR-15-02). | P1 |
| TC-FOR-FR15-002 | FR-15 (AC-FR-15-03) | **Happy Path: Export MIS report to CSV and PDF formats** | An MIS report for import volume over the last 30 days has been generated. | 1. Request CSV export of the import volume report. 2. Download and verify CSV structure (headers, data types, completeness). 3. Request PDF export of the same report. 4. Download and verify PDF formatting (tables, charts, header with date range). | CSV export: File downloads with proper headers (date, evidence_id, source_tool, import_status, artifact_count). All rows from the report are present. PDF export: File downloads with formatted tables, optional charts for visualization, and a header showing the date range and generation timestamp. Both formats contain identical data. | P2 |
| TC-FOR-FR15-003 | FR-15 (AC-FR-15-04) | **Happy Path: Scheduled MIS report generation with recipient list** | Administrator has configured a weekly scheduled MIS report for "Finding Severity Distribution" with recipients: all Supervisor-role users in unit "EAGLE-FORCE-HYD". Schedule: every Monday at 08:00 UTC. | 1. Configure the scheduled report via the admin panel. 2. Wait for the scheduled time (or trigger manually in test). 3. Verify report generation. 4. Verify recipients receive the report (in-app notification and/or email if configured). | The report is generated at the scheduled time. Recipients matching the role + unit criteria receive an in-app notification with a link to download the report. The report covers the previous week's data (Monday-to-Sunday). The schedule persists and will execute again the following Monday. Generation timestamp and schedule_id are recorded. | P2 |
| TC-FOR-FR15-004 | FR-15 (AC-FR-15-05) | **Role-Based: Permission-controlled access to MIS reports** | An Analyst, Supervisor, and SupportOperator are logged in. MIS report access permissions are configured: Supervisor can access all reports, Analyst can access only case-volume and artifact-count reports, SupportOperator can access queue-backlog reports only. | 1. As Analyst, access case-volume report. Verify allowed. 2. As Analyst, access report-turnaround-time report. Verify denied. 3. As Supervisor, access all reports. Verify allowed. 4. As SupportOperator, access queue-backlog. Verify allowed. 5. As SupportOperator, access finding-severity-distribution. Verify denied. | Each role can only access MIS reports permitted by their role configuration. Denied access returns HTTP 403. The React UI only renders navigation links to reports the user is permitted to view (no broken links or unauthorized access attempts). | P3 |
| TC-FOR-FR15-005 | FR-15 (EC-FR-15-01, FH-FR-15-01, FH-FR-15-02) | **Edge Case: Large report export and scheduled generation failure** | A report spanning 12 months with millions of artifact records is requested. Separately, a scheduled report generation encounters a database timeout. | 1. Request MIS export for 12 months of artifact data. 2. Observe handling for the large data set. 3. Simulate scheduled report database timeout. 4. Verify failure handling. | Large export: The system detects the large data set and switches to asynchronous generation. The API returns HTTP 202 Accepted with a job_id. When generation completes, a downloadable link is provided via notification. Scheduled failure: An operational alert is created (FH-FR-15-01). The schedule is retained (not deleted). The next scheduled execution will attempt normally. The alert references the failed generation attempt with error details. | P4 |

---

### FR-16 -- Configuration, Template & Model Governance

**BRD Reference:** FR-16, AC-FR-16-01 through AC-FR-16-05, BR-FR-16-01 through BR-FR-16-02
**Priority:** Should Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR16-001 | FR-16 (AC-FR-16-01, AC-FR-16-02) | **Happy Path: Create, version, and publish a keyword dictionary configuration** | A Configuration Manager user is logged in. No keyword dictionary is currently published. | 1. Create a new keyword dictionary config via the React admin panel with content including narcotics slang terms in English, Hindi, and Telugu. Version label: "v1.0". 2. Verify config status = "Draft". 3. Publish the dictionary via `POST /api/v1/config/versions/{configVersionId}/publish` with step-up re-auth and effective_from date. 4. Verify status transitions. | Config record created: config_version_id, config_type = "Dictionary", version_label = "v1.0", status = "Draft", content_json = (dictionary entries). After publish: status = "Published", published_at and published_by are populated. The dictionary is active and used by the AI analysis pipeline. An audit event records the publish action. The config is versioned with draft/published state tracking. | P1 |
| TC-FOR-FR16-002 | FR-16 (AC-FR-16-03, AC-FR-16-04) | **Happy Path: Export/import configuration package for environment promotion without application redeployment** | A published keyword dictionary v1.0 and a published report template v2.0 exist in the staging environment. The production environment is a separate instance. | 1. In staging, export the configuration package as JSON via the admin panel. 2. Verify the JSON contains both configs with their version metadata. 3. In production, import the JSON package. 4. Verify the configs are created in Draft status in production. 5. Publish them in production. 6. Verify no application redeployment was required. | Export: JSON file contains both configurations with config_type, version_label, content_json, and metadata. Import: Both configs are created in production in Draft status. After publishing in production: they are active without any code deployment or application restart (AC-FR-16-04). The entire process is auditable with import/export events. | P2 |
| TC-FOR-FR16-003 | FR-16 (AC-FR-16-05, BR-FR-16-01) | **Happy Path: Track which config_version_id and model_version produced each finding and score** | Keyword dictionary v1.0 is published. AI model "narcotics-classifier" v2.1 is active. 100 findings are generated. | 1. Ingest evidence and run AI analysis. 2. Query findings. 3. Inspect the config and model version references on each finding. 4. Publish dictionary v2.0. Run analysis on new evidence. 5. Verify new findings reference v2.0. | Findings from the first analysis reference: keyword dictionary v1.0 (in rule_hits_json or config metadata) and model_version = "narcotics-classifier-v2.1". Findings from the second analysis (after v2.0 publish) reference dictionary v2.0. Only one published version is active per config_type (BR-FR-16-01). Previous findings retain their original version references. Risk scores similarly record the score_version used. | P2 |
| TC-FOR-FR16-004 | FR-16 (BR-FR-16-02, FH-FR-16-01) | **Edge Case: Rollback configuration version and validation failure blocks publish** | Keyword dictionary v2.0 is Published. A new v3.0 is created but contains a syntax error in the regex patterns (invalid regex). | 1. Attempt to publish v3.0 via `POST /api/v1/config/versions/{configVersionId}/publish`. 2. Verify validation failure. 3. Roll back to v2.0. 4. Verify rollback behavior. | Publish v3.0: API returns HTTP 422 with validation_errors detailing the invalid regex patterns. v3.0 remains in Draft status. Rollback to v2.0: A new active version pointer is created referencing v2.0. v2.0 is re-activated as the current published version. Intermediate versions (including failed v3.0) are not deleted (BR-FR-16-02). The rollback is recorded in the audit trail with the rollback reason. | P3 |
| TC-FOR-FR16-005 | FR-16 (EC-FR-16-01, FH-FR-16-02) | **Edge Case: Template references removed schema field; config import with unknown keys** | A report template v3.0 references field "suspect_vehicle_reg" which existed in schema v1.0 but was removed in schema v2.0. Separately, a config JSON import contains an unknown key "threat_level_override" not in the current schema. | 1. Attempt to publish template v3.0 that references the removed field. 2. Verify handling. 3. Import a config JSON package with the unknown key. 4. Verify import behavior. | Template with removed field: validation detects the missing field reference. Publish is blocked with error "Field 'suspect_vehicle_reg' not found in current schema version." Config import with unknown key: Depending on import policy, the system either (a) rejects the entire package with error "Unknown key: threat_level_override" or (b) maps/ignores unknown keys per policy configuration. The behavior is consistent with the configured import policy. Both scenarios are logged with appropriate detail. | P4 |

---

### FR-17 -- Retention, Archive & Purge Approval

**BRD Reference:** FR-17, AC-FR-17-01 through AC-FR-17-05, BR-FR-17-01 through BR-FR-17-02
**Priority:** Must Have

| TC ID | FR Ref | Test Description | Pre-conditions | Test Steps | Expected Result | Priority |
|---|---|---|---|---|---|---|
| TC-FOR-FR17-001 | FR-17 (AC-FR-17-01, AC-FR-17-02) | **Happy Path: Apply retention policy and set legal hold on a case** | A case exists with source evidence, normalized artifacts, reports, and audit logs. A retention policy "RP-STANDARD-5Y" is defined with 5-year retention for evidence and reports. | 1. As Administrator, apply retention policy "RP-STANDARD-5Y" to the case. 2. Verify the policy is recorded with effective dates. 3. Set legal_hold_status to "InvestigationHold" on the case. 4. Verify the hold is applied to all associated evidence, artifacts, and reports. | The retention policy is applied: case record shows policy_id = "RP-STANDARD-5Y" with effective_from date. legal_hold_status transitions: None -> InvestigationHold. All objects under the case (evidence sources, artifacts, reports, audit logs) inherit the legal hold. An audit event records both the policy application and legal hold actions. The case is now protected from any archive or purge operations. | P1 |
| TC-FOR-FR17-002 | FR-17 (AC-FR-17-03, AC-FR-17-04, BR-FR-17-01) | **Negative: Attempt to purge evidence under legal hold and end-user hard-delete prevention** | A case is under LegalHold status. Evidence sources exist. An Analyst and an Administrator are logged in. | 1. As Analyst, attempt to delete evidence via any available UI action or API call. 2. Verify end users cannot hard-delete. 3. As Administrator, attempt to initiate purge workflow on the legally held case. 4. Verify purge is blocked. | Analyst delete: No delete option is available in the React UI. If API is called directly, returns HTTP 403 "END_USER_DELETE_NOT_PERMITTED". Administrator purge: The system rejects the purge request with error "LEGAL_HOLD_ACTIVE -- purge not permitted on objects under legal hold" (BR-FR-17-01). No evidence, artifacts, or reports are deleted. The rejection is recorded in the audit trail. | P1 |
| TC-FOR-FR17-003 | FR-17 (AC-FR-17-03, BR-FR-17-02) | **Happy Path: Approved purge workflow for expired-retention records without legal hold** | A closed case has retention policy with effective_to date in the past (retention expired). legal_hold_status = "PurgeApproved". No active legal holds reference this case. | 1. System identifies records with expired retention. 2. Administrator initiates purge workflow. 3. A privileged approver (Supervisor or Administrator) reviews and approves the purge with recorded_reason: "Retention period expired per policy RP-STANDARD-5Y. No outstanding legal holds." 4. Purge executes. 5. Verify audit trail. | Purge workflow: requires privileged approval (Supervisor+), recorded_reason, and audit event. After approval: source evidence files are permanently deleted from object storage. Normalized artifacts and reports are removed. Audit logs for the purge action itself are retained (not purged). The purge audit event records: actor, approval chain, reason, list of purged object IDs, and timestamp. The case metadata entry may be retained as a tombstone record. | P2 |
| TC-FOR-FR17-004 | FR-17 (AC-FR-17-05) | **Happy Path: Archive records to cold storage and verify metadata discoverability** | A closed case with expired retention but legal_hold_status = "None" has 50 GB of evidence data. Archive policy is configured for cold storage after 2 years. | 1. System identifies the case as eligible for archival based on the 2-year policy. 2. Administrator approves archival. 3. Evidence files are moved to cold storage. 4. As an authorized user, search for the archived case. 5. Verify metadata is discoverable. | Archived records: evidence files are moved to cold storage (object_storage_uri updated to cold-storage path). Case metadata, artifact metadata, and report metadata remain in the primary database and are discoverable by authorized users (AC-FR-17-05). The React search results show an "Archived" badge on the case. Evidence preview shows "Archived -- request restore to view original files." An audit event records the archive action. | P2 |
| TC-FOR-FR17-005 | FR-17 (EC-FR-17-01, EC-FR-17-02, FH-FR-17-01, FH-FR-17-02) | **Edge Case: Case closure during external legal review; cold-storage restore; partial purge failure** | Case-A is closed but a report is under external legal review (legal_hold_status = "LegalHold"). Case-B is archived to cold storage. A purge operation on Case-C partially fails (deletes 3 of 5 evidence files before encountering a storage error). | 1. Verify Case-A cannot be purged or archived while under LegalHold. 2. Request restore of Case-B from cold storage. Verify the restore process. 3. Trigger purge on Case-C and simulate partial failure. 4. Verify compensating behavior. | Case-A: Purge and archive operations are blocked. The case remains protected until legal hold is released. Case-B restore: A restore request is submitted. Evidence files are copied from cold storage back to primary storage. The restore is auditable and may take significant time (async with notification). Case-C partial failure: The system creates a compensating audit log recording which files were deleted and which failed. Further deletion is halted (FH-FR-17-02). An operational alert is raised. The case retains references to the remaining 2 un-deleted files. Manual intervention is required to complete or reverse the partial purge. | P3 |

---

## 3. Traceability Matrix

This matrix maps every Functional Requirement (FR) to its corresponding test case IDs, ensuring 100% coverage.

| FR Ref | FR Title | Test Case IDs | Coverage Status |
|---|---|---|---|
| FR-01 | Case Management & Assignment | TC-FOR-FR01-001, TC-FOR-FR01-002, TC-FOR-FR01-003, TC-FOR-FR01-004, TC-FOR-FR01-005 | Covered |
| FR-02 | Evidence Intake & Registration | TC-FOR-FR02-001, TC-FOR-FR02-002, TC-FOR-FR02-003, TC-FOR-FR02-004, TC-FOR-FR02-005 | Covered |
| FR-03 | Multi-Tool Ingestion, Parsing & Normalization | TC-FOR-FR03-001, TC-FOR-FR03-002, TC-FOR-FR03-003, TC-FOR-FR03-004, TC-FOR-FR03-005 | Covered |
| FR-04 | Evidence Preservation, Audit Trail & Chain of Custody | TC-FOR-FR04-001, TC-FOR-FR04-002, TC-FOR-FR04-003, TC-FOR-FR04-004, TC-FOR-FR04-005 | Covered |
| FR-05 | Artifact Repository, Search & Review Workspace | TC-FOR-FR05-001, TC-FOR-FR05-002, TC-FOR-FR05-003, TC-FOR-FR05-004, TC-FOR-FR05-005 | Covered |
| FR-06 | OCR & Derived Artifact Generation | TC-FOR-FR06-001, TC-FOR-FR06-002, TC-FOR-FR06-003, TC-FOR-FR06-004, TC-FOR-FR06-005 | Covered |
| FR-07 | AI Suspicious Content Detection & Classification | TC-FOR-FR07-001, TC-FOR-FR07-002, TC-FOR-FR07-003, TC-FOR-FR07-004, TC-FOR-FR07-005 | Covered |
| FR-08 | Entity Extraction, Resolution & Link Analysis | TC-FOR-FR08-001, TC-FOR-FR08-002, TC-FOR-FR08-003, TC-FOR-FR08-004, TC-FOR-FR08-005 | Covered |
| FR-09 | Risk Scoring & Prioritization | TC-FOR-FR09-001, TC-FOR-FR09-002, TC-FOR-FR09-003, TC-FOR-FR09-004, TC-FOR-FR09-005 | Covered |
| FR-10 | Legal Mapping & Statutory Reference Management | TC-FOR-FR10-001, TC-FOR-FR10-002, TC-FOR-FR10-003, TC-FOR-FR10-004, TC-FOR-FR10-005 | Covered |
| FR-11 | Report Composition, Approval, Redaction & Export | TC-FOR-FR11-001, TC-FOR-FR11-002, TC-FOR-FR11-003, TC-FOR-FR11-004, TC-FOR-FR11-005 | Covered |
| FR-12 | DOPAMS Integration & Synchronization | TC-FOR-FR12-001, TC-FOR-FR12-002, TC-FOR-FR12-003, TC-FOR-FR12-004, TC-FOR-FR12-005 | Covered |
| FR-13 | Alerts, Notifications & Escalations | TC-FOR-FR13-001, TC-FOR-FR13-002, TC-FOR-FR13-003, TC-FOR-FR13-004, TC-FOR-FR13-005 | Covered |
| FR-14 | Identity, RBAC & Session Security | TC-FOR-FR14-001, TC-FOR-FR14-002, TC-FOR-FR14-003, TC-FOR-FR14-004, TC-FOR-FR14-005 | Covered |
| FR-15 | MIS, Analytics & Operational Reporting | TC-FOR-FR15-001, TC-FOR-FR15-002, TC-FOR-FR15-003, TC-FOR-FR15-004, TC-FOR-FR15-005 | Covered |
| FR-16 | Configuration, Template & Model Governance | TC-FOR-FR16-001, TC-FOR-FR16-002, TC-FOR-FR16-003, TC-FOR-FR16-004, TC-FOR-FR16-005 | Covered |
| FR-17 | Retention, Archive & Purge Approval | TC-FOR-FR17-001, TC-FOR-FR17-002, TC-FOR-FR17-003, TC-FOR-FR17-004, TC-FOR-FR17-005 | Covered |

### Cross-Cutting Concern Coverage

| Concern | Covered By Test Cases |
|---|---|
| DOPAMS Integration | TC-FOR-FR01-004, TC-FOR-FR12-001 through TC-FOR-FR12-005 |
| Chain of Custody | TC-FOR-FR04-001, TC-FOR-FR04-002, TC-FOR-FR04-005, TC-FOR-FR11-001 |
| Immutability / Evidence Preservation | TC-FOR-FR02-001, TC-FOR-FR02-005, TC-FOR-FR04-001, TC-FOR-FR04-004, TC-FOR-FR17-002 |
| RBAC / Role-Based Access | TC-FOR-FR01-003, TC-FOR-FR04-003, TC-FOR-FR10-002, TC-FOR-FR14-001 through TC-FOR-FR14-005, TC-FOR-FR15-004 |
| Idempotency | TC-FOR-FR02-003, TC-FOR-FR12-002 |
| Audit Trail | TC-FOR-FR04-001 through TC-FOR-FR04-005, TC-FOR-FR08-003, TC-FOR-FR17-003 |
| MFA / Step-Up Auth | TC-FOR-FR14-001, TC-FOR-FR14-003 |
| Multilingual / Unicode | TC-FOR-FR06-001, TC-FOR-FR07-005 |
| Failure Handling / Resilience | TC-FOR-FR02-005, TC-FOR-FR03-003, TC-FOR-FR04-004, TC-FOR-FR06-003, TC-FOR-FR06-004, TC-FOR-FR07-004, TC-FOR-FR09-004, TC-FOR-FR12-003, TC-FOR-FR17-005 |
| Configuration Governance | TC-FOR-FR16-001 through TC-FOR-FR16-005, TC-FOR-FR09-005 |
| React Frontend (UI-specific) | TC-FOR-FR01-003, TC-FOR-FR02-001, TC-FOR-FR05-001 through TC-FOR-FR05-005, TC-FOR-FR08-002, TC-FOR-FR13-001 |
| Node.js API (API-specific) | TC-FOR-FR01-002, TC-FOR-FR02-002, TC-FOR-FR02-003, TC-FOR-FR11-003, TC-FOR-FR14-004 |
| Forensic Tool Coverage (UFED, XRY, Oxygen, FTK, AXIOM, Belkasoft) | TC-FOR-FR02-001, TC-FOR-FR03-001, TC-FOR-FR03-002 |
| Session Management | TC-FOR-FR14-004 |
| SLA Timers / Escalation | TC-FOR-FR13-001, TC-FOR-FR13-003 |

---

### Appendix A: Role Reference

| Role Code | Role Name | Permission Level |
|---|---|---|
| ROL-001 | Administrator | PL-5 Admin |
| ROL-002 | Supervisor / Approver | PL-4 Supervisory |
| ROL-003 | Forensic Analyst | PL-3 Analyst |
| ROL-004 | Investigator / Case Officer | PL-2 Controlled Read / Request |
| ROL-005 | Auditor / Legal Reviewer | PL-2 Audit Read |
| ROL-006 | Integration Service | PL-SVC Service Account |
| ROL-007 | Configuration Manager | PL-4 Config Admin |
| ROL-008 | Support / Hypercare Operator | PL-2 Ops Support |

### Appendix B: Artifact Type Reference

| Artifact Type | Description |
|---|---|
| Message | Text messages (SMS, chat, email body) |
| CallLog | Voice/video call records |
| Contact | Address book entries |
| MediaImage | Photographs, screenshots |
| MediaVideo | Video files |
| Audio | Audio recordings, voicemail |
| Document | PDF, Word, Excel files |
| BrowsingHistory | Browser history, bookmarks |
| AppUsage | Application usage logs |
| SocialArtifact | Social media posts, interactions |
| FinancialIndicator | UPI, bank transactions, wallet activity |
| IdentityDocument | Aadhaar, PAN, passport scans |
| DerivedText | OCR output, extracted text |
| Unknown | Unclassified artifacts |

### Appendix C: Forensic Tool Reference

| Tool Code | Tool Name |
|---|---|
| UFED | Cellebrite UFED |
| XRY | MSAB XRY |
| Oxygen | Oxygen Forensic Detective |
| FTK | AccessData FTK |
| AXIOM | Magnet AXIOM |
| Belkasoft | Belkasoft Evidence Center |
| Generic | Configurable generic adapter |

### Appendix D: Priority Level Definitions

| Priority | Label | Definition |
|---|---|---|
| P1 | Critical | Core functionality that must work for the system to be usable. Blocks release if failing. |
| P2 | High | Important functionality for daily operations. Should be resolved before release. |
| P3 | Medium | Edge cases and secondary flows. Can be deferred to a patch release if necessary. |
| P4 | Low | Cosmetic, non-blocking edge cases. Can be addressed in future sprints. |

---

*End of Document*
