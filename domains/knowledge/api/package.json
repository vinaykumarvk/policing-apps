{
  "name": "@puda/knowledge-api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm --prefix ../../.. --workspace @policing-platform/authz run build && tsc -p tsconfig.json",
    "typecheck": "npm --prefix ../../.. --workspace @policing-platform/authz run build && tsc -p tsconfig.json --noEmit",
    "test": "npm --prefix ../../.. --workspace @policing-platform/authz run build && vitest run"
  },
  "dependencies": {
    "@policing-platform/authz": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.0"
  }
}
